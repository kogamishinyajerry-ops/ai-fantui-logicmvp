# Workbench Annotation Proposals

Runtime `AnnotationProposal` JSON records are written here during local Workbench use. Proposal records are intentionally ignored by Git; keep only this README, `.gitignore`, and `.gitkeep` in the repository.
