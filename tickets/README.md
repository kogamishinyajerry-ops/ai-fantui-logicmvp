# Workbench Tickets

Runtime ticket payload JSON files are written here by the local Workbench ticket publisher. Ticket records are intentionally ignored by Git; commit only this README, `.gitignore`, and `.gitkeep`.
