.DS_Store
.env
.env.*
!.env.example

__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

.venv/
venv/
dist/
build/
*.egg-info/

.codex/
*.log

artifacts/workbench-bundles/
onboarding验证/

# Generated process artifacts (archived to GitHub 2026-04-24, keep gitignored locally)
tmp/
output/
# P44-03: change-proposal store (per-session demo state, not source-of-truth)
.planning/proposals/
# P44-05: dev-queue handoff briefs (per-accept demo state; Claude Code reads
# these in a later /gsd-execute-phase session and deletes them on completion)
.planning/dev_queue/

# P51-03: governance decision history (per-host demo state, written
# at runtime by the orchestrator's governance gate)
data/governance_decisions.jsonl
