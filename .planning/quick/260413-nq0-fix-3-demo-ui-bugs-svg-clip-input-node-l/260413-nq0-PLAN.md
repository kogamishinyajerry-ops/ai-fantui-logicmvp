---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/well_harness/static/demo.html
  - src/well_harness/controller_adapter.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "SVG viewBox no longer clips TRA text at y=432"
    - "Input nodes (sw1, sw2, RA, ENG, GND, EEC En, N1K, TRA, Inhibit) light up in the frontend"
    - "NotDep node and its 4th connection line to L1 gate are removed from the SVG"
  artifacts:
    - path: "src/well_harness/static/demo.html"
      contains: "viewBox=\"0 0 900 480\""
    - path: "src/well_harness/static/demo.html"
      contains: "NotDep"
      not_contains: "data-node=\"reverser_not_deployed_eec\""
    - path: "src/well_harness/controller_adapter.py"
      contains: "sw1"
      contains: "radio_altitude_ft"
      contains: "engine_running"
---

<objective>
Fix 3 demo UI bugs: (1) SVG viewBox clips TRA text, (2) input nodes stay gray, (3) remove NotDep SVG node.
</objective>

<context>
@src/well_harness/static/demo.html
@src/well_harness/controller_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SVG viewBox height to prevent clipping</name>
  <files>src/well_harness/static/demo.html</files>
  <action>
Change the SVG viewBox attribute on line 327 from `viewBox="0 0 900 320"` to `viewBox="0 0 900 480"` in the thrust-reverser chain topology SVG. The TRA value text at line 386 has y="432", which is clipped by the current height of 320. Increasing to 480 gives enough headroom for all input nodes (the lowest input node, TRA, ends at y=432).
</action>
  <verify>
  <automated>grep -n 'viewBox="0 0 900 480"' src/well_harness/static/demo.html</automated>
  </verify>
  <done>viewBox="0 0 900 480" present in thrust-reverser SVG</done>
</task>

<task type="auto">
  <name>Task 2: Add input conditions to asserted_component_values so frontend can light them up</name>
  <files>src/well_harness/controller_adapter.py</files>
  <action>
In the `evaluate_snapshot()` method of `ReferenceDeployControllerAdapter` (lines 114-142), add the 9 raw input conditions to `asserted_component_values` so that the frontend `applySystemNodeStates()` can read and display their values. The inputs are: radio_altitude_ft, sw1, sw2, engine_running, aircraft_on_ground, eec_enable, n1k, tra_deg, reverser_inhibited. These come directly from the `inputs` object already extracted from the snapshot. Append them to the existing asserted_component_values dict (which currently only has output commands).
</action>
  <verify>
  <automated>grep -c "sw1.*outputs" src/well_harness/controller_adapter.py</automated>
  </verify>
  <done>asserted_component_values includes all 9 input conditions plus existing output commands</done>
</task>

<task type="auto">
  <name>Task 3: Remove NotDep SVG node and its L1 connection line</name>
  <files>src/well_harness/static/demo.html</files>
  <action>
Remove the NotDep (reverser_not_deployed_eec) SVG node from the thrust-reverser chain topology. It is NOT an independent L1 input — it is derived from deploy_position_percent==0 (a plant output). Remove all 4 NotDep elements:
1. The rect at lines 358-360: `<rect x="10" y="138" ... data-node="reverser_not_deployed_eec"/>`
2. The label text at lines 358-360: `<text x="46" y="148" ...>NotDep</text>`
3. The value text at lines 358-360: `<text x="46" y="162" ... data-value-for="reverser_not_deployed_eec">—</text>`
4. The 4th L1 input connection line at line 456: `<line x1="82" y1="153" x2="110" y2="70" class="conn-line conn-input"/>`

Also remove the "NotDep" section label if it exists in the input conditions label area.
</action>
  <verify>
  <automated>grep -c 'data-node="reverser_not_deployed_eec"' src/well_harness/static/demo.html</automated>
  </verify>
  <done>reverser_not_deployed_eec node and NotDep label/value removed from SVG; 4th L1 connection line removed</done>
</task>

</tasks>

<success_criteria>
- viewBox height changed from 320 to 480 in thrust-reverser SVG
- 9 input conditions present in asserted_component_values (sw1, sw2, radio_altitude_ft, engine_running, aircraft_on_ground, eec_enable, n1k, tra_deg, reverser_inhibited)
- NotDep SVG node removed from thrust-reverser topology
- 4th L1 connection line (NotDep→L1 gate) removed
</success_criteria>

<output>
After completion, create `.planning/quick/260413-nq0-fix-3-demo-ui-bugs-svg-clip-input-node-l/260413-nq0-SUMMARY.md`
</output>
