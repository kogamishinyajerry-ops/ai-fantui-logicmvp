---
phase: 09-add-system-switcher-smoke
plan: '01'
type: execute
wave: '1'
depends_on: []
files_modified:
  - tests/test_system_switcher_smoke.py
  - tools/run_gsd_validation_suite.py
autonomous: true
requirements: []
must_haves:
  truths:
    - Headless Chromium can load the demo UI and find the system switcher
    - System switcher exposes exactly 3 options: thrust-reverser, landing-gear, bleed-air
    - Switching systems shows the correct topology SVG and hides the others
    - Monitor section is visible with at least one checkbox rendered
    - No console errors occur during page load and switching
  artifacts:
    - path: tests/test_system_switcher_smoke.py
      provides: Headless Playwright smoke test for system switcher UI
      min_lines: 60
    - path: tools/run_gsd_validation_suite.py
      provides: New ValidationCommand for the smoke test
      modifies: build_default_commands()
  key_links:
    - from: tests/test_system_switcher_smoke.py
      to: http://localhost:7891/
      via: playwright.sync_api.Page.goto
      pattern: page.goto.*7891
    - from: tools/run_gsd_validation_suite.py
      to: tests/test_system_switcher_smoke.py
      via: ValidationCommand argv
      pattern: test_system_switcher_smoke
---

<objective>
Create a headless Playwright smoke test that verifies the demo UI system switcher works correctly: page loads, 3 system options exist, switching between systems shows the correct topology SVG, monitor section has checkboxes, and no console errors occur. Then wire it into the shared validation suite.
</objective>

<context>
@tools/demo_path_smoke.py
@tools/run_gsd_validation_suite.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright smoke test for system switcher</name>
  <files>tests/test_system_switcher_smoke.py</files>
  <action>
Create `tests/test_system_switcher_smoke.py` using `from playwright.sync_api import sync_playwright`.

Structure:
- `DEMO_URL = "http://localhost:7891/"`
- A `run_smoke()` function (no args) that returns `(exit_code: int, report: dict, text_lines: list[str])` — same contract as `demo_path_smoke.run_smoke_suite()`.
- Inside `run_smoke()`, launch headless Chromium via `sync_playwright()` context manager, navigate to `DEMO_URL`.
- Collect console errors via `page.on("console", ...)` listener (only ERROR-level).
- Scenarios to verify (fail on first error, return 1):
  1. Page loads — `page.title()` is non-empty.
  2. System selector exists: `page.locator("#system-selector")`.
  3. System selector has exactly 3 options: count options in `#system-selector`, values must be `["thrust-reverser", "landing-gear", "bleed-air"]` in that order.
  4. Switching to `landing-gear` shows `#chain-topology-landing-gear` (display != none) and hides the other two topologies.
  5. Switching to `bleed-air` shows `#chain-topology-bleed-air` and hides others.
  6. Switching back to `thrust-reverser` shows `#chain-topology-thrust-reverser` and hides others.
  7. Monitor section visible: `page.locator(".monitor-panel")` is visible.
  8. Monitor checkboxes rendered: `page.locator("#monitor-series-checkboxes input[type=checkbox]")` count > 0.
  9. No console errors collected during the above steps.
- On pass: return `(0, report, text_lines)` where `report` has `"status": "pass"` and `text_lines` contains `"PASS: system switcher smoke"` plus one line per scenario.
- On fail: return `(1, report, text_lines)` with `"status": "fail"` and a line describing the first failure.
- Add `if __name__ == "__main__": raise SystemExit(main())` pattern at bottom with a `main()` that calls `run_smoke()` and prints text_lines then returns the exit code.
- Use `--format json` argv support (like `demo_path_smoke.py`) so the suite can call it with `--format json` and get JSON output. When `--format json` is passed, print the report dict as JSON and exit with the code from `run_smoke()`.
</action>
  <verify>
  <automated>cd /Users/Zhuanz/20260407\ YJX\ AI\ FANTUI\ LogicMVP && python3 tests/test_system_switcher_smoke.py --format json; echo "exit:$?"</automated>
  </verify>
  <done>Headless Playwright smoke test passes against the demo UI, exit 0, no console errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add smoke test to validation suite</name>
  <files>tools/run_gsd_validation_suite.py</files>
  <action>
In `tools/run_gsd_validation_suite.py`, add a new `ValidationCommand` to `build_default_commands()`:

```python
ValidationCommand(
    "system_switcher_smoke",
    (python, "tests/test_system_switcher_smoke.py", "--format", "json"),
),
```

Insert it after the existing `demo_path_smoke` entry (keep commands in logical order).
</action>
  <verify>
  <automated>cd /Users/Zhuanz/20260407\ YJX\ AI\ FANTUI\ LogicMVP && python3 tools/run_gsd_validation_suite.py --format json 2>&1 | python3 -c "import sys,json; d=json.load(sys.stdin); ok=[r for r in d['results'] if r['status']=='pass']; print(f'PASS: {len(ok)}/{d[\"command_count\"]}')"</automated>
  </verify>
  <done>New validation command registered; suite includes system_switcher_smoke in its default command list.</done>
</task>

</tasks>

<verification>
Run the test standalone against the already-running demo server on port 7891. The test must exit 0 when all checks pass.
</verification>

<success_criteria>
- `tests/test_system_switcher_smoke.py` exists, uses `sync_playwright`, covers all 9 check items, returns exit 0/1 and JSON/text output.
- `tools/run_gsd_validation_suite.py` includes `system_switcher_smoke` in `build_default_commands()`.
- Both files pass their automated verify commands.
</success_criteria>

<output>
After completion, create `.planning/quick/260413-p9i-add-headless-playwright-smoke-test-for-s/260413-p9i-01-SUMMARY.md`
</output>
