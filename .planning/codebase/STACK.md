# Stack

- Python package with `setuptools`.
- Python 3.9+ runtime target.
- Standard-library local HTTP demo server.
- Static HTML/CSS/JS cockpit UI.
- `unittest` test suite.
- Optional `jsonschema` validation helpers.
