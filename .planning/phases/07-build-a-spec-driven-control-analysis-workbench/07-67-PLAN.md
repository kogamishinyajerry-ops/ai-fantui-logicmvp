# P7-67 Plan - Add Controller Truth Adapter Metadata Schema Validation To Shared Suite

## Objective

Promote the controller truth adapter metadata contract into the shared validation suite, so the adapter identity/source-of-truth boundary is protected beside the root spec and downstream workbench artifact contracts.

## Context

- `P7-66` published `docs/json_schema/controller_truth_adapter_metadata_v1.schema.json` and made reference adapter metadata schema-aware.
- That protection currently lives in focused adapter tests; the shared validation suite does not yet run a standalone adapter metadata contract check.
- A shared check should also prove the reference workbench spec's `source_of_truth` remains aligned with the reference adapter metadata, preventing quiet drift between spec and truth boundary.

## Tasks

1. Add a standalone controller-truth-adapter metadata schema validation script.
2. Add a cross-contract check that the reference spec `source_of_truth` matches adapter metadata.
3. Add regression coverage, update the validation-suite command inventory, and re-run focused/shared validation before Notion sync.

## Success Criteria

- The repo exposes a stable controller-truth-adapter metadata schema validation entry point.
- Validation coverage proves adapter metadata and the reference spec source-of-truth boundary cannot silently diverge.
- `run_gsd_validation_suite.py --format json` passes with controller-truth-adapter metadata schema validation included.
