# P7-64 Plan - Add Control System Spec Schema Validation To Shared Suite

## Objective

Promote the root control-system spec contract into the shared validation suite, so the system-agnostic spec schema stays protected before downstream playback, diagnosis, knowledge, and bundle artifacts consume it.

## Context

- `P7-51` published `docs/json_schema/control_system_spec_v1.schema.json` and made generated specs schema-aware.
- Later P7 work promoted playback, diagnosis, knowledge, and bundle contracts into shared validation checks, but the root spec contract itself still lived only in focused regression tests.
- The highest-value next convergence step is to validate both the reference spec export and intake-generated second-system specs through a standalone shared-suite entry point.

## Tasks

1. Add a standalone control-system-spec schema validation script that exercises the CLI reference spec and ready intake-generated specs.
2. Add regression coverage for the new validation script and update the validation-suite command inventory.
3. Re-run focused and shared validation checks, then sync the plan through the Notion control plane.

## Success Criteria

- The repo exposes a stable control-system-spec schema validation entry point.
- Validation coverage proves the new script passes in JSON/text mode and integrates with the shared suite inventory.
- `run_gsd_validation_suite.py --format json` passes with control-system-spec schema validation included.
