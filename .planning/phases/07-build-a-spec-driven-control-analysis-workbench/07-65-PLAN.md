# P7-65 Plan - Add Fault Taxonomy Schema Validation To Shared Suite

## Objective

Promote the reusable fault taxonomy contract into the shared validation suite, so supported `fault_kind` semantics stay aligned across taxonomy payloads and the root control-system spec schema.

## Context

- `P7-53` published `docs/json_schema/fault_taxonomy_v1.schema.json` and made intake/diagnosis reject unknown fault kinds.
- `P7-64` promoted the root control-system spec schema into shared validation, but the taxonomy contract still only had focused regression coverage.
- The next highest-value convergence step is a standalone shared-suite check that validates the taxonomy payload and proves `control_system_spec_v1` carries the same `faultKindValue.enum`.

## Tasks

1. Add a standalone fault-taxonomy schema validation script.
2. Add a cross-contract check that the control-system spec schema's `faultKindValue.enum` matches `SUPPORTED_FAULT_KINDS`.
3. Add regression coverage, update the validation-suite command inventory, and re-run focused/shared validation before Notion sync.

## Success Criteria

- The repo exposes a stable fault-taxonomy schema validation entry point.
- Validation coverage proves the taxonomy payload and root spec enum cannot silently diverge.
- `run_gsd_validation_suite.py --format json` passes with fault-taxonomy schema validation included.
