# P7-69 Plan - Publish Second System Smoke Report Contract

## Objective

Turn the second-system smoke proof into a formal schema-aware report, so generalization evidence is a machine-readable artifact instead of only an ad hoc CLI payload.

## Context

- `P7-54` added the reusable second-system smoke proof against the custom reverse-control packet.
- Later P7 work promoted root spec, fault taxonomy, adapter metadata, playback, diagnosis, knowledge, bundle, and archive contracts into schema-aware artifacts.
- The second-system smoke report is now the main proof that a non-reference packet can traverse intake -> clarification -> playback -> diagnosis -> knowledge, so it should also expose `$schema` / `kind` / `version`.

## Tasks

1. Add schema-aware serialization for `SecondSystemSmokeReport`.
2. Publish `docs/json_schema/second_system_smoke_v1.schema.json`.
3. Add focused regression coverage proving the CLI and generated report payload match the published schema.

## Success Criteria

- `second-system-smoke --format json` exposes a stable `$schema` / `kind` / `version` contract.
- The second-system smoke schema documents the generalization proof payload.
- Regression coverage proves the generated smoke report validates against the published schema.
