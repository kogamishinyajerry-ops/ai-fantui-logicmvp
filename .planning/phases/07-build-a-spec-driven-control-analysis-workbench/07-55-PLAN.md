# P7-55 Plan - Add Second-System Smoke To Shared Validation Suite

## Objective

Promote the new second-system smoke proof into the shared validation suite, so the repo's repeatable baseline automatically checks a non-reference control system on every full validation pass.

## Context

- `P7-54` added `well_harness second-system-smoke`, proving the custom reverse-control packet can traverse the full intake->bundle->playback->diagnosis->knowledge workflow.
- But until that proof joins the shared validation suite, a future change could silently break the second-system path while the reference-system and schema checks continue to pass.
- The next strongest move is to treat second-system smoke as a first-class shared validation check, alongside the existing unit, schema, demo-smoke, and Notion control-plane checks.

## Tasks

1. Add the second-system smoke CLI command to `tools/run_gsd_validation_suite.py`.
2. Update validation-suite regression coverage so the expected shared check list includes the second-system smoke check.
3. Re-run the real shared suite and confirm all checks pass with the new ninth validation command.

## Success Criteria

- The shared validation suite runs `second-system-smoke` as part of the default check list.
- Validation-suite unit tests expect the new shared check.
- A real `run_gsd_validation_suite.py --format json` execution passes with the second-system smoke command included.
