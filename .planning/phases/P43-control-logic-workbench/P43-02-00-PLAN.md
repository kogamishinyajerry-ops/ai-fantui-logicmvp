---
phase: P43
batch: Batch-2 (P43-02 + P43-03 + P43-04 · combined per Q1=D gate batching)
plan: P43-02-00
plan_revision: v1 (draft · pre Codex r1)
title: P43-02 Batch — Orchestrator + Document Pipeline + Freeze Gate (combined plan)
status: DRAFT · awaiting Codex r1 adversarial review
date: 2026-04-21
owner: Claude App Opus 4.7 (Solo Executor) · v5.2 solo-signed + v5.3 addendum
scope_tier: Tier 1 · adapter-boundary write (ai_doc_analyzer.py + generate_adapter.py write · workbench.js authority-contract mechanical)
verified-by: pending Codex r1 post-plan adversarial review (Q7=A · per-touchpoint)
preconditions:
  - `GATE-P43-PLAN (v7): Approved` (Kogami 2026-04-20 · main `99211bd`)
  - `GATE-P43-01-CLOSURE: Approved` (Kogami 2026-04-21 · main `61b12b3`)
  - P43-00-PLAN.md v7 + P43-01-00-PLAN.md v5 both frozen on main
  - P43-01 delivered: `docs/P43-api-contract-lock.yaml` + `docs/P43-contract-proof-report.md` + Bugs A/B1/B2 fixed in `src/well_harness/ai_doc_analyzer.py`
  - Bug D still deferred at `ai_doc_analyzer.py:799` (fix lands in this Batch · P43-03 sub-phase)
  - Non-blocking polish residual at `src/well_harness/demo_server.py:2666` (candidate for P43-02 or standalone slice)
  - Q lock from P43-00: Q1=D · Q2=A · Q4=A · Q7=A · Q8=B · Q10=B · Q12=B+a
non-goals:
  - 不执行 P43-05..P43-10（本 Batch = 02+03+04 only · Batch-3/4 留待后续 Kogami 重审）
  - 不改 P43-00 v7 plan · 不改 P43-01 v5 plan
  - 不新建 `workbench/orchestrator.py` / `workflow_engine.py` / `draft_truth.py` / `state.yaml` / `workflow.db` · 任何新持久化源（P43-00 §3d blacklist 硬规则）
  - 不新建前端 SPA shell / 替换 `workbench.js` 框架（vanilla JS 继承 · Q2=A · §3d blacklist）
  - 不 bump 既有 JSON schema `$id`（P42 已锁 · §3d blacklist）
  - 不引入 Alpine.js / HTMX / React / Vue 等前端框架
  - 不实装 R3 `test_r3_runtime_mutation_blocked` 作为默认 lane 硬性（opt-in e2e only · 源级 deepFreeze 存在即 mechanical closure · P43-00 §3e v5 已定）
  - 不实装 R5 `test_r5_validator_node_parity_mandatory` 作为 P43-02 Exit Criteria（归 P43-09 Final Approval 一次性 behavior 证 · P43-00 §3e v7 strengthen directive）
  - 不新增 `generate_adapter.py` 中的 domain hardcode 或 "hardcode 开关"（§3d L3 禁 · v4 收紧）
  - 不以"列出断裂"为通过条件（P43-00 non-goal #16 硬规则 · 延伸到本 Batch · 每 R1-R6 test 必 asserted_pass · 每 P43-03 ID fix 必经 regression 锁）
  - 不在 P43-02/03/04 任一 sub-phase commit 上跳过 Codex review（Q7=A · per-touchpoint · 每个 adapter-boundary commit 必调）
  - 不把 P43-03 Bug D fix 做成 "rename only"：必须证明 `_inject_clarification_answers` 与 `_unanswered_clarifications` 契约 closure（runtime assert · 非仅 source-level rename）
  - 不把 P43-04 Appendix A 做成 "静态模板拷贝"：必须从 intake clarification_answers + ambiguity list 中动态生成 open-question 登记
---

# P43-02 Batch · Orchestrator + Document Pipeline + Freeze Gate — v1 (draft)

## 0. TL;DR

Batch-2 combined plan per Q1=D. Covers P43-02 (workflow automaton + authority contract R1-R6 + archive compat + multi-tab lock + API contract regression lock + one of two `demo_server.py:2666` polish options), P43-03 (PDF/docx server-side extraction + Bug D fix), P43-04 (FREEZE event handling + Appendix A dynamic generator). Single Kogami gate: **GATE-P43-02-BATCH-CLOSURE**.

Scope discipline: follow P43-00 §3d whitelist strictly (Source/Doc/Test/Tooling). All new file creation explicitly authorized in §3. Q7=A honored per-touchpoint.

**Estimated size**: ~2200-2800 LOC (source + tests + docs). ~3-4 days wall-time at Solo Executor cadence. 3 sub-phase commits × ~2 Codex review rounds each = ~6 reviews minimum.

## 1. Scope per sub-phase

### 1a · P43-02 · Orchestrator + authority contract

**Authority anchors**: P43-00 §3c (workflow automaton) · §3d (archive/API compat) · §3e (R1-R6 mechanical).

Must-land deliverables:

- **Workflow automaton contract** (§3c): state enum (17 states) + event list (10+ events) + transition table + error taxonomy (6 error types) + idempotency rules + persistence model (two-source: localStorage + manifest; no third source) + side-effect ordering (Final Approval 4-step rollback-safe) + cross-phase integration test ownership declarations.
- **Authority contract R1-R6 mechanical verification** (§3e): 12 test functions (R1: `test_r1_backend_no_draft_state_emission` + `test_r1_helper_payload_builders_no_draft` + `test_r1_handler_call_closure` · R2: `test_r2_final_approval_no_draft_read` · R3: `test_r3_controlled_writer_only` + `test_r3_no_draft_origin` + `test_r3_deepfreeze_enforced` + `test_r3_reiterate_state_transition` · R4: `test_r4_generator_source_grep` + `test_r4_generator_output_invariance` · R5: `test_r5_validator_exists` + `test_r5_validator_fixture_required_substrings` · R6: `test_r6_final_approval_handler_removes_draft` + `test_r6_archive_excludes_draft` · Observability: `test_observability_logdraftwrite_defined` + `test_draft_frozen_badge_html_exists`).
- **`cli.py` extension**: add `workbench new | continue | status` subcommands (L1 additive · §3d).
- **`workbench.js` extension**: add `draftDesignState` key + `assignFrozenSpec(newSpec, {origin})` controlled accessor + `deepFreeze()` helper + `validateDraftAgainstFrozen(draft, frozen)` validator + `// P43: final-approval-handler-begin`/`end` convention markers + `// P43: validate-draft-begin`/`end` convention markers (L1 additive · §3d).
- **`workbench.debug.js`** (new file · Tooling Whitelist): `logDraftWrite(writer_step, timestamp, diff_summary)`.
- **`workbench.html`** extension: `#draft-frozen-diff-badge` DOM + Final Approval handler section (L1 additive · §3d).
- **`tools/check_authority_contract.py`** (new · Tooling Whitelist): static-scan executor; invoked by R1-R6 tests AND pre-commit hook AND CI workflow.
- **`.github/workflows/p43-authority-contract.yml`** (new · Tooling Whitelist): CI job running authority-contract tests + static scan.
- **`.pre-commit-config.yaml`** (new-or-L1-extend · Tooling Whitelist): pre-commit hook invoking `check_authority_contract.py`.
- **Archive backward-compat test** (`tests/test_p43_archive_backward_compat.py`): build pre-P43 archive fixture → restore via `workbench_bundle.py` restore path → assert no migration script needed.
- **API contract-lock regression test** (`tests/test_p43_api_contract_lock.py`): parse `docs/P43-api-contract-lock.yaml` · for each endpoint, send representative request and assert response shape matches YAML.
- **Multi-tab lock test** (`tests/test_p43_multi_tab_lock.py`): simulate C13 cross-tab scenario via localStorage BroadcastChannel polyfill or headless-browser (OPEN question Q2-MT · see §Q).
- **Docs** (new · Doc Deliverables Whitelist): `docs/P43-workflow-automaton-contract.md` + `docs/P43-workflow-automaton.yaml` + `docs/P43-authority-contract-verification.md` + `docs/P43-archive-compat-report.md`.

Also in P43-02: close the non-blocking polish at `src/well_harness/demo_server.py:2666` (error message "apply_all_safe must be true" → soften to "must be truthy" OR harden handler to strict bool) — see §Q Q5.

### 1b · P43-03 · Document pipeline + Bug D fix

**Authority anchors**: P43-00 Q12=B+a (server-side pypdf + python-docx · no OCR) · §3d L2 allowing `ai_doc_analyzer.py` adds PDF/DOCX server-side reader.

Must-land deliverables:

- **Server-side document extraction**: `src/well_harness/ai_doc_analyzer.py` L1 adds `extract_document_text(file_bytes, content_type)` function using `pypdf` for PDF and `python-docx` for DOCX. Falls back to text decode for `text/plain` / `text/markdown`.
- **New endpoint** (L1 additive · `demo_server.py`): `POST /api/document/extract` — accepts binary upload, returns `{document_text, extraction_method, warnings}` or `{error, message}`.
- **Frontend wiring** (L1 additive · `static/ai-doc-analyzer.js`): replace `reader.readAsText(file)` at line 224 with `fetch('/api/document/extract', {method: 'POST', body: file})`; keep fallback for `.txt`/`.md` via browser-side read (content-type guard).
- **Bug D fix** (L2 · `ai_doc_analyzer.py:799`): `_inject_clarification_answers` rewrite — map session_clarification_history tuples to the stable question_ids from `default_workbench_clarification_questions()` at `system_spec.py:244` (source-order mapping: first answer → first stable ID · 2nd → 2nd · etc.) instead of `clarify-{i}`.
- **Bug D regression tests** (new · `tests/test_p43_clarification_stable_ids.py`): (i) session-path flow accepts answers and ready_for_spec_build=True; (ii) legacy `clarify-{i}` inputs still tolerated as graceful fallback if any exist.
- **Document pipeline tests** (new · `tests/test_p43_document_pipeline.py`): PDF/DOCX fixtures → extract via new endpoint → assert text content present + known marker strings; error branches (corrupted PDF, encrypted PDF, unsupported type).
- **API contract-lock YAML update** (L2 on `docs/P43-api-contract-lock.yaml`): add `/api/document/extract` endpoint entry with request/response/error branches.

### 1c · P43-04 · Freeze gate + Appendix A generator

**Authority anchors**: P43-00 §1 user-journey Step 2c · §3b · §3c FREEZE state.

Must-land deliverables:

- **FREEZE event implementation** (L1 additive): workflow automaton FREEZE event transitions from `AWAITING_ANSWERS` (or `PARSING` if all answers already present) to `FROZEN` · guarded by "all clarifications answered OR explicitly-waived with operator alias".
- **Frozen spec hash binding** (L1 additive · `workbench.js`): on FREEZE event, compute SHA-256 of the frozen spec JSON (stable-serialized via sorted keys) and store alongside spec in localStorage under `wellHarnessWorkbench.v1.frozenSpecSHA.{system_id}`.
- **Appendix A generator** (L1 additive · `src/well_harness/tools/appendix_a_generator.py` · new file — **needs P43-00 §3d whitelist delta** · see §Q Q1): given frozen spec + intake clarification_answers + ambiguity list, produce `docs/<system>/traceability_matrix.md` with open-questions registry using the P34-P42 template (pattern from `docs/c919_etras/` precedent).
- **CLI subcommand** (L1 on `cli.py`): `workbench freeze <system_id> --alias <operator>` invokes the generator + stamps localStorage state.
- **FREEZE state transition test** (new · `tests/test_p43_freeze_gate.py`): parameterized over legal/illegal pre-states; asserts post-state = FROZEN only from legal pre-states; asserts Appendix A file content includes every open question.
- **Appendix A template test** (new · reuses `tests/test_p43_freeze_gate.py`): fixture spec with 3 open questions → generator emits a markdown file with 3 rows in the registry table + SHA header + operator-alias footer.

### 1d · Scope discipline

- Must-land across Batch-2: (i) all R1-R6 tests PASS default lane; (ii) archive backward-compat PASS; (iii) API contract-lock regression PASS against latest endpoint enumeration; (iv) multi-tab lock PASS per Q2-MT decision; (v) document pipeline PASS for PDF + DOCX + text + error branches; (vi) Bug D regression test PASS; (vii) FREEZE transition PASS; (viii) Appendix A generator PASS.
- Report-only: none in this Batch (all findings must be fixed in-scope; nothing deferred further — everything deferred by P43-01 to P43-03 lands here).
- 发现新 Counter-F-class bug → 登记报告 Additional Findings + Kogami R4 arbitration (path ① precedent); don't auto-expand scope without arbitration.
- Any sub-phase Step blocked > 2 hours → stop · escalate Kogami.

## 2. Deliverables (per §3d whitelist · strict compliance)

### 2a · Source Code (L1/L2 per §3d ladder)

| File | Ladder | Scope |
|------|--------|-------|
| `src/well_harness/cli.py` | L1 | `workbench new | continue | status | freeze` subcommands |
| `src/well_harness/workbench_bundle.py` | L1 | optional manifest fields for P43 state-machine traces (if any) · add nothing that breaks P42 restore |
| `src/well_harness/static/workbench.js` | L1 | `draftDesignState` key · `assignFrozenSpec` · `deepFreeze` · `validateDraftAgainstFrozen` · convention markers · `frozenSpecSHA` storage |
| `src/well_harness/static/workbench.html` | L1 | `#draft-frozen-diff-badge` + Final Approval handler section |
| `src/well_harness/static/workbench.css` | L1 | classes for draft/frozen diff badge + Final Approval section |
| `src/well_harness/static/ai-doc-analyzer.js` | L1/L2 | L1 replace `readAsText` with `fetch /api/document/extract` path · L2 fallback for `.txt`/`.md` |
| `src/well_harness/static/ai-doc-analyzer.html` | L1 | file-type indicator / extraction progress |
| `src/well_harness/static/ai-doc-analyzer.css` | L1 | additions only |
| `src/well_harness/demo_server.py` | L1/L2 | L1 add `/api/document/extract` + `/api/workbench/freeze` (if any) · L2 soften `demo_server.py:2666` message per Q5 |
| `src/well_harness/ai_doc_analyzer.py` | L1/L2 | L1 add `extract_document_text` · L2 rewrite `_inject_clarification_answers` stable-ID contract (Bug D) |
| `src/well_harness/document_intake.py` | L1/L2 | L1 add `validate_clarification_answer_ids` helper if needed · L2 allow tolerance of unknown question_id with warning |
| `src/well_harness/tools/generate_adapter.py` | — | read-only per R4 · no changes beyond frozen-spec-kwargs extension (already §3d L2-allowed but not planned to invoke) |

### 2b · Doc Deliverables (new · §3d Doc Deliverables Whitelist)

| File | Scope | Size |
|------|-------|------|
| `docs/P43-workflow-automaton-contract.md` | §3c state enum + events + transitions + error taxonomy + idempotency rules + persistence model + side-effect ordering · prose description | ~350 lines |
| `docs/P43-workflow-automaton.yaml` | Machine-readable companion to above · runtime state-transition validation · used by `test_p43_workflow_automaton.py` | ~200 lines |
| `docs/P43-authority-contract-verification.md` | §3e R1-R6 verification plan · per-rule test function mapping · source-grep patterns · CI integration | ~250 lines |
| `docs/P43-archive-compat-report.md` | Matrix of pre-P43 archives × restore paths · regression test results | ~100 lines |

**L2 update** (not new file): `docs/P43-api-contract-lock.yaml` — add `/api/document/extract` and `/api/workbench/freeze` entries.

### 2c · Test Whitelist (new · §3d Test Whitelist)

| File | Scope | Size |
|------|-------|------|
| `tests/test_p43_authority_contract_r1_r6.py` | 12 test functions per §3e R1-R6 + 2 observability tests | ~350 LOC |
| `tests/test_p43_workflow_automaton.py` | state-transition table validation · each transition tested; illegal transitions asserted to reject | ~250 LOC |
| `tests/test_p43_archive_backward_compat.py` | fixture = pre-P43 manifest · restore via workbench_bundle.py · no migration script | ~100 LOC |
| `tests/test_p43_api_contract_lock.py` | parse YAML · for each endpoint, send representative request · assert response shape | ~200 LOC |
| `tests/test_p43_multi_tab_lock.py` | C13 cross-tab scenario · Q2-MT decision (see §Q) | ~100 LOC (default lane · or opt-in e2e per Q2-MT) |
| `tests/test_p43_document_pipeline.py` | PDF/DOCX/text fixtures · extract · assert text + markers + error branches | ~150 LOC |
| `tests/test_p43_clarification_stable_ids.py` | Bug D regression · session-path + legacy tolerance | ~80 LOC |
| `tests/test_p43_freeze_gate.py` | FREEZE state transition + Appendix A template validation | ~150 LOC |
| `tests/fixtures/p43_validator_cases.json` | §3e R5 fixture-only validator I/O · 4 conflict types | ~50 lines JSON |
| `tests/fixtures/p43_document_pipeline/*.pdf/.docx/.md/.txt` | test corpus for P43-03 pipeline | ~5 files |
| `tests/fixtures/p43_pre_archive/*` | pre-P43 archive fixture · for backward-compat | ~3 files |

### 2d · Tooling + CI Whitelist (new · §3d Tooling+CI Whitelist)

| File | Scope | Size |
|------|-------|------|
| `tools/check_authority_contract.py` | static-scan executor · R1-R6 source-grep rules | ~200 LOC |
| `.github/workflows/p43-authority-contract.yml` | CI job · runs R1-R6 tests + static scan on PR | ~40 lines |
| `.pre-commit-config.yaml` | pre-commit hook invoking `check_authority_contract.py` · L1-extend if file exists, else new | ~20 lines |
| `src/well_harness/static/workbench.debug.js` | `logDraftWrite` observability (Q5=B non-blocking fold-in ok) | ~60 LOC |
| `src/well_harness/tools/appendix_a_generator.py` **(P43-00 §3d whitelist delta needed · see §Q Q1)** | frozen-spec + clarification + ambiguity → Appendix A markdown | ~150 LOC |

## 3. Execution plan (per sub-phase · each Step = independent commit · Q7=A Codex per-touchpoint)

### 3a · P43-02 Orchestrator sub-phase

| Step | Scope | Codex required |
|------|-------|----------------|
| A | Workflow automaton contract docs (`P43-workflow-automaton-contract.md` + `.yaml`) · no source changes | No (doc only) |
| B | Authority contract R1-R6 tests scaffold (tests written; `tools/check_authority_contract.py` minimally implemented so tests can find fixtures; fails until rules implemented in Step C) | Yes (adapter-boundary: tools/) |
| C | `workbench.js` authority-contract additions (`assignFrozenSpec`, `deepFreeze`, `validateDraftAgainstFrozen`, convention markers, `draftDesignState` writes, `frozenSpecSHA`) · `workbench.debug.js` · `workbench.html`/`css` additions · Step B tests now PASS | **Yes** (workbench.js adapter-boundary) |
| D | `cli.py workbench new|continue|status` subcommands + workbench_bundle.py manifest optional fields | Yes (cli.py adapter-boundary) |
| E | Archive backward-compat test + API contract-lock regression test | No (test only) |
| F | Multi-tab lock test (Q2-MT: default lane string-grep OR opt-in e2e) | Only if opt-in e2e (headless browser) |
| G | CI workflow + pre-commit hook + `tools/check_authority_contract.py` finalized | Yes (CI boundary) |
| H | `docs/P43-authority-contract-verification.md` + `docs/P43-archive-compat-report.md` + `demo_server.py:2666` polish per Q5 | Only if Q5=harden-to-strict-bool (source change) |

### 3b · P43-03 Document pipeline sub-phase

| Step | Scope | Codex required |
|------|-------|----------------|
| A | `extract_document_text` in `ai_doc_analyzer.py` + pypdf/python-docx dependency declaration | Yes (ai_doc_analyzer adapter-boundary) |
| B | `POST /api/document/extract` in `demo_server.py` | Yes (demo_server adapter-boundary) |
| C | `tests/test_p43_document_pipeline.py` + fixtures | No (test only) |
| D | `ai-doc-analyzer.js` wiring: replace `readAsText` with `fetch /api/document/extract` + fallback | Yes (frontend + api client · multi-file) |
| E | Bug D fix in `_inject_clarification_answers` + `test_p43_clarification_stable_ids.py` | Yes (Counter F · ai_doc_analyzer adapter-boundary) |
| F | `docs/P43-api-contract-lock.yaml` L2 update: add `/api/document/extract` endpoint | No (doc update only) |

### 3c · P43-04 Freeze gate sub-phase

| Step | Scope | Codex required |
|------|-------|----------------|
| A | FREEZE event in workflow automaton + transition rules | No (contract doc update) |
| B | `workbench.js` FREEZE event handler + `frozenSpecSHA` storage | **Yes** (workbench.js adapter-boundary) |
| C | `src/well_harness/tools/appendix_a_generator.py` (new · requires §3d whitelist delta) | Yes (new tool · requires approval) |
| D | `cli.py workbench freeze` subcommand | Yes (cli adapter-boundary) |
| E | `tests/test_p43_freeze_gate.py` + FREEZE fixtures | No (test only) |
| F | Combined Batch-2 three-lane regression (re-run default + e2e + adversarial wrapper) | No (verification only) |
| G | Batch closure report (`docs/P43-02-batch-closure-report.md` · parallel to P43-01 contract-proof-report · new file — **§3d Doc Deliverables Whitelist delta needed · see §Q Q1**) | No (doc only; but whitelist delta needs approval) |

## 4. Exit Criteria (per-sub-phase asserted_pass · combined Batch-2 gate)

### Per-sub-phase

**P43-02 (Orchestrator)**:
1. All 12 R1-R6 authority-contract tests + 2 observability tests PASS default lane.
2. `tools/check_authority_contract.py` runs clean on HEAD.
3. CI workflow + pre-commit hook both green.
4. Archive backward-compat test PASS: pre-P43 fixture restores without migration script.
5. API contract-lock regression test PASS: all 7+1 endpoints (P43-01's 7 + `/api/document/extract` added in P43-03) match YAML.
6. Multi-tab lock test PASS per Q2-MT lane.
7. Workflow automaton contract docs present and internally consistent.
8. `demo_server.py:2666` polish per Q5 decision landed.

**P43-03 (Document pipeline)**:
9. `POST /api/document/extract` returns extracted text for PDF + DOCX + TXT + MD fixtures.
10. Corrupted-PDF, encrypted-PDF, unsupported-type error branches each return structured 400.
11. `ai-doc-analyzer.js` frontend uses server-side extract for PDF/DOCX; `%PDF-1.7` garbage no longer reaches `_documentText` (Playwright regression proves this; `tests/test_p43_readAsText_browser_behavior.py` gets updated to assert post-fix clean-text shape instead of broken-shape).
12. Bug D regression test PASS: session-path clarification answers consumed via stable question_ids; `ready_for_spec_build=True` achievable from session flow.

**P43-04 (Freeze gate)**:
13. FREEZE state transition test PASS for all legal/illegal pre-states.
14. `workbench freeze` CLI produces valid Appendix A markdown matching P34-P42 template structure.
15. Appendix A generator correctly emits one registry row per open question.
16. `frozenSpecSHA` stored in localStorage; retrievable across browser reload.

### Combined Batch regression

17. **Three-lane regression vs post-P43-01 main baseline** (commit `61b12b3`):
    - Default pytest: **≥ 800 + N new default tests passed** (P43-02/03/04 adds authority R1-R6 + workflow + archive-compat + api-contract-lock + doc-pipeline + clarification-stable-ids + freeze-gate = ~8-10 new default test files · estimate 30-50 new test cases). Exact expected count to be locked at Step C of each sub-phase execution.
    - E2E pytest: **≥ 50 passed** (P43-01 baseline + any P43-02 opt-in e2e from Q2-MT).
    - Zero regression on pre-existing 800 tests.

18. **Codex Q7=A coverage**: every adapter-boundary commit has a Codex review trailer; no `可过-Gate` from any round missing.

## 5. Tier 1 self-audit (C1-C?)

| Counter | Hypothesis | Self-verification |
|---------|-----------|-------------------|
| C1 · phantom source file | Did I reference any file that doesn't exist at HEAD `61b12b3`? | Plan cites `P43-00-PLAN.md` (exists), `P43-01-00-PLAN.md` (exists), `docs/P43-api-contract-lock.yaml` (exists after P43-01), `src/well_harness/ai_doc_analyzer.py:799` (exists). No phantom. |
| C2 · whitelist violation | Am I proposing to create a file outside §3d whitelist? | `tools/appendix_a_generator.py` IS NOT in P43-00 §3d Tooling+CI whitelist (that whitelist names only `check_authority_contract.py`, CI workflow, pre-commit, `workbench.debug.js`). **Whitelist delta required** · see §Q Q1. Also `docs/P43-02-batch-closure-report.md` IS NOT in §3d Doc Deliverables whitelist · **whitelist delta required**. |
| C3 · Bug D fix scope | Am I treating Bug D as rename-only vs contract? | §1b and §4 #12 explicitly require regression test verifying runtime stable-ID contract. OK. |
| C4 · Appendix A template | Am I cloning a frozen template or dynamically generating? | §1c explicitly says dynamic generation from intake + clarification + ambiguity. OK. |
| C5 · Codex fatigue | Per-touchpoint Codex with ~15 adapter-boundary commits. Sustainable? | §3 tables mark "Codex required" per Step. Estimate 8-10 Codex calls across Batch-2 (not every commit). Q7=A requires Codex per-touchpoint but allows batching reviews at sub-phase close. |
| C6 · multi-tab lock lane | Default lane vs opt-in e2e? | Open as Q2-MT · see §Q. |
| C7 · non-blocking polish fold-in | `demo_server.py:2666` polish lands as Step H in 1a? | Yes · controlled by Q5. |
| C8 · §3d whitelist self-audit | Am I honoring §3d blacklist (no `state.yaml`, no `workflow_engine.py`, no SPA shell)? | None of above proposed. Blacklist honored. |
| C9 · test count inflation | 8-10 new test files · is this over-engineering? | Each test file maps to a specific §3e rule (R1-R6) or §3d compat requirement. No duplication. |
| C10 · P43-03 Bug D Codex boundary | Bug D fix touches ai_doc_analyzer.py line 799 — previously fixed Bugs A/B1/B2 at lines 840/843/866/867. Does R6 handler-call-closure test still pass after Bug D touch? | Must run existing `tests/test_p43_doc_analyzer_blocker_fix.py` as part of Batch regression. Noted in §4 #17. |
| C11 · Appendix A signature path | Is Appendix A consumed by downstream P43-08 Final Approval? | Yes · per P43-00 §1 Step 2c frozen spec + Appendix A · Step 8 Final Approval. P43-02 Batch only generates; P43-08 (Batch-4) consumes. |

## 6. Gate strategy

**Single gate**: GATE-P43-02-BATCH-CLOSURE covering all three sub-phases.

Submission trigger: all 17 Exit Criteria met + Q7=A Codex coverage complete + three-lane regression green.

Per Q1=D batch pattern (P43-01 independent · P43-02..04 combined · P43-05..07 combined · P43-08..10 combined).

## 7. Stop points

1. Any R1-R6 test fails after §3e full implementation (Step C of 3a) → stop · escalate Kogami.
2. Archive backward-compat regression fails → stop · Kogami arbitration on L3 blocking change.
3. Bug D fix breaks existing `test_p43_doc_analyzer_blocker_fix.py` regression → stop · Codex re-review before proceeding.
4. Codex Q7=A `需阻止` verdict on any adapter-boundary commit → stop · path ① rewrite before proceeding.
5. Three-lane regression shows ANY pre-existing test regression → stop · root-cause before Batch closure.
6. §Q open questions (Q1 whitelist delta, Q2-MT multi-tab lane, Q5 demo_server:2666 polish) unresolved after Codex r1 → escalate Kogami.

## 8. Governance (Codex per Q7=A)

Codex review trailer required at:
- Step 3a/C (workbench.js adapter-boundary · primary authority contract write)
- Step 3a/D (cli.py + workbench_bundle adapter-boundary)
- Step 3a/G (CI + static scan)
- Step 3b/A (ai_doc_analyzer extract function · new writer)
- Step 3b/B (demo_server new endpoint · new writer)
- Step 3b/D (ai-doc-analyzer.js frontend + api client · multi-file)
- Step 3b/E (Bug D fix · Counter F surface)
- Step 3c/B (workbench.js FREEZE handler)
- Step 3c/C (appendix_a_generator new file · new tool)
- Step 3c/D (cli freeze subcommand)

Total: ~10 Codex adapter-boundary review rounds across Batch-2. Plus Step G per-sub-phase post-implementation closure review (3 sub-phases × 1 round each = 3 closure reviews). Total ~13 Codex rounds for the Batch.

## §Q · Open questions (for Codex r1 + Kogami)

### Q1 · §3d whitelist delta approval

This plan needs **two whitelist deltas** to P43-00 §3d:

1. **New file**: `src/well_harness/tools/appendix_a_generator.py` (~150 LOC) — not named in P43-00 §3d Tooling+CI Whitelist. Needed for P43-04 Appendix A dynamic generation.
2. **New file**: `docs/P43-02-batch-closure-report.md` (~400 lines) — not named in P43-00 §3d Doc Deliverables Whitelist. Needed as P43-02-Batch counterpart to P43-01's contract-proof-report.

- **A** · Request Kogami to amend P43-00 §3d whitelist (formal plan delta · adds ~5 lines to P43-00) → then proceed in P43-02 Batch.
- **B** · Absorb both under existing whitelists: `appendix_a_generator.py` → re-home under `src/well_harness/` (not `/tools/`, which is already less restricted per §3d) · `P43-02-batch-closure-report.md` → write into `.planning/phases/P43-control-logic-workbench/reports/p43-02-batch-closure/` (supporting-artifact pattern · same as P43-01's detailed report).
- **C** · Drop Appendix A generator entirely; hand-author template per-system instead (defers automation to post-P43).
- **Executor 建议**: **B** — keeps P43-00 untouched (no plan churn), reuses the P43-01 governance inversion precedent (supporting artifact vs plan-whitelisted summary), and keeps generator as a regular Python module.

### Q2-MT · Multi-tab lock test lane

Cross-tab scenario C13 detection. Options:

- **A** · Default lane: string-grep `workbench.js` for `BroadcastChannel` / `storage` event handler + assert presence of appropriate handlers. Fast but shallow.
- **B** · Opt-in e2e: Playwright two-page test · open system_id in tab 1 · open same in tab 2 · assert second tab shows "another tab active" lock indicator. Real but slow.
- **C** · Both: default lane grep for structural presence + opt-in e2e for behavior. Consistent with P43-01 S3b/S4 pattern.
- **Executor 建议**: **C** — parallels P43-01 structure (grep + opt-in Playwright).

### Q5 · `demo_server.py:2666` polish

Codex Step G r4 flagged non-blocking message drift: error message says "apply_all_safe must be true" but runtime guard is truthiness-based.

- **A** · Soften message: "apply_all_safe must be truthy" (one-line change · preserves behavior · aligned with YAML).
- **B** · Harden handler to strict bool: `apply_all_safe is True` (behavior change · breaks any caller currently passing truthy non-bool values · L2 update needed on YAML too).
- **C** · Leave as-is; document the wording gap in Batch closure report.
- **Executor 建议**: **A** — softens language without behavior change; one-line message update + YAML already says truthy semantics.

### Q8-batch · Batch size sanity

Batch-2 = ~2200-2800 LOC with 3 sub-phases. Compare to P43-01 (~200 LOC source + ~500 lines tests/fixtures + ~600 lines docs = ~1300 LOC total).

- **A** · Proceed with combined plan; single Kogami gate as Q1=D prescribes.
- **B** · Split Batch-2 into two mini-plans (P43-02 orchestrator + P43-03/04 combined) · would violate Q1=D · would need Kogami override.
- **C** · Stage execution: plan covers all three sub-phases (this v1 spec) but execution commits per-sub-phase with mini-gates between (P43-02 closure before starting P43-03, etc.) · single final GATE-P43-02-BATCH-CLOSURE.
- **Executor 建议**: **C** — honors Q1=D (one gate) while giving Kogami per-sub-phase visibility via commit arc. Same pattern P43-01 used for Steps A-G.

## §9 · Signature

**Signed**: Claude App Opus 4.7 (Solo Executor · v5.2 solo-signed + v5.3 addendum) · 2026-04-21

**Revision**: v1 draft · pre Codex r1

**Next action**: Submit for Codex r1 adversarial review per Q7=A milestone-wide rule.
