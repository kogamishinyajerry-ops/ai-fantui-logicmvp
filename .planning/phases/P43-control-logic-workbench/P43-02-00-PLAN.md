---
phase: P43
batch: Batch-2 (P43-02 + P43-03 + P43-04 · combined per Q1=D)
plan: P43-02-00
plan_revision: v3.1 (janitorial on v3 · Codex r3 3 required + 1 polish (all weak-signal text consistency) addressed · no structural expansion)
title: P43-02 Batch — Orchestrator + Document Pipeline + Freeze Gate (combined plan · v3.1)
status: DRAFT · awaiting Codex r4 adversarial review (expected 可过-Gate after weak-signal closure)
date: 2026-04-21
owner: Claude App Opus 4.7 (Solo Executor · v5.2 solo-signed + v5.3 addendum)
scope_tier: Tier 1 · adapter-boundary write (ai_doc_analyzer.py + workbench.js authority-contract · workbench_bundle.py manifest additive)
verified-by: pending Codex r3 post-plan adversarial review (Q7=A · per-touchpoint)
prior_review:
  r1:
    verdict: 需修正·信号强 (6 required + 2 polish)
    fixes_applied: [whitelist_story_Q1A, appendix_a_scope_narrow, bug_d_stable_id, dual_sha_manifest, internal_consistency, q5b_removal]
    polish_applied: [readAsText_test_rewrite, pypdf_packaging_q6]
  r2:
    verdict: 需修正·信号强 (3 required + 1 polish)
    fixes_applied: [freeze_artifact_aligned_to_parent, pyproject_explicit_delta_entry, bug_d_semantic_category_binding]
    polish_applied: [regression_count_alignment]
  r3:
    verdict: 需修正·信号弱 (3 required janitorial + 1 polish · all text consistency · no structural concern)
    fixes_applied: [§2b_heading_updated_to_reflect_q1_delta_entry_8, pyproject_preemptive_language_removed, bug_d_cases_6_propagated_to_all_references]
    polish_applied: [§6_v2→v3_wording_scrub]
preconditions:
  - `GATE-P43-PLAN (v7): Approved` (Kogami 2026-04-20 · main `99211bd`)
  - `GATE-P43-01-CLOSURE: Approved` (Kogami 2026-04-21 · main `61b12b3`)
  - P43-00-PLAN.md v7 + P43-01-00-PLAN.md v5 both frozen on main (neither touched by this plan)
  - P43-01 delivered: `docs/P43-api-contract-lock.yaml` (7 endpoints) + `docs/P43-contract-proof-report.md` + Bugs A/B1/B2 fixed in `src/well_harness/ai_doc_analyzer.py`
  - Bug D still deferred at `ai_doc_analyzer.py:785` (fix lands in this Batch · P43-03 sub-phase · see §1b and r1-finding-#3 closure)
  - Non-blocking polish residual at `src/well_harness/demo_server.py:2666` (Q5-A soften)
  - Q lock from P43-00: Q1=D · Q2=A · Q4=A · Q7=A · Q8=B · Q10=B · Q12=B+a
non-goals:
  - 不执行 P43-05..P43-10（本 Batch = 02+03+04 only）
  - 不改 P43-00 v7 plan · 不改 P43-01 v5 plan
  - 不新建 `workbench/orchestrator.py` / `workflow_engine.py` / `draft_truth.py` / `state.yaml` / `workflow.db` · 任何新持久化源（P43-00 §3d blacklist 硬规则）
  - 不新建前端 SPA shell / 替换 `workbench.js` 框架（vanilla JS 继承 · Q2=A · §3d blacklist）
  - 不 bump 既有 JSON schema `$id`（P42 已锁 · §3d blacklist）
  - **不新建 `src/well_harness/tools/appendix_a_generator.py`**（v2 · Codex r1 finding #2 closure · 原 v1 该文件超范围 · 改由 `cli.py workbench freeze` 内联 ~30 LOC 格式化函数产出 open-questions registry SKELETON 而已）
  - 不实装 R3 `test_r3_runtime_mutation_blocked` 作为默认 lane 硬性（opt-in e2e only · 源级 deepFreeze 存在即 mechanical closure · P43-00 §3e v5 已定）
  - 不实装 R5 `test_r5_validator_node_parity_mandatory` 作为 P43-02 Exit Criteria（归 P43-09 Final Approval MANDATORY · P43-00 §3e v7 strengthen directive）
  - 不新增 `generate_adapter.py` domain hardcode（§3d L3 禁）
  - 不以"列出断裂"为通过条件（P43-00 non-goal #16 · 每 R1-R6 test 必 asserted_pass · 每 P43-03 ID fix 必经 regression 锁）
  - 不跳 Codex review on any adapter-boundary commit（Q7=A · per-touchpoint）
  - 不把 Bug D fix 做成 "rename only"：必须证明 clarification_history → clarification_answers **stable-ID contract closure**（runtime · 非 source-level rename · 见 r1-#3 closure）
  - **不靠 ambiguity source-order 做 stable-ID 映射**（v3 · Codex r2 finding #3 closure · 源顺序 = 隐式契约 · Ambiguity 无语义 type 字段无法支撑）· 改 **L1 additive `Ambiguity.category` field 语义绑定** · missing/unknown → `clarify-{i}` + warning fallback
  - 不把 freeze-time 产物做成 "full traceability matrix"：v3 只产 **open-questions registry 骨架**（`docs/<system>/traceability_matrix.md` 的 open-questions section 初始填充 · 其他 section 延 P43-08 或 post-P43 扩写 · 与 P43-00 §2c line 190 P34-P42 precedent 对齐 · v2 "open_questions_<system>.md" 分叉已撤回）· 完整矩阵扩写延 P43-08 Final Approval 或 post-P43（Codex r1 #2 + r2 #1 closure）
  - 不产出未经 §3d 形式授权的 whitelist 新增（v3 按 Q1=A 走 formal §3d delta 请求 · **8 entry** 打包一次 · v2 的 6 + pyproject.toml + `docs/<system>/traceability_matrix.md` per-system emission · 见 §Q Q1）
  - 不 harden `demo_server.py:2666` 为 strict bool（L3 语义变更 · 违 §3d · v1 Q5-B 删 · r1 finding #6 closure）
---

# P43-02 Batch · Orchestrator + Document Pipeline + Freeze Gate — v3 (surgical addendum on v2)

## 0. TL;DR · v3 surgical addendum (on top of v2)

Codex r2 verdict on v2: **需修正·信号强**（3 required + 1 polish）。v3 是 **外科 addendum**，不动 v2 已闭合的 6 r1 findings，只再封闭 r2 指出的 3 必修 + 1 polish · 不动源码 · 只改 plan 文件。

| r2 finding | v2 遗漏 | v3 修法 | 本文档定位 |
|---|---|---|---|
| #1 Whitelist 未真闭合 | v2 把 freeze 产物改为 `docs/<system>/open_questions_<system>.md` · 但 parent `P43-00:190` anchor 的是 `docs/{system}/traceability_matrix.md`（P34-P42 precedent · `docs/c919_etras/`, `docs/thrust_reverser/` 已存）· 且 v2 还引了不存在的 "Q1 entry #7" dangling ref | **回归 parent anchor**：freeze-time emit `docs/<system>/traceability_matrix.md` 的 **open-questions section 初始填充** 而已（其他 section 空占位延 P43-08 扩写）· 在 §Q Q1 delta 新增 **entry #8** per-system traceability_matrix.md 授权 · 删除所有 `open_questions_<system>.md` 残留 | §1c + §2a + §2b + §4 #15 + §Q Q1 |
| #2 pyproject.toml 仍超 §3d | v2 判 "L1 additive · 如 Codex r2 争议则 pre-emptive 列入" · Codex r2 争议了 | 正式列 **§Q Q1 delta entry #7** · 向 Kogami 申请 · 不再 pre-emptive 措辞 | §Q Q1 |
| #3 Bug D 仍位置耦合 | v2 "source-order 映射 · 溢出 fallback clarify-{i}" = 把脆性前移 · `Ambiguity` 无 category 字段支撑位置绑定 | **L1 additive 加 `Ambiguity.category: str | None = None` field**（backward-compat: default None · 既有 fixture/test 零破）· LLM prompt 扩 emit category ∈ `{source_documents / component_state_domains / timeline_rules / fault_taxonomy}`（四 canonical IDs）· `_build_questions_from_ambiguities` 按 ambiguity.category 语义绑定至 stable ID · missing/None/unknown → `clarify-{i}` + log.warning fallback · 非位置映射 · regression 新增 test 场景 (i') category → stable ID 语义绑定 · (iv) missing category → clarify-{i} graceful · (v) unknown category → warn+fallback | §1b + §2a + §3b Step E + §4 #13 + §5 C3 |
| #4 polish 计数 drift | v2 §4 #18 "~40 new" vs §5 C9 "~46 new" 口径不一 | 统一到 **~46 new**（16 authority + ~30 other）· 全文统一措辞 | §4 #18 + §5 C9 |

v2 TL;DR 的 r1 fix matrix 保留（见下 · 结构不动）。

## 0a. TL;DR · v2 → v1 fix matrix (保留 · v3 不改)

Codex r1 verdict on v1: **需修正·信号强**（6 required + 2 polish）。v2 是 **外科式重写**，不扩结构，只封闭 r1 指出的 6 必修 + 2 polish · 不动源码 · 只改 plan 文件。

| r1 finding | v1 问题 | v2 修法 | 本文档定位 |
|---|---|---|---|
| #1 Whitelist 故事 | v1 Q1 推 "方案 B 混用 .planning/ 迂回" · 治理不清 | **Q1=A** · formal §3d delta 请求 · 6 entry 打包一次 · drop `appendix_a_generator.py` | §Q Q1 + §2d + §8 |
| #2 Appendix A 超范围 | v1 要产 "动态 Appendix A" · 工作量/语义远超 freeze-gate 责任 | freeze-time **只产 open-questions registry 骨架**（`open_questions_<system>.md` · 三列空占位）· 由 `cli.py workbench freeze` 内联 ~30 LOC formatter · 完整 traceability matrix 延后 P43-08 或 post-P43 | §1c + §2a |
| #3 Bug D 靠 stable-ID | v1 只说 "rewrite `_inject_clarification_answers`"，没给 Question.id 产生源 | `_build_questions_from_ambiguities`（`ai_doc_analyzer.py:322`）在构造 `Question` 时直接取 `default_workbench_clarification_questions()` 的 stable ID（`source_documents`/`component_state_domains`/`timeline_rules`/`fault_taxonomy`）· `clarification_history` L2 扩为 `list[tuple[question_id, question_text, answer]]` · 旧 2-tuple graceful tolerate · `document_intake.validate_clarification_answer_ids` 对未知 ID warn+skip | §1b + §2a |
| #4 Dual-SHA | v1 只提 "Q12=B+a canonical/binary SHA" · 没落 test 和 manifest 契约 | 新 test `tests/test_p43_dual_sha_manifest.py` + `workbench_bundle.py` manifest L1 additive 加 **两个 optional null-tolerant 字段** `canonical_text_sha256` + `source_binary_sha256`（P42 既有 archive 不含 · restore 时 null 容忍）| §2a + §2c |
| #5 内部计数统一 | v1 到处 "12 / 14 / 16" 混用 · "7+1 / 8" 混用 · /api/workbench/freeze 既说要又不产 | R1-R6 = **14 默认 lane tests**（R1=3 · R2=1 · R3=4 去 runtime_mutation opt-in · R4=2 · R5=2 去 node_parity 归 P43-09 · R6=2） **+ observability=2** = **16 tests**。Codex rounds = **13 concrete**（10 adapter-boundary + 3 closure）。**`/api/workbench/freeze` DROP · CLI-only** · 总 endpoints = **8** (P43-01 的 7 + P43-03 的 `/api/document/extract`) · 全文写死 | §3e + §4 + §8 |
| #6 Q5-B 删 | v1 Q5-B "harden to strict bool" = L3 语义变更 · 违 §3d | 删 Q5-B · 只留 Q5-A (soften) + Q5-C (leave + closure report) · 推荐 A | §Q Q5 |
| #7 polish readAsText test | v1 §4 #11 只说 "regression proves this"，没说怎么改测试 | P43-03 Step D 明示 **rewrite** `tests/test_p43_readAsText_browser_behavior.py` · assert 已修路径（preview 不以 `%PDF-` 开头 · Content-Type guard 生效）· broken-shape 证据留 `docs/P43-contract-proof-report.md §2 S4` · 不动 | §3b/D + §4 #11 |
| #8 polish pypdf/python-docx 打包 | v1 没说依赖如何声明 | §Q 新增 Q6 · pyproject.toml `[project.optional-dependencies] document = ["pypdf>=4.0", "python-docx>=1.0"]` + `ai_doc_analyzer.py` 函数本地 lazy import（推荐 A） | §Q Q6 |

**Estimated size**: ~2100-2700 LOC (source + tests + docs · 比 v1 略降 · Appendix A 生成器剔除)。~3-4 days wall-time · 13 Codex 审查点。

## 1. Scope per sub-phase · v2

### 1a · P43-02 · Orchestrator + authority contract

**Authority anchors**: P43-00 §3c (workflow automaton) · §3d (archive/API compat) · §3e R1-R6 mechanical。

Must-land deliverables（v1 继承 · 未被 r1 指正的部分保留）:

- **Workflow automaton contract** (§3c): state enum + event list + transition table + error taxonomy + idempotency rules + persistence model (two-source: localStorage + manifest) + side-effect ordering + cross-phase integration test ownership declarations.
- **Authority contract R1-R6 mechanical verification** (§3e): **14 默认 lane tests + 2 observability = 16 tests total**（精确列表 见 §3e）。
  > **Codex r1 finding #5 closure**: v1 "12 tests"/"14 tests" 混用 · v2 全文一律写 **14 R1-R6 默认 + 2 observability = 16**。
- **`cli.py` extension**: add `workbench new | continue | status | freeze` 4 subcommands (L1 additive · §3d · **`freeze` 内含 ~30 LOC open-questions registry formatter** · r1 #2 closure).
- **`workbench.js` extension** (L1 additive · §3d): `draftDesignState` key · `assignFrozenSpec(newSpec, {origin})` 受控访问器 · `deepFreeze()` helper · `validateDraftAgainstFrozen(draft, frozen)` validator · convention markers `// P43: final-approval-handler-begin/end` / `// P43: validate-draft-begin/end` · `frozenSpecSHA` storage.
- **`workbench.debug.js`** (new · Tooling Whitelist): `logDraftWrite(writer_step, timestamp, diff_summary)`.
- **`workbench.html`** extension: `#draft-frozen-diff-badge` DOM + Final Approval handler section (L1 additive).
- **`tools/check_authority_contract.py`** (new · Tooling Whitelist): static-scan executor; invoked by R1-R6 tests + pre-commit + CI workflow.
- **`.github/workflows/p43-authority-contract.yml`** + **`.pre-commit-config.yaml`** (both · Tooling Whitelist).
- **Archive backward-compat test** (`tests/test_p43_archive_backward_compat.py`): pre-P43 fixture → restore → assert no migration needed.
- **API contract-lock regression test** (`tests/test_p43_api_contract_lock.py`): parse `docs/P43-api-contract-lock.yaml` · for each of **8 endpoints** (P43-01 的 7 + P43-03 的 `/api/document/extract`) · send request · assert response shape.
  > **Codex r1 finding #5 closure**: v1 "7+1" 写法含糊 · v2 明文 **8 endpoints · `/api/workbench/freeze` dropped** (freeze 走 CLI only)。
- **Multi-tab lock test** (`tests/test_p43_multi_tab_lock.py`): C13 cross-tab 场景 · Q2-MT lane 决定（见 §Q）.
- **Dual-SHA manifest test** (`tests/test_p43_dual_sha_manifest.py` · **NEW v2 · r1 #4 closure**): 给 workbench_bundle 写 `canonical_text_sha256` + `source_binary_sha256` 两字段 · restore 在两字段都缺失 / 仅缺 canonical / 仅缺 binary / 都有 四种组合下 PASS · 证 null-tolerant 向后兼容。
- **Docs** (new · Doc Deliverables Whitelist · 已在 P43-00 §3d 授权 · v2 不增): `P43-workflow-automaton-contract.md` + `.yaml` + `P43-authority-contract-verification.md` + `P43-archive-compat-report.md`.
- **`demo_server.py:2666` polish** per Q5-A: soften message "apply_all_safe must be true" → "apply_all_safe must be truthy"（一行文案 · 无行为变更 · YAML 已 truthy · Q5-B 删 · r1 #6 closure）.

### 1b · P43-03 · Document pipeline + Bug D fix

**Authority anchors**: P43-00 Q12=B+a · §3d L2 allowing `ai_doc_analyzer.py` 加 PDF/DOCX reader.

Must-land deliverables:

- **Server-side document extraction**: `ai_doc_analyzer.py` L1 加 `extract_document_text(file_bytes, content_type)` · 用 `pypdf` (PDF) / `python-docx` (DOCX) / 纯 text decode (text/plain, text/markdown)。**Lazy local import 在函数内**（Codex r1 polish #8 · 见 §Q Q6）.
- **New endpoint** (L1 additive · `demo_server.py`): `POST /api/document/extract` · 接受 binary upload · 返 `{document_text, extraction_method, warnings}` 或 `{error, message}`.
- **Frontend wiring** (L1 additive · `static/ai-doc-analyzer.js`): 替换 line 224 `reader.readAsText(file)` 为 `fetch('/api/document/extract', ...)` · 保留 `.txt`/`.md` browser-side fallback (content-type guard).
- **Bug D fix (semantic category binding · r1 #3 + r2 #3 closure · v3 强化)**:
  - **`Ambiguity` dataclass L1 additive 加 `category: str | None = None` field**（`ai_doc_analyzer.py:121-127` dataclass 扩 · `from_dict` 用 `data.get("category")` 默认 None · L2 backward-compat：既有 fixture/mock 零改）· LLM prompt（MOCK + 真实模式均 touched）扩 instruct emit `category` ∈ `{"source_documents", "component_state_domains", "timeline_rules", "fault_taxonomy"}`（四 canonical IDs · 映射 `system_spec.py:244-270` `default_workbench_clarification_questions()`）
  - `ai_doc_analyzer.py:322` `_build_questions_from_ambiguities` — 按 `ambiguity.category` 语义绑定 `Question.id` 到对应 stable ID · **category=None / unknown → `clarify-{i}` + `log.warning("Ambiguity %s missing/unknown category; falling back to positional clarify-%d", amb.id, i)` fallback**（不 fail · graceful · regression test 守）· **非位置映射**（v2 source-order overflow 策略已撤回）
  - `AmbiguityResolutionSession.clarification_history` L2 backward-compat 扩为 `list[tuple[question_id, question_text, answer]]`（3-tuple · question_id 可能是 canonical ID 或 `clarify-{i}` · 取决于 category 是否命中）· 旧 2-tuple graceful tolerate（`len(tpl)` 分支）· `_inject_clarification_answers` 用 3-tuple 按 question_id 键；2-tuple fallback 原 `clarify-{i}` 路径
  - `document_intake.py:874-912` `assess_intake_packet` 的 `_unanswered_clarifications(packet.clarification_answers, required_questions)` gate 保留（仍按 canonical IDs 门禁）· L1 加 `validate_clarification_answer_ids(answers, known_ids)` helper · 未知 ID → `log.warning` + skip（**不 fail** · 非 L3 gate 变更 · 不放宽契约）
  > **Codex r2 finding #3 closure**: v2 source-order 映射 "把脆性前移" · `Ambiguity` 无语义 type 字段无法支撑位置绑定。v3 加 `category` L1 additive 字段做语义绑定 · missing/unknown graceful fallback · 非位置耦合。
- **Bug D regression tests** (`tests/test_p43_clarification_stable_ids.py`): (i) **category 命中 canonical → Question.id=stable ID** · answer injected · `ready_for_spec_build=True`（v3 新 · 语义绑定核心 case）; (ii) legacy 2-tuple clarification_history graceful tolerate（backward-compat · 不 crash）; (iii) unknown canonical ID in answer → validate_clarification_answer_ids warn+skip（证 L2 非 L3）; (iv) **category=None → clarify-{i} fallback + warning emitted**（v3 新 · 非位置耦合证据）; (v) **category 为 unknown str → clarify-{i} fallback + warning emitted**（v3 新）; (vi) **既有 fixture（无 category 字段）加载不破**（backward-compat · fixture 零改）.
- **Document pipeline tests** (`tests/test_p43_document_pipeline.py`): PDF/DOCX/TXT/MD fixtures → extract → assert content + markers + error branches (corrupted / encrypted / unsupported).
- **API contract-lock YAML update** (L2 on `docs/P43-api-contract-lock.yaml`): 加 `/api/document/extract` entry（第 8 个 endpoint）.
- **ReadAsText regression test rewrite** (Codex r1 polish #7 closure · §3b Step D): `tests/test_p43_readAsText_browser_behavior.py` · assert **已修路径**（preview 不以 `%PDF-` 开头 · Content-Type guard 生效 · server-side extract 走通）· broken-shape 证据留 `docs/P43-contract-proof-report.md §2 S4`（已存在 P43-01 归档 · 不动）.

### 1c · P43-04 · Freeze gate + open-questions registry SKELETON（r1 #2 closure）

**Authority anchors**: P43-00 §1 user-journey Step 2c · §3b · §3c FREEZE state.

Must-land deliverables:

- **FREEZE event implementation** (L1 additive): workflow automaton FREEZE event · 从 `AWAITING_ANSWERS`（或 `PARSING`）→ `FROZEN` · guard = "all clarifications answered OR explicitly-waived with operator alias".
- **Frozen spec hash binding** (L1 additive · `workbench.js`): FREEZE 时 SHA-256 spec JSON (sorted keys canonical) · localStorage key `wellHarnessWorkbench.v1.frozenSpecSHA.{system_id}`.
- **Open-questions registry SKELETON** (r1 #2 + r2 #1 closure · 不是完整 Appendix A):
  - 产物 = **`docs/<system>/traceability_matrix.md`**（per system · 与 P43-00 §2c line 190 P34-P42 precedent 完全对齐 · 既有 `docs/c919_etras/traceability_matrix.md` + `docs/thrust_reverser/traceability_matrix.md` 为 exemplar · v2 的 `open_questions_<system>.md` 分叉撤回 · r2 #1 closure）
  - 内容 = 模板分四节（同 P34-P42）· v3 只填 **open-questions registry section**（三列表 `| question_id | question_text | resolution_state |` · 每行一条 frozen spec 里 unanswered/waived question · resolution_state ∈ `{pending, waived-by-<alias>, answered}`）· **其他 section（source documents / components / logic nodes / acceptance scenarios）留空占位 + 明示 "扩写延 P43-08 Final Approval 或 post-P43" 行内 comment**
  - 产生路径 = **`cli.py workbench freeze` 内联 formatter ~30 LOC**（L1 cli.py extend · 不建新 Python 文件 · 不进 `tools/`）
  - **完整 matrix（其他 section 的实质内容扩写）明示延后 P43-08 Final Approval 或 post-P43**（当前 Batch 不产）
  > **Codex r2 finding #1 closure**: v2 自创 `open_questions_<system>.md` 分叉文件与 parent P43-00:190 anchor `docs/{system}/traceability_matrix.md` 不一致 · 且夹带不存在的 "Q1 entry #7" dangling ref。v3 回归 parent anchor · freeze-time 只填 open-questions section · 其他 section 空占位 · §Q Q1 delta entry #8 显式授权 per-system emission。
- **CLI subcommand** (L1 on `cli.py`): `workbench freeze <system_id> --alias <operator>` · 调用内联 formatter + stamps localStorage state.
- **FREEZE state transition test** + **registry skeleton test** (共 `tests/test_p43_freeze_gate.py`): 参数化 legal/illegal pre-states · post-state=FROZEN from legal only · fixture spec with 3 open questions → emit `docs/<system>/traceability_matrix.md` with 3 rows in open-questions section + SHA header + operator-alias footer + 其他 section 空占位标记（"deferred to P43-08"）.

### 1d · Scope discipline

- Must-land across Batch-2: (i) 16 authority tests (14 R1-R6 + 2 obs) PASS default lane; (ii) archive backward-compat PASS; (iii) API contract-lock regression PASS for 8 endpoints; (iv) multi-tab lock PASS per Q2-MT; (v) document pipeline PASS (PDF+DOCX+TXT+MD+error); (vi) Bug D regression PASS (**6 cases** · v3 · r2 #3 closure · 见 §4 #13 完整 roster); (vii) FREEZE transition PASS; (viii) registry SKELETON emission PASS; (ix) dual-SHA manifest PASS (4 组合).
- Report-only: none.
- 发现新 Counter-F-class bug → 登记 Additional Findings + Kogami R4 arbitration (path ① precedent).
- Any sub-phase Step blocked > 2 hours → stop · escalate Kogami.

## 2. Deliverables (per §3d whitelist · strict compliance · v2 delta pending Kogami formal approval)

### 2a · Source Code (L1/L2 per §3d ladder)

| File | Ladder | v2 scope |
|------|--------|----------|
| `src/well_harness/cli.py` | L1 | `workbench new | continue | status | freeze` · `freeze` 内联 ~30 LOC open-questions registry formatter |
| `src/well_harness/workbench_bundle.py` | L1 | manifest 加两个 optional null-tolerant 字段 `canonical_text_sha256` + `source_binary_sha256`（dual-SHA · r1 #4 · P42 兼容） |
| `src/well_harness/static/workbench.js` | L1 | `draftDesignState` key · `assignFrozenSpec` · `deepFreeze` · `validateDraftAgainstFrozen` · convention markers · `frozenSpecSHA` storage · FREEZE event handler |
| `src/well_harness/static/workbench.html` | L1 | `#draft-frozen-diff-badge` + Final Approval handler section |
| `src/well_harness/static/workbench.css` | L1 | classes for draft/frozen diff + Final Approval section |
| `src/well_harness/static/ai-doc-analyzer.js` | L1/L2 | L1 `readAsText` → `fetch /api/document/extract` · L2 `.txt`/`.md` content-type guard fallback |
| `src/well_harness/static/ai-doc-analyzer.html` | L1 | file-type indicator |
| `src/well_harness/static/ai-doc-analyzer.css` | L1 | additions only |
| `src/well_harness/demo_server.py` | L1/L2 | L1 `/api/document/extract` · L2 `:2666` 文案 soften (Q5-A 推荐 · Q5-B L3 删) |
| `src/well_harness/ai_doc_analyzer.py` | L1/L2 | L1 `extract_document_text` (lazy local imports · Q6-A) · **L1 `Ambiguity` dataclass 加 `category: str | None = None` 字段**（v3 · r2 #3 closure · 语义绑定）· L1 LLM prompt 扩 instruct emit `category` ∈ 4 canonical IDs（MOCK + 真实）· L2 `_build_questions_from_ambiguities` 按 category 语义绑定 stable ID · missing/unknown → `clarify-{i}` warning fallback · `clarification_history` 扩 3-tuple (L2 backward-compat 2-tuple graceful) · `_inject_clarification_answers` 分支处理 2/3-tuple |
| `src/well_harness/document_intake.py` | L1 | `validate_clarification_answer_ids` helper · 未知 warn+skip |
| `src/well_harness/tools/generate_adapter.py` | — | read-only per R4 · 不改 |
| **`pyproject.toml`** (v2 新 · Q6-A) | L1 extend | `[project.optional-dependencies] document = ["pypdf>=4.0", "python-docx>=1.0"]`（不影响 base install）|

> **Note on `pyproject.toml`** (v3 · r2 #2 closure): 属于仓库根 packaging metadata file · 非 `src/well_harness/` 白名单条目 · 且 P43-00 §3d Source Code Whitelist 未列。改动为 `[project.optional-dependencies] document = ["pypdf>=4.0", "python-docx>=1.0"]` 纯 additive（base install 不变 · 不改既有 required deps · 不改 build system · 不触 lockfile · 不 bump schema $id）· 但 v3 **不再用 "L1-additive-silent" 规避**，正式列入 §Q Q1 delta entry #7 向 Kogami 申请形式授权。

### 2b · Doc Deliverables (v3 · 4 项在 P43-00 §3d 授权 · 1 项 per-system emission 按 §Q Q1 delta entry #8 申请形式授权)

| File | Scope | Size | §3d 授权 |
|------|-------|------|---------|
| `docs/P43-workflow-automaton-contract.md` | state enum + events + transitions + error taxonomy | ~350 lines | YES (P43-00 §3d) |
| `docs/P43-workflow-automaton.yaml` | machine-readable · 用于 test_p43_workflow_automaton.py | ~200 lines | YES (P43-00 §3d) |
| `docs/P43-authority-contract-verification.md` | R1-R6 test 实装说明 + CI 集成 | ~250 lines | YES (P43-00 §3d) |
| `docs/P43-archive-compat-report.md` | pre-P43 archive × restore 矩阵 | ~100 lines | YES (P43-00 §3d) |
| L2 update: `docs/P43-api-contract-lock.yaml` | 加 `/api/document/extract`（第 8 endpoint） | +~20 lines | YES (L2 既有 · P43-01 已建) |
| L1 new (per-system emission): `docs/<system>/traceability_matrix.md` | freeze-time SKELETON · open-questions section 初始填充 · 其他 section 空占位延 P43-08 | ~30 lines each | **aligned with P43-00 §2c:190 anchor · §Q Q1 delta entry #8 形式授权**（v3 · r2 #1 closure · v2 的 `open_questions_<system>.md` 分叉撤回） |

> **Codex r1 finding #2 + r2 finding #1 closure**: v1 `docs/P43-02-batch-closure-report.md`（~400 lines）已剔除 · batch closure report 改走 `.planning/phases/P43-control-logic-workbench/reports/` 路径（P43-01 governance inversion precedent · 不占 `docs/` 白名单）。v2 自创 `open_questions_<system>.md` 分叉回归 parent anchor `docs/<system>/traceability_matrix.md`。

### 2c · Test Whitelist (v3 delta pending · Q1=A formal 请求 · 8 entry 打包一次 · 含 tests + fixtures + pyproject + traceability_matrix)

| File | Scope | Size | §3d 现状 |
|------|-------|------|---------|
| `tests/test_p43_authority_contract_r1_r6.py` | 14 R1-R6 + 2 observability = 16 tests | ~400 LOC | YES (P43-00 §3d) |
| `tests/test_p43_workflow_automaton.py` | state-transition table · illegal reject | ~250 LOC | YES (P43-00 §3d) |
| `tests/test_p43_archive_backward_compat.py` | pre-P43 fixture · restore · no migration | ~100 LOC | YES (P43-00 §3d) |
| `tests/test_p43_api_contract_lock.py` | parse YAML · 8 endpoints shape assert | ~200 LOC | YES (P43-00 §3d) |
| `tests/test_p43_multi_tab_lock.py` | C13 cross-tab · Q2-MT lane | ~100 LOC | YES (P43-00 §3d) |
| `tests/fixtures/p43_validator_cases.json` | R5 fixture I/O + required_substrings | ~50 lines JSON | YES (P43-00 §3d · v6 新增) |
| **`tests/test_p43_document_pipeline.py`** | PDF/DOCX/TXT/MD + error branches | ~150 LOC | **NO · §3d delta #1** |
| **`tests/test_p43_clarification_stable_ids.py`** | Bug D semantic category binding regression · **6 cases** (v3 · r2 #3 closure) | ~150 LOC | **NO · §3d delta #2** |
| **`tests/test_p43_freeze_gate.py`** | FREEZE transition + registry SKELETON | ~150 LOC | **NO · §3d delta #3** |
| **`tests/test_p43_dual_sha_manifest.py`** (r1 #4) | dual-SHA manifest × 4 组合 | ~100 LOC | **NO · §3d delta #4** |
| **`tests/fixtures/p43_document_pipeline/`** (dir) | PDF/DOCX/TXT/MD corpus | ~5 files | **NO · §3d delta #5** |
| **`tests/fixtures/p43_pre_archive/`** (dir) | pre-P43 archive fixture | ~3 files | **NO · §3d delta #6** |

### 2d · Tooling + CI Whitelist (v2 不增 · v1 `appendix_a_generator.py` 已剔除 · r1 #1+#2 closure)

| File | Scope | §3d 现状 |
|------|-------|---------|
| `tools/check_authority_contract.py` | static-scan executor | YES (P43-00 §3d · v5) |
| `.github/workflows/p43-authority-contract.yml` | CI job | YES (P43-00 §3d · v5) |
| `.pre-commit-config.yaml` | pre-commit hook (new or L1 extend) | YES (P43-00 §3d · v5) |
| `src/well_harness/static/workbench.debug.js` | observability logDraftWrite | YES (P43-00 §3d · v5) |
| ~~`src/well_harness/tools/appendix_a_generator.py`~~ | ~~150 LOC Appendix A generator~~ | **DROP v2 · Codex r1 finding #1+#2 closure · formatter 内联入 cli.py 替代** |

## 3. Execution plan (Codex Q7=A per adapter-boundary · 10 adapter + 3 closure = 13 rounds)

### 3a · P43-02 Orchestrator sub-phase

| Step | Scope | Codex required |
|------|-------|----------------|
| A | Workflow automaton contract docs (`P43-workflow-automaton-contract.md` + `.yaml`) | No (doc only) |
| B | R1-R6 authority-contract tests scaffold + `tools/check_authority_contract.py` 最小骨架 | **Yes** · round #1 |
| C | `workbench.js` authority-contract 落地 (assignFrozenSpec/deepFreeze/validateDraftAgainstFrozen/markers/draftDesignState/frozenSpecSHA) + `workbench.debug.js` + HTML/CSS · Step B tests 转 PASS | **Yes** · round #2 (workbench.js primary) |
| D | `cli.py workbench new|continue|status` + `workbench_bundle.py` manifest dual-SHA L1 additive 两字段 | **Yes** · round #3 (cli + bundle adapter) |
| E | Archive backward-compat test + API contract-lock regression test (8 endpoints) + **dual-SHA manifest test** (r1 #4) | No (test only) |
| F | Multi-tab lock test (Q2-MT · 默认 string-grep + opt-in e2e 双车道) | Only if opt-in e2e |
| G | CI workflow + pre-commit hook + `tools/check_authority_contract.py` finalize | **Yes** · round #4 (CI boundary) |
| H | `docs/P43-authority-contract-verification.md` + `docs/P43-archive-compat-report.md` + `demo_server.py:2666` Q5-A soften (文案 only · 无行为变更 · 不触 Codex) | No |
| **Closure** | P43-02 sub-phase closure review | **Yes** · round #11 (closure) |

### 3b · P43-03 Document pipeline sub-phase

| Step | Scope | Codex required |
|------|-------|----------------|
| A | `extract_document_text` in `ai_doc_analyzer.py` + `pyproject.toml` optional deps (Q6-A) + lazy local import | **Yes** · round #5 (ai_doc_analyzer adapter) |
| B | `POST /api/document/extract` in `demo_server.py` | **Yes** · round #6 (demo_server adapter) |
| C | `tests/test_p43_document_pipeline.py` + fixtures (§3d delta #1 + #5 approved pre-req) | No (test only) |
| D | `ai-doc-analyzer.js` wiring (readAsText → fetch) + **rewrite `test_p43_readAsText_browser_behavior.py` 断言已修路径**（preview 不以 `%PDF-` 开头 · Content-Type guard 生效 · r1 polish #7 closure · broken-shape 证据留 `docs/P43-contract-proof-report.md §2 S4` 不动） | **Yes** · round #7 (frontend + api client · multi-file) |
| E | Bug D fix (**Ambiguity.category L1 additive field + LLM prompt 扩** · `_build_questions_from_ambiguities` 按 category 语义绑定 · missing/unknown → clarify-{i} warning fallback · clarification_history 3-tuple L2 · `_inject_clarification_answers` 2/3-tuple 分支 · `document_intake.validate_clarification_answer_ids` warn+skip) + `test_p43_clarification_stable_ids.py` (§3d delta #2 approved pre-req · 6 test cases 覆盖语义绑定 + 所有 fallback) | **Yes** · round #8 (Counter F · multi-function · dataclass + prompt + helpers) |
| F | `docs/P43-api-contract-lock.yaml` L2 add `/api/document/extract`（第 8 endpoint） | No (doc only) |
| **Closure** | P43-03 sub-phase closure review | **Yes** · round #12 (closure) |

### 3c · P43-04 Freeze gate sub-phase

| Step | Scope | Codex required |
|------|-------|----------------|
| A | FREEZE event in workflow automaton contract + transition rules | No (doc update) |
| B | `workbench.js` FREEZE handler + `frozenSpecSHA` storage | **Yes** · round #9 (workbench.js adapter) |
| C | `cli.py workbench freeze --alias` + 内联 ~30 LOC registry SKELETON formatter | **Yes** · round #10 (cli adapter · **替代 v1 appendix_a_generator.py** · r1 #1+#2 closure) |
| D | `tests/test_p43_freeze_gate.py` + FREEZE fixtures + SKELETON assertion (§3d delta #3 approved pre-req) | No (test only) |
| E | Combined Batch-2 three-lane regression (default + e2e + adversarial wrapper) | No |
| F | Batch closure artifact `.planning/phases/P43-control-logic-workbench/reports/P43-02-batch-closure-report.md`（.planning/ 路径 · 不占 docs/ 白名单 · P43-01 governance inversion precedent） | No |
| **Closure** | P43-04 sub-phase closure review · combined batch closure sanity | **Yes** · round #13 (closure) |

## 4. Exit Criteria (per-sub-phase asserted_pass · combined Batch-2 gate)

### Per-sub-phase

**P43-02 (Orchestrator)**:
1. 所有 **16 tests** (14 R1-R6 + 2 observability) PASS 默认 lane.
2. `tools/check_authority_contract.py` runs clean on HEAD.
3. CI workflow + pre-commit hook both green.
4. Archive backward-compat test PASS.
5. API contract-lock regression test PASS: **8 endpoints**（P43-01 的 7 + `/api/document/extract`）match YAML.
6. Multi-tab lock test PASS per Q2-MT lane.
7. Workflow automaton contract docs present and internally consistent.
8. `demo_server.py:2666` soften (Q5-A) landed · 文案 only.
9. **Dual-SHA manifest test PASS 4 组合** (r1 #4): 都缺 / 仅缺 canonical / 仅缺 binary / 都有 · P42 archive null-tolerant。

**P43-03 (Document pipeline)**:
10. `POST /api/document/extract` returns extracted text for PDF + DOCX + TXT + MD fixtures.
11. Corrupted-PDF / encrypted-PDF / unsupported-type each return structured 400.
12. `ai-doc-analyzer.js` frontend 用 server-side extract for PDF/DOCX · `%PDF-1.7` garbage 不再达 `_documentText`. **`test_p43_readAsText_browser_behavior.py` rewritten · assert 已修路径**（preview 不以 `%PDF-` 开头 · Content-Type guard 生效 · r1 polish #7 closure · broken-shape 证据保留 `docs/P43-contract-proof-report.md §2 S4` 不动）.
13. **Bug D regression PASS 6 cases** (v3 · r2 #3 closure): (i) category → canonical stable ID 语义绑定; (ii) legacy 2-tuple graceful; (iii) unknown canonical ID in answer → validate warn+skip; (iv) category=None → clarify-{i} fallback + warning; (v) category=unknown str → clarify-{i} fallback + warning; (vi) 既有无 category fixture 加载不破 (backward-compat).

**P43-04 (Freeze gate)**:
14. FREEZE state transition test PASS legal/illegal.
15. `workbench freeze` CLI emits **`docs/<system>/traceability_matrix.md`** 初始填充 · open-questions section 填 row per open question · 其他 section 空占位 + "deferred to P43-08" 标注 · SHA header · operator-alias footer (v3 · r2 #1 closure · anchor 对齐 P43-00:190 P34-P42 precedent).
16. `frozenSpecSHA` stored in localStorage · retrievable across reload.
17. **完整 `traceability_matrix.md` 其他 section 扩写延后 P43-08 Final Approval 或 post-P43** (r1 #2 + r2 #1 closure · 非本 Batch Exit).

### Combined Batch regression

18. **Three-lane regression vs post-P43-01 main baseline** (`61b12b3`):
    - Default pytest: ≥ (800 baseline + **~46 new tests**) passed (**16 authority** (14 R1-R6 + 2 observability) + **~30 other** (document_pipeline / clarification stable-ID 6 cases / freeze_gate / dual_sha / archive_compat / api_contract_lock / workflow_automaton / multi_tab_lock)) · 口径与 §5 C9 统一 (v3 · r2 polish #1 closure).
    - E2E pytest: ≥ 50 passed (P43-01 baseline + optional Q2-MT opt-in).
    - Zero regression on pre-existing 800 tests.

19. **Codex Q7=A coverage**: **13 Codex rounds** landed (10 adapter-boundary + 3 sub-phase closure · 精确 roster 见 §8) · 无 `可过-Gate` 缺失 · 无 `需阻止` 遗留.

## 5. Tier 1 self-audit (C1-C12 · v2 re-audit)

| Counter | v2 答 |
|---------|------|
| C1 · phantom file | v3 cites `system_spec.py:244-270` ✓ · `ai_doc_analyzer.py:121-127` (Ambiguity dataclass) ✓ · `ai_doc_analyzer.py:322/785` ✓ · `document_intake.py:874-912` ✓ · `ai-doc-analyzer.js:224` ✓ · `demo_server.py:2666` ✓ · `docs/c919_etras/traceability_matrix.md` + `docs/thrust_reverser/traceability_matrix.md` ✓（precedent 已存）· no phantom. |
| C2 · whitelist violation | **Q1=A formal §3d delta 请求 8 entry**（4 tests + 2 fixtures dir + `pyproject.toml` + `docs/<system>/traceability_matrix.md` per-system · v3 r2 #1+#2 closure）· **drop** `appendix_a_generator.py` + `docs/P43-02-batch-closure-report.md` + `docs/<system>/open_questions_<system>.md` 分叉。零 silent violation。 |
| C3 · Bug D fix scope | **v3 · 语义 category binding**（Ambiguity.category L1 additive · LLM prompt 扩 emit category · `_build_questions_from_ambiguities` 按 category → stable ID · missing/unknown graceful fallback clarify-{i} + warning）· 非位置耦合（v2 source-order 策略已撤回 · r2 #3 closure）· runtime 6 regression cases 全 asserted_pass。|
| C4 · Appendix A scope | v3 freeze-time 只填 `docs/<system>/traceability_matrix.md` 的 open-questions section · 其他 section 空占位 + "deferred to P43-08" · formatter 内联 cli.py ~30 LOC · 完整扩写延后。r1 #2 + r2 #1 closed。|
| C5 · Codex fatigue | **13 rounds concrete** · 10 adapter + 3 closure · sustainable · Q7=A 满足。|
| C6 · multi-tab lock lane | Q2-MT 保留 · 默认 string-grep + opt-in e2e 双车道（Codex r1/r2 未指正）。|
| C7 · non-blocking polish fold-in | Q5-A soften 一行文案 · 非 L3 · 无 Codex。Q5-B deleted。|
| C8 · §3d blacklist 自审 | 无 `state.yaml` / `workflow_engine.py` / `draft_truth.py` / SPA shell / `workbench/orchestrator.py` / schema $id bump / hardcode 开关。|
| C9 · test count 膨胀 | **16 authority** (14 R1-R6 + 2 obs) + **~30 other** (document_pipeline + clarification 6 cases + freeze_gate + dual_sha + archive_compat + api_contract_lock + workflow_automaton + multi_tab_lock) = **~46 new** · §4 #18 口径统一 (v3 · r2 polish #1 closure) · 每 test 映 §3e 或 §3d 一条 · 无 duplication。|
| C10 · Bug D 与 P43-01 regression | `tests/test_p43_doc_analyzer_blocker_fix.py` 进 Batch regression · clarification_history 3-tuple 扩不破既有 blocker/blocking_reasons assert（结构正交）。|
| C11 · dual-SHA P42 兼容 | `canonical_text_sha256` + `source_binary_sha256` 两 optional null-tolerant 字段 · P42 archive 两字段都缺 · restore 时 null 容忍 · 4 组合 test 守（r1 #4 closure）。|
| C12 · Appendix A signature path | Registry SKELETON 在 P43-04 产 · 完整 matrix 在 P43-08 产（延后）· P43-09 Final Approval 消费完整 matrix。v2 不越俎代庖。|

## 6. Gate strategy

> **重要区分 · v2 新增明示 · v3 措辞修 (r3 polish #1)**:
>
> - **本 Gate = `GATE-P43-02-BATCH-PLAN-QUALITY`**（plan-quality 前置门 · 审 v3 plan 本身是否可执行 · 无源码 · 无 Exit Criteria #1-#19 证据）
> - **执行 Gate = `GATE-P43-02-BATCH-CLOSURE`**（待 Batch-2 全部 3 sub-phase 源码落地 + 19 Exit Criteria 全绿 + 13 Codex rounds 全 `可过-Gate` 后提交）

**本 Gate (PLAN-QUALITY)** 提交条件:
- v3 plan commit 已落 `codex/p43-02-orchestrator-extend` HEAD
- Codex r3 adversarial review 返 **可过-Gate** 或 **需修正·信号弱**（弱 = 再一轮 surgical · 本 plan 总预算允许）
- **8 entry §3d delta 请求**向 Kogami 打包一次（§Q Q1）

**执行 Gate (BATCH-CLOSURE)** 提交条件（post-Gate · 不在本 v3 plan-quality 责任范围）: 见 §4 全 19 条 + §8 13 rounds。

Per Q1=D batch pattern (P43-01 独立 · P43-02..04 合并 · P43-05..07 合并 · P43-08..10 合并).

## 7. Stop points

1. 任何 R1-R6 test 实装后仍 fail → stop · 升 Kogami.
2. Archive backward-compat regression fail → stop · Kogami L3 仲裁.
3. Bug D fix 破 `test_p43_doc_analyzer_blocker_fix.py` → stop · Codex re-review.
4. Codex Q7=A 任一 round `需阻止` → stop · path ① surgical rewrite.
5. Three-lane regression 任一 pre-existing test regression → stop · root-cause.
6. §Q open questions (Q1 §3d delta / Q2-MT / Q5 / Q6) Codex r3 后仍 unresolved → Kogami 仲裁.
7. **§3d delta 8 entry Kogami 任一 reject** → stop · 评估是否可 work-around（例：test 拆到现有 authority_contract_r1_r6.py 聚合文件下 · fixture 合并 p43_validator_cases.json · pyproject.toml deps 改 external requirements file · traceability_matrix.md 写回 .planning/）· 必要时启 §Q Q1 Option B 或 C.

## 8. Governance (Codex per Q7=A · 13 rounds · 10 adapter + 3 closure)

**10 adapter-boundary reviews**:

| # | Step | Scope |
|---|------|-------|
| 1 | 3a/B | R1-R6 scaffold + `tools/check_authority_contract.py` min 骨架 |
| 2 | 3a/C | `workbench.js` authority-contract primary write |
| 3 | 3a/D | `cli.py` subcommands + `workbench_bundle.py` dual-SHA manifest |
| 4 | 3a/G | CI workflow + pre-commit + tools finalize |
| 5 | 3b/A | `ai_doc_analyzer.extract_document_text` + `pyproject.toml` optional deps + lazy import |
| 6 | 3b/B | `demo_server` `/api/document/extract` endpoint |
| 7 | 3b/D | `ai-doc-analyzer.js` wiring + readAsText test rewrite |
| 8 | 3b/E | Bug D stable-ID fix (multi-function · Counter F surface) |
| 9 | 3c/B | `workbench.js` FREEZE handler + frozenSpecSHA |
| 10 | 3c/C | `cli.py workbench freeze` + 内联 registry SKELETON formatter |

**3 sub-phase closure reviews**:

| # | Step | Scope |
|---|------|-------|
| 11 | P43-02 closure | authority contract + archive compat + api contract + dual-SHA combined sanity |
| 12 | P43-03 closure | document pipeline + Bug D + readAsText regression combined sanity |
| 13 | P43-04 closure | FREEZE + registry SKELETON + full Batch-2 three-lane regression |

**Total**: **13 rounds concrete**。No ambiguous "~10" / "8-10" 表达（r1 #5 closure）.

## §Q · Open questions (for Codex r2 + Kogami)

### Q1 · §3d whitelist delta · formal 请求 (A · 8 entry 打包一次 · r1 #1 + r2 #1/#2 closure)

v3 按 Q1=**A** 走：向 Kogami 正式请求 P43-00 §3d 扩展 **8 个 entry**（一次打包 · 不分散 · 不迂回走 .planning/）:

| # | Delta entry | 所属 whitelist 子段 | 理由 |
|---|-------------|-------------------|------|
| 1 | `tests/test_p43_document_pipeline.py` | Test Whitelist | P43-03 document pipeline regression |
| 2 | `tests/test_p43_clarification_stable_ids.py` | Test Whitelist | P43-03 Bug D semantic category binding regression (v3 · r2 #3) |
| 3 | `tests/test_p43_freeze_gate.py` | Test Whitelist | P43-04 FREEZE transition + `traceability_matrix.md` SKELETON emission |
| 4 | `tests/test_p43_dual_sha_manifest.py` | Test Whitelist | P43-02 Q12=B+a dual-SHA manifest regression (r1 #4) |
| 5 | `tests/fixtures/p43_document_pipeline/` (dir) | Test Whitelist | PDF/DOCX/TXT/MD fixture corpus |
| 6 | `tests/fixtures/p43_pre_archive/` (dir) | Test Whitelist | pre-P43 archive fixture (backward-compat) |
| **7** (v3 · r2 #2) | `pyproject.toml` L1 additive of `[project.optional-dependencies] document = ["pypdf>=4.0", "python-docx>=1.0"]` | Source Code Whitelist (new row for repo-root packaging metadata) | Q12=B+a 服务端 pypdf + python-docx deps 声明 · base install 不变 · 无 lockfile 触碰 · 无 required-dep 变更 · 纯 extras · v2 判 L1-additive-silent · r2 驳回 · v3 promote 到正式 delta |
| **8** (v3 · r2 #1) | `docs/<system>/traceability_matrix.md` (per-system emission · freeze-time SKELETON with open-questions section populated · 其他 section 空占位延 P43-08) | Doc Deliverables Whitelist (new sub-row for per-system precedent emission) | P43-00 §2c line 190 已 anchor P34-P42 precedent 为 Appendix A 模板 · 既有 `docs/c919_etras/traceability_matrix.md` + `docs/thrust_reverser/traceability_matrix.md` 为 exemplar · 但 §3d Doc Deliverables Whitelist 未显式授权 per-system emission · v3 formal delta 以避免 silent extension |

**已 DROP（v1 → v2 → v3）**:
- ❌ `src/well_harness/tools/appendix_a_generator.py`（r1 #2 closure · formatter 内联入 cli.py）
- ❌ `docs/P43-02-batch-closure-report.md`（r1 #1 closure · 改 `.planning/phases/.../reports/` 路径）
- ❌ `docs/<system>/open_questions_<system>.md` 分叉文件（v3 · r2 #1 closure · 回归 parent anchor `traceability_matrix.md`）

> **Codex r1 finding #1 + r2 finding #1/#2 closure**: v1 Q1 混淆方案 B · v2 漏 pyproject.toml + 用 open_questions 分叉自创文件 · v3 一次收齐 8 entry。

- **A** · 打包 8 entry 一次请 Kogami 批 · amend P43-00 §3d (Test Whitelist + Source Code Whitelist + Doc Deliverables Whitelist 各小幅加条) · 加 ~14 行。**Executor 建议 · 默认 A**.
- **B** · 拒 A 部分条目 · 要求 v4 plan surgical 再剥（例：砍 dual-SHA test · 砍 category 字段 · 把 traceability_matrix.md 写回 .planning/）· 但这样会丢 r1 #3/#4 或 r2 #1/#3 closure · 不推荐.
- **C** · 拒 A · 要求把 pyproject.toml optional deps 改走 `requirements-document.txt` external lockfile（避免改 pyproject）· 可接受 backup · 但破坏现代 Python packaging 标准 · 非 Executor 首选.

### Q2-MT · Multi-tab lock test lane（v1 保留 · Codex r1 未指正）

- **A** · 默认 lane string-grep (BroadcastChannel / storage handler presence)
- **B** · Opt-in e2e Playwright two-page
- **C** · Both（默认 grep + opt-in behavior）· **Executor 建议 · 与 P43-01 S3b/S4 parallel**

### Q5 · `demo_server.py:2666` polish（v2 · Q5-B 删 · r1 #6 closure）

Codex Step G r4 flagged: 消息 "apply_all_safe must be true" vs runtime truthy guard。

- **A** · Soften 文案 "must be truthy"（一行 · 无行为变更 · 与 YAML truthy 对齐）· **Executor 建议**
- ~~B · Harden strict bool~~ · **删（L3 语义变更 · 违 §3d · r1 #6 closure）**
- **C** · Leave · 文案 gap 写入 batch closure report

> **Codex r1 finding #6 closure**: v1 Q5-B 是 L3 breaking（必传参语义改）· 与 §3d L3 禁止并与 P43-00 §3d `demo_server.py` "rename / remove / 语义变更" 黑线直接冲突。v2 删 Q5-B · 只留 A/C · A 是一行文案修。

### Q6 · pypdf / python-docx packaging（v2 新 · r1 polish #8）

Q12=B+a 服务端抽取依赖 pypdf + python-docx · 打包选项:

- **A** · `pyproject.toml` `[project.optional-dependencies] document = ["pypdf>=4.0", "python-docx>=1.0"]` + `ai_doc_analyzer.extract_document_text` 函数内 lazy local import（`def extract_document_text(...): import pypdf; ...`）· base install 不变 · install extras 才有能力 · `pip install well-harness[document]`。**Executor 建议**.
- **B** · 作为 required dep 进 base · 简单但 base install 膨胀。
- **C** · 无依赖声明 · 运行时 ImportError 手工指引 · 差用户体验。

### Q8-batch · Batch size sanity（v1 保留）

v2 ~2100-2700 LOC · 3 sub-phases · 与 Q1=D 的单 Kogami gate 一致。

- **A** · Proceed combined plan single Kogami closure gate.
- **B** · Split · 违 Q1=D · 需 Kogami override.
- **C** · Staged execution · per-sub-phase 提交 · single final `GATE-P43-02-BATCH-CLOSURE`（PR arc 给 Kogami 可视性）· **Executor 建议**.

## §9 · Signature

**Signed**: Claude App Opus 4.7 (Solo Executor · v5.2 solo-signed + v5.3 addendum) · 2026-04-21

**Revision**: v3 surgical addendum on v2 · Codex r2 3 required + 1 polish addressed · no structural expansion

**Fixes applied (r1)**: whitelist_story_Q1A · appendix_a_scope_narrow · bug_d_stable_id · dual_sha_manifest · internal_consistency · q5b_removal

**Polish applied (r1)**: readAsText_test_rewrite · pypdf_packaging_q6

**Fixes applied (r2 · v3)**: freeze_artifact_aligned_to_parent (target = `docs/<system>/traceability_matrix.md` · open-questions section 初始填充 · 其他 section 延 P43-08) · pyproject_explicit_delta_entry (§Q Q1 entry #7) · bug_d_semantic_category_binding (Ambiguity.category L1 additive · LLM prompt 扩 · `_build_questions_from_ambiguities` 按 category → stable ID · missing/unknown → clarify-{i} warning fallback · 非位置耦合)

**Polish applied (r2 · v3)**: regression_count_alignment (§4 #18 + §5 C9 口径统一 ~46 new)

**Next action**: Submit for **Codex r3** adversarial review (Q7=A milestone-wide rule) · then submit **GATE-P43-02-BATCH-PLAN-QUALITY** (plan-quality 前置门) to Kogami · execution Gate `GATE-P43-02-BATCH-CLOSURE` unchanged · 待源码落齐。
