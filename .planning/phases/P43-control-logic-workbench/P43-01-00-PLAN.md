---
phase: P43
sub_phase: P43-01
plan: P43-01-00
plan_revision: v1
title: P43-01 Contract Proof Spike — real pdf happy/failure path + blocker bug fix + primitive contract lock
status: re-drafted · Awaiting GATE-P43-01-PLAN (Kogami)
date: 2026-04-20
owner: Claude App Opus 4.7 (Solo Executor) · v5.2 solo-signed + v5.3 addendum
scope_tier: Tier 1 · sub-phase (adapter-boundary write via `ai_doc_analyzer.py` · Codex review 必调)
preconditions:
  - `GATE-P43-PLAN (v7): Approved` (Kogami 2026-04-20 · main `b2f8692`)
  - P43-00-PLAN.md v7 §3a scope + Q1=D/Q8=B locked
  - Parent non-goals #15/#16/#17/#18 + §3d whitelists + §3e authority contract 均生效
  - v5.3 addendum: P43 milestone-wide Codex review 硬性规则（Q7=A）
non-goals:
  - 本 P43-01 不启动 P43-02+（workflow automaton / orchestrator extend / panel gen / wiring / debug / annotate / iterate / Final Approval / archive · 留给各自 sub-phase）
  - 不改 P43-00 v7 plan（plan 已 Gate-Approved · 残留走 P43-02+ plan · 不回改 P43-00）
  - 不实装 `draft_design_state` authority contract R1-R6（留给 P43-02）· 本 spike 不写 `draftDesignState` / `assignFrozenSpec` / `validateDraftAgainstFrozen` 任何 production 代码
  - 不扩 `workbench.js` 功能（仅做 schema inventory · dump 现有 localStorage key + manifest shape · 不加 UI panel / 不加 localStorage key / 不改 handler）
  - 不修 `generate_adapter.py` 的 hardcode（P43-01 只暴露 · 登记 known limitation · 实际处理留给独立 future phase）
  - 不扩 `demo_server.py` endpoint 语义（仅 inventory · dump 既有 `/api/workbench/*` contract · 非改动）
  - 不 bump JSON schema version
  - 不新建未在 §3d 授权的 source / doc / test 文件
  - 不以 "列出断裂" 为通过条件（P43-00 non-goal #16 硬规则 · Exit Criteria 必 asserted_pass）
---

# P43-01 · Contract Proof Spike

## 0. TL;DR

单 sub-phase · 单 sprint · ~1 day · ~200 LOC (ai_doc_analyzer.py fix + regression test) + ~500 LOC docs + ~500 LOC asserted-pass test harness + fixtures。

**为什么先跑 P43-01**：P43-00 v1 的"primitives 库存"是按 LOC 数的伪库存 · Codex r1 用 2 条真 bug 打碎（`blockers`/`blocking_reasons` key mismatch + `ai-doc-analyzer.js readAsText` 对 binary pdf/docx 静默失败）。v7 Plan §3a 规定：P43-02+ 所有 sub-phase plan **必须** 引用 P43-01 契约报告 · 不得 re-invent LOC assumption。**未 asserted_pass · P43-02+ 自动冻结**（non-goal #16）。

**P43-01 核心产出：**
1. `docs/P43-contract-proof-report.md` · primitive API 契约表 + 真实断裂清单 + asserted-pass 证据
2. `docs/P43-api-contract-lock.yaml` · `/api/workbench/*` endpoint contract（request body shape / response body shape · 回归测 lock）
3. `ai_doc_analyzer.py:838` bug 修 · `blockers` → `blocking_reasons`
4. `tests/test_p43_doc_analyzer_blocker_fix.py` · regression test（blocker surfaces through pipeline）
5. `tests/fixtures/p43_spike/*` · 真实 pdf 端到端 happy path + 人造 blocker failure path fixture

## 1. Scope（v7 §3a 8 item · 精简优先级）

### 1a · Must-land（5 必跑）

| # | Item | 输出 |
|---|------|------|
| S1 | 真实 happy path 端到端 asserted_pass | pdf → intake → ai_doc_analyzer → bundle → archive · 每步 SHA/input/output assert 表 |
| S2 | 真实 failure path blocker 正确 surface | 人造 intake packet with `component.allowed_range=None` · blocker 必被前端 surface |
| S3 | 修 `blockers`/`blocking_reasons` 真 bug | `ai_doc_analyzer.py:838` + line 841 改 "blocking_reasons" · regression test |
| S4 | PDF/docx 抽取能力实测 | `ai-doc-analyzer.js:191 readAsText()` 对 pdf/docx binary 行为 dump（预计乱码）· 结论入报告 |
| S5 | `demo_server.py` API 契约 lock | dump `/api/workbench/*` 所有 endpoint request/response shape → yaml |

### 1b · Report-only（3 登记 · 不修）

| # | Item | 输出 |
|---|------|------|
| R6 | analyzer ID ↔ intake clarification ID 契约核 | analyzer 产 `q-*` / `clarify-*` vs intake 收 `source_documents` / `component_state_domains` · 映射表入报告（若断 · 登记 P43-03 必修） |
| R7 | `generate_adapter.py:255,448` hardcode 暴露 | `max_n1k_deploy_limit=60.0` + `thr_lock` terminal hardcode · 对非 thrust-reverser spec 影响 demo + 报告登记 known limitation（非 P43-01 修复） |
| R8 | `workbench.js` + `workbench_bundle.py` schema inventory | 全部 localStorage key 名 / manifest shape / handoff-note shape · dump 真实数据入报告 |

### 1c · Scope discipline（Codex r3 "P43-01 scope 膨胀" 响应）

- Must-land 5 条 · 全部 asserted_pass · 不可降级
- Report-only 3 条 · 只 inventory · 不实装
- 发现新断裂（非上述 8 条）→ 登记报告 "Additional findings" section · 不扩 P43-01 scope
- 若执行中 S1-S5 任一 block 超 2 小时 · stop · 升 Kogami 判（部分 asserted_pass vs 全部冻结）

## 2. Deliverables（v7 §3d whitelist 内）

### 2a · Source Code 改动

| 文件 | 类型 | 规模 | §3d ladder |
|------|------|------|-----------|
| `src/well_harness/ai_doc_analyzer.py` | L2 bug fix · line 838 + 841 `blockers` → `blocking_reasons` · 不相关重构禁 | ~5 LOC | L2 · whitelist row 10 |

### 2b · Doc Deliverables（v7 Doc Deliverables Whitelist）

| 新建文件 | 规模 | 内容概要 |
|---------|------|---------|
| `docs/P43-contract-proof-report.md` | ~500 行 | §2a-h: 每 S1-S5 asserted_pass 证据 + R6-R8 inventory · 每条 primitive API 契约表 + 每条断裂清单 + 每条修复计划 |
| `docs/P43-api-contract-lock.yaml` | ~200 行 | `/api/workbench/bootstrap` / `bundle` / `archive-restore` / `recent-archives` 等 endpoint 的 request.body + response.body shape（字段 + 类型 + required） |

### 2c · Test Whitelist 新建

| 新建测试 | 规模 | 内容 |
|---------|------|------|
| `tests/test_p43_doc_analyzer_blocker_fix.py` | ~80 LOC · 3 test | ① `assessment.get("blocking_reasons")` truthy · pipeline 返 error dict 含 blockers ② happy path · `blocking_reasons=[]` · pipeline build bundle ③ regression · 旧 bug 不复发（`assessment["blockers"]` 读 raise 或返 default） |
| `tests/fixtures/p43_spike/real_pdf_happy_path/*` | ~3 files | `uploads/20260417-C919反推控制逻辑需求文档.pdf` 端到端产物（intake.json / bundle.json / archive_manifest.json · expected hash / shape）|
| `tests/fixtures/p43_spike/synthetic_blocker/*` | ~2 files | 人造 intake packet（component 缺 allowed_range）· expected blocker list |

注：`tests/fixtures/p43_spike/*` 属 `tests/fixtures/*` · Test Whitelist 的"fixture 子目录允许" generic 授权范围（v7 §3d Test Whitelist note："其他 tests/ 文件新建需 Executor 判是否合理"· 新 fixture 子目录合理 · 用 spike 名前缀 `p43_spike/` 保持与后续 P43-02+ fixture 分离）

## 3. Execution plan（按序 · 每步独立 commit）

### Step A · 环境勘查 + 真实 happy path 跑通（S1）

1. 找真实 pdf 入 intake pipeline 的入口：
   - CLI path: `python -m well_harness.cli bundle --intake <intake.json>`（or similar · 现仓实际命令以 S5 inventory 验）
   - Browser path: `workbench.js` → `/api/workbench/bundle` POST
2. 选 CLI 路径作 asserted_pass 主验（避开 browser binary 抽取已知断裂 S4）
3. 构造 intake packet（用 `uploads/20260417-C919反推控制逻辑需求文档.pdf` + 人工撰写 `source_documents` / `component_state_domains` 作最小 compliant input）
4. 跑 bundle · 跑 archive · 收集每步产物
5. 每产物 asserted_pass assert：
   - intake 产 `ready` or `blocked` 显式
   - bundle 产 workbench_bundle_v1.schema.json compliant payload
   - archive 产 archive_manifest_v1.schema.json compliant manifest · 含 bundle.json / README.md / workspace_handoff.json
6. 若任何 assert fail → 登记报告 §2b · 不继续 Step B 前 stop

**Step A commit：** `feat(P43-01): real happy path harness + fixtures (S1)` · 仅加 fixture + 报告 §2a 骨架 · 不改 src

### Step B · 修 `blockers`/`blocking_reasons` bug（S3）

**Codex r1 的 Counter F 真 bug · P43-01 不可跳过。**

1. Read `ai_doc_analyzer.py:815-855` 整体理解 `run_pipeline_from_intake` 流程
2. Read `document_intake.py:940 / 1028` 确认 emit key = `"blocking_reasons"`
3. Edit `ai_doc_analyzer.py:838`: `if assessment.get("blockers"):` → `if assessment.get("blocking_reasons"):`
4. Edit `ai_doc_analyzer.py:841`: `"blockers": assessment["blockers"]` → `"blocking_reasons": assessment["blocking_reasons"]`
5. 写 `tests/test_p43_doc_analyzer_blocker_fix.py`:
   - test 1 · positive：intake with `blocking_reasons=["foo"]` · pipeline returns dict with `error == "intake blocked"` + `blocking_reasons=["foo"]`
   - test 2 · happy：intake with `blocking_reasons=[]` · pipeline builds bundle（不返 error）
   - test 3 · regression · 断 `assessment.get("blockers")` 已不被用（grep-level 或运行时不触发）
6. 跑 default lane · assert 3 test pass
7. 跑全量 default lane regression · assert 796+3=799（P42 baseline +3）· 无其他回归

**Step B commit：** `fix(P43-01): ai_doc_analyzer blockers/blocking_reasons key mismatch (S3) · Codex r1 Counter F real bug closure · 3 regression tests added` · 含 ai_doc_analyzer.py + test file

**Codex review 必调（v5.3 adapter-boundary）：** `ai_doc_analyzer.py` 修改 = adapter-boundary write · 必须在 commit 后即刻 `/codex-gpt54` adversarial review · verified-by trailer 入 commit metadata。

### Step C · 真实 failure path（S2）

1. 基于 Step A intake packet · 人造一份 `component` 缺 `allowed_range`（`state_shape=analog`）· 保存为 `tests/fixtures/p43_spike/synthetic_blocker/intake.json`
2. 跑 `ai_doc_analyzer.run_pipeline_from_intake(intake)` · assert return dict 含 `error` + `blocking_reasons` 非空 · blocker content 语义 `component ... is analog but missing allowed_range.`
3. 证据入报告 §2c · 确认 blocker 路径 Step B 修复后端到端 surface
4. Optional: browser path 核（Step A 选 CLI 避开 · 此处 dump browser API `POST /api/workbench/bundle` 对同 intake 返错 shape · 验前端能拿 error dict）

**Step C commit：** `test(P43-01): real failure path blocker surface proof (S2)` · 仅加 fixture + 报告 §2c

### Step D · PDF/docx 抽取实测（S4）

**无代码改动 · 纯 dump 行为 + 登记。**

1. 开 headless browser（或直接 Node subprocess 运行 `ai-doc-analyzer.js:191 readAsText`）· load `uploads/20260417-...-.pdf` 和 `20260409-...-.docx` binary
2. Dump `readAsText` 返的字符串（预计：PDF header `%PDF-1.x...` + 大量 binary 乱码 · docx 应返 `PK\x03\x04...`）
3. 测试 analyzer 对该 "乱码 document_text" 的行为 · 是 silently 返 empty spec · 还是产 garbage clarification 问题 · 还是 raise
4. 结论入报告 §2d · "PDF/docx 浏览器端 readAsText 路径 broken" · 登记 P43-03 必修项（Q12=B+a 已决定 · 服务端 pypdf+python-docx）· 此处不实装只证实

**Step D commit：** `docs(P43-01): PDF/docx browser readAsText breakage proof (S4)` · 仅报告 §2d

### Step E · API 契约 lock（S5 + R8）

1. 枚举 `demo_server.py` 所有 `/api/workbench/*` endpoint（grep `WORKBENCH_*_PATH` 常量 + handler 分派）
2. 每 endpoint：
   - method（GET/POST）
   - request body shape（从 `request_payload.get(...)` 调用点反推）
   - response body shape（从 `_send_json(200, {...})` payload 反推）
   - required vs optional field
3. Dump 到 `docs/P43-api-contract-lock.yaml` · 格式：
   ```yaml
   endpoints:
     /api/workbench/bundle:
       method: POST
       request_body:
         required: [intake]
         properties:
           intake: {type: object, $ref: system_intake_packet_v1}
           workspace_handoff: {type: object, optional: true}
           workspace_snapshot: {type: object, optional: true}
       response_body_ready:
         required: [bundle]
         properties:
           bundle: {type: object, $ref: workbench_bundle_v1}
       response_body_blocked:
         required: [error, blocking_reasons]
     # ...
   ```
4. Dump `workbench.js` 所有 localStorage key（grep `wellHarnessWorkbench` / `localStorage.setItem` / `.getItem`）入报告 §2h · `workbench_bundle.py` manifest shape / handoff-note shape 亦入 §2h

**Step E commit：** `docs(P43-01): API contract lock + workbench schema inventory (S5 + R8)` · 仅 docs

### Step F · Report-only（R6 + R7）

1. R6 analyzer ID 契约：
   - grep `ai_doc_analyzer.py` 产的 clarification ID 前缀（预计 `q-*` / `clarify-*`）
   - grep `document_intake.py` 收的 clarification_answers key 前缀（预计 `source_documents` / `component_state_domains` / ...）
   - 做映射表 · 若断 · 登记 P43-03 必修
2. R7 generator hardcode：
   - `generate_adapter.py:255` `max_n1k_deploy_limit=60.0` · 含义：仅对 thrust-reverser 有意义 · 其他 system 可能不用
   - `generate_adapter.py:448` `thr_lock` terminal · 含义：thrust-reverser 特定 terminal component ID · 其他 system 不一定有
   - 结论入报告 §2f · 登记 known limitation · 后续独立 future phase 处理（非 P43 scope）

**Step F commit：** `docs(P43-01): R6/R7 report-only findings (analyzer contract + generator hardcode)` · 仅报告 §2e §2f

### Step G · 报告 finalize + Codex review + Gate

1. `docs/P43-contract-proof-report.md` finalize · 含：
   - Executive summary（asserted_pass/partial/fail 总结）
   - §2a-§2h 每 S / R 详细证据
   - §3 Additional findings（若有）
   - §4 P43-02+ scope recommendations（是保留 v7 scope 还是要求 v8）
2. Executor 自查：non-goal #16 asserted_pass 是否满足（S1 happy path + S2 failure path 都 asserted_pass）
3. Codex adversarial review（v5.3 addendum 必调 · Q7=A milestone-wide）：审报告是否真 asserted_pass · 断裂清单是否完整 · 修复计划是否机械
4. 提交 Kogami 审 GATE-P43-01-CLOSURE + 基于报告批 P43-02 方向（保留 v7 / 要求 P43-00 v8 / 其他）

**Step G commit：** `docs(P43-01): contract proof report finalize + Codex adversarial review trailer` · 报告最终版

## 4. Exit Criteria（非"列伤" · 必 asserted_pass · P43-00 non-goal #16 硬守）

1. **S1 asserted_pass**：真实 pdf 端到端 happy path · 每步 SHA + shape + required-field assert 通过 · fixture 入 `tests/fixtures/p43_spike/real_pdf_happy_path/*` · 测试在默认 lane 跑通
2. **S2 asserted_pass**：真实 failure path · `blocking_reasons` 非空 · pipeline 返 error dict · 被正确 surface · 测试在默认 lane 跑通
3. **S3 asserted_pass**：`ai_doc_analyzer.py:838+841` `blockers` → `blocking_reasons` 修 · `test_p43_doc_analyzer_blocker_fix.py` 3 test 默认 lane pass · Codex adversarial review 返"可过"或等价
4. **S4 报告 ** · PDF/docx `readAsText` 行为实测数据入报告 · 结论（broken / functional / partial）明确
5. **S5 asserted_pass**：`docs/P43-api-contract-lock.yaml` 覆盖所有 `/api/workbench/*` endpoint · 每 endpoint request/response shape 明确 · 字段 required/optional 明确
6. **R6/R7/R8 报告** · 3 条 inventory 结论入报告 · 后续 phase 修复 checklist 明确
7. **三轨回归** · vs P42 main baseline (`a6521ca`)：default pytest 799 passed / 1 skipped（+3 spike tests）· e2e 49 passed · adversarial wrapper 1 passed · 无既有测试回归
8. **`docs/P43-contract-proof-report.md` 完整** · Kogami 可审 · P43-02+ scope 方向可决
9. **Codex adversarial review 证据** · verified-by trailer 入 `a_doc_analyzer.py` 修改 commit（Step B）· 第二轮 report review 证据入 Step G commit · 两轮 verdict 入 `09C 外部审查简报`

## 5. Tier 1 self-audit（3 counter）

**C1 · Spike 可能揭示 v7 plan 不可执行**
- 风险：S4 PDF/docx readAsText 必 broken（已预判）· 但若 S1 happy path 也 block（例如 CLI bundle 对 intake 产 garbage bundle）· 则 P43-02+ scope 需大改
- 缓解：Step A asserted_pass 是 Gate · fail 直接停工升 Kogami · 不强推 · non-goal #16 已硬写

**C2 · Bug 修可能引入新回归**
- 风险：`blockers`/`blocking_reasons` 改 · 若下游 code 仍读 `blockers` 则破
- 缓解：Step B test 3 做 regression scan · 跑全量 default lane · Codex adversarial 第二眼
- 附加：grep 全仓 `assessment.get("blockers")` / `assessment["blockers"]` / `"blockers":` 其他 reader 位置 · 若有同步修

**C3 · Contract lock 静态 dump 可能遗漏 dynamic endpoint**
- 风险：`demo_server.py` 若有动态 path 路由 · grep `WORKBENCH_*_PATH` 漏
- 缓解：既用常量 grep · 也用 `do_POST` / `do_GET` method body AST scan · 加 assertion "no dynamic path construction beyond listed constants"；若发现动态 · 登记 C15 blind spot 加 Exit Criteria

## 6. Open Questions（Kogami 签 GATE-P43-01-PLAN 时仲裁）

### Q1 · S1 happy path 驱动路径
- A · CLI `python -m well_harness.cli bundle --intake ...` (推荐 · 避开 browser binary 抽取已知断裂)
- B · Browser `POST /api/workbench/bundle` (依赖 S4 readAsText · 若 S4 broken 则 S1 也 cascade)
- **Executor 建议：A**

### Q2 · S3 Codex review 时机
- A · Step B commit 后即刻（推荐 · 修完立刻验）
- B · Step G finalize 一次性（延迟 · 若修被 override 需重跑）
- **Executor 建议：A**

### Q3 · Spike 报告格式
- A · 纯 markdown（lean）
- **B · markdown + yaml contract-lock（推荐 · 下游 phase 机器可 consume）**
- **Executor 建议：B**

### Q4 · S4 dump 方法
- A · headless browser（Playwright）· 环境依赖重
- **B · Node subprocess 直接跑 ai-doc-analyzer.js readAsText 相当代码（推荐 · 更轻）**
- C · 仅 grep 源码推断（不实跑 · 不接受）
- **Executor 建议：B**

### Q5 · Additional findings 处理
- A · 全部登记报告 + 全部推给 P43-02+ plan
- **B · 登记报告 · 分类 critical/warning/nit · critical 可能触发 P43-00 v8（若 > 3 条）· 其他继续（推荐）**
- **Executor 建议：B**

## 7. 停点

**停点 1**：GATE-P43-01-PLAN 提交后 Codex adversarial review（v5.3 addendum）· 返"可过"才进 Step A
**停点 2**：Kogami 签 `GATE-P43-01-PLAN: Approved` + Q1-Q5 仲裁 · 才开始 Step A
**停点 2a**：Step A (S1) fail 或 partial · stop · 升 Kogami · 非自动降级
**停点 2b**：Step B (S3) bug fix Codex review 返"需修正" · path ① 到 P43-01-00 v2（非 P43-00）
**停点 3**：Step G 报告 finalize + Codex review · 提交 GATE-P43-01-CLOSURE
**停点 4**：Kogami 基于报告 decide P43-02+ scope · 若保留 v7 scope → 启 P43-02（workflow + orchestrator + doc pipeline + Freeze · Q1=D batching）

## 8. Codex review plan

**Step B 后第一轮（必调 · v5.3 adapter-boundary）**：
> 审 `ai_doc_analyzer.py:838+841` bug fix · assert key rename 正确 · `tests/test_p43_doc_analyzer_blocker_fix.py` 3 test 覆盖足够 · grep 全仓无其他 stale `assessment.get("blockers")` reader · 报告 verdict "可过" 或 "需修正 + 具体缺口"

**Step G 后第二轮（必调 · Q7=A milestone-wide）**：
> 审 `docs/P43-contract-proof-report.md` 是否真 asserted_pass（S1-S5 证据是否充分 · R6-R8 inventory 是否完整）· Exit Criteria 7 条是否可机械验证 · P43-02+ scope 建议是否 evidence-based · 报告 verdict "可过" / "需修正·信号弱" / "需修正·信号强" / "需阻止 · 建议 P43-00 v8 或 R4 撤回 P43"

**Codex 输出处理：**
- 可过 → 继续 Gate
- 需修正·信号弱 → path ① 到 P43-01-00 v2 · 在 P43-01 内修
- 需修正·信号强 / 需阻止 → 升 Kogami · R4 决策（是否触发 P43-00 v8 / 撤回 P43）

---

**Signed:** Claude App Opus 4.7 (Solo Executor) · v5.2 solo-signed + v5.3 addendum · 2026-04-20
**Revision:** v1
**Awaiting:** Codex adversarial review (v5.3 addendum · pre-Gate) + `GATE-P43-01-PLAN: Approved` (Kogami) + Q1-Q5 仲裁
**Parent:** P43-00-PLAN.md v7 (GATE-P43-PLAN (v7) Approved 2026-04-20 · main `b2f8692`)
