---
phase: P43
sub_phase: P43-01
plan: P43-01-00
plan_revision: v3 (post-Codex-round-2 · path ① · 5-fix surgical · final before hard stop per §7)
title: P43-01 Contract Proof Spike — correct source_documents schema + honest S1 scope + clarify-{i} drift R6 strengthen + honest scope admission
status: re-drafted · Awaiting GATE-P43-01-PLAN (v3) (Kogami)
date: 2026-04-20
owner: Claude App Opus 4.7 (Solo Executor) · v5.2 solo-signed + v5.3 addendum
scope_tier: Tier 1 · sub-phase (adapter-boundary write via `ai_doc_analyzer.py` · Codex review 必调)
verified-by: codex-gpt54-xhigh (r1 需阻止 · v2 closed Fix #2/#3/#4 · r2 需修正·信号强 · 5 v3 surgical fixes · final path ① round per §7 stop point 2)
preconditions:
  - `GATE-P43-PLAN (v7): Approved` (Kogami 2026-04-20 · main `b2f8692`)
  - P43-00-PLAN.md v7 §3a scope + Q1=D/Q8=B locked
  - P43-01 v1 (`8ca2c68`) → Codex r1 `需阻止` · 9-point audit · 4 required fixes
  - P43-01 v2 (`8e39e7e`) → Codex r2 `需修正·信号强` · Fix #2/#3/#4 closed · Fix #1 residual + 5 v3 fixes required
  - v5.3 addendum: P43 milestone-wide Codex review 硬性规则（Q7=A）
non-goals:
  - 本 P43-01 不启动 P43-02+（workflow / orchestrator / panel / wiring / debug / annotate / iterate / Final Approval / archive）
  - 不改 P43-00 v7 plan
  - 不实装 `draft_design_state` authority contract R1-R6（留给 P43-02）
  - 不扩 `workbench.js` / `ai-doc-analyzer.js` 功能（仅：① S3 frontend 必要同步以修 Counter F 完整 closure · ② 其他位置仅 inventory）
  - 不修 `generate_adapter.py` hardcode（只暴露登记）
  - 不扩 `demo_server.py` endpoint 语义（仅 inventory · `/api/workbench/*` + `/api/p15/*` 全 contract lock）
  - 不 bump JSON schema version
  - 不新建未在 §3d 授权的 source / doc / test 文件
  - 不以 "列出断裂" 为通过条件（P43-00 non-goal #16 硬规则）
  - **不伪造 E2E 路径**（Codex r1 指正 · 新 non-goal）· S1 必须调真实 `/api/p15/run-pipeline` 或真实 `run_pipeline_from_intake(intake_dict)` entrypoint · 不以"手构 packet + CLI bundle"替代
  - **不留 frontend/backend 键漂移**（Codex r1 Counter F 完整 closure）· 若后端 response key 变 · 前端同步；否则 blocker 路径 UI 仍断
---

# P43-01 · Contract Proof Spike (v3)

## 0. TL;DR · v2 → v3 差异（Codex r2 verdict 需修正·信号强 · 5 surgical · final path ① round）

Codex r2 verdict: `需修正·信号强` · 6-point audit (2026-04-20)。v2 关闭了 r1 Fix #2 (blocked contract) / #3 (JS consumer) / #4 (fixtures whitelist) · 但 Fix #1 (S1 真路径) 仍有残留事实错 + 4 新 structural 发现。v3 是 final surgical（§7 停点 2：v3 若仍需修正 · 硬停 path ① · 升 Kogami R4 决策）。

| Codex r2 残留 | v2 事实错 / 未覆盖 | v3 修正 |
|--------------|-----------------|--------|
| **v3 Fix A · source_documents schema 错** | v2 §3 Step A 写 source_documents 字段为 `{kind, path, sha256}` · 实际 `SourceDocumentRef` 需要 `id/kind/title/location/role` · 且 packet 顶层需要 `source_of_truth`（`document_intake.py:47,186,293`）| v3 Step A + §2c fixture 字段矫正：`{id, kind, title, location, role}` + packet 顶 `source_of_truth` |
| **v3 Fix B · SHA "真实 pdf SHA 引用" 误导** | v2 宣称 intake 含真实 pdf SHA · 但 `run_pipeline_from_intake()` 不 read file · 不 validate sha256 · S1 最多证 "packet 带 pdf 元数据" 非 "pdf 被消费" | v3 S1 scope 诚实化：改称 **"pipeline function contract proof"** · 非 "pdf consumption proof"。SHA 字段保留（便于 P40 provenance alignment）但不作"pdf 被消费"证据。honest boundary 在 §1c 写明 |
| **v3 Fix C · session_id/clarification 注入层漂移（Counter F 第二层 real bug）** | v2 未覆盖 `_inject_clarification_answers()`（`ai_doc_analyzer.py:785-806`）· 写 `question_id: f"clarify-{i}"` · 但 `_unanswered_clarifications` 期望稳定 question_id（`source_documents` / `component_state_domains` · `document_intake.py:267,271`）· 答案被静默丢弃 · `ready_for_spec_build` 永远 false | v3 R6 scope 强化：新命名 R6-A `analyzer ID ↔ intake clarification ID 契约核 · 含 `clarify-{i}` 真 bug`。报告必须：① 实证 `_inject_clarification_answers()` 写 `clarify-{i}` · `_unanswered_clarifications` 读 stable ID · 两者不匹配 ② session_clarification_history flow 断 · 证据 dump ③ 登记 P43-03 必修（Q12=B+a 框架下加 ID 契约）。**仍为 report-only · 不在 P43-01 修** |
| **v3 Fix D · 诚实 scope 声明** | v2 §0 宣 "no scope creep" 但实际加了第 4 个 default test + Playwright test file + 回归从 v1 的 799/49 变 800/50 · 自相矛盾 | v3 §0 + §4 明写："v1→v2 确实扩了 · v2 加 1 default test（test 4 frontend consumer audit）+ 1 Playwright opt-in test（readAsText behavior）· default lane 799→800 · e2e lane 49→50 · v3 不再扩"。v3 scope 硬承诺：不新加 test · 不新加 fixture 文件 · 不新加 deliverable |
| **v3 Fix E · Q1 A+B full-pipeline gap 诚实承认** | v2 Q1 A+B 覆盖函数层+HTTP handler · 未覆盖前端 `p15RunPipeline()` 包装 + `FileReader.readAsText()` + session_id injection 分支 · 宣称 "两者并跑互证" 实为 partial | v3 Q1 新增 D 选项 "诚实承认 full pipeline proof 需 Playwright + 真实 UI flow · 但 scope 超 P43-01 · 归 P43-03+"。推荐 A+B 并保留 Q1 C (Playwright) 作 future reference · 不假装 "full pipeline 已证" |

**v3 scope 真承诺**：只修 5 点 · 0 新 test · 0 新 fixture · 0 新 deliverable。§2c 表格 test 计数保持 v2 的 4 default + 1 e2e。§2a + §2b deliverables 不动。Step A-G 流不变。

---

## 0a. TL;DR · v1 → v2 差异（Codex r1 verdict 需阻止 · 4 必补 · 保留历史）

Codex r1 verdict: `需阻止` · 9-point audit (2026-04-20)。v1 因 Executor 起草前未实核代码 · 多处事实错。v2 是 surgical 4-fix · 不扩 scope。

| Codex r1 残留 | v1 事实错 | v2 修正 |
|---------------|---------|--------|
| **Fix #1 · S1 真路径** | v1 "手构 intake + CLI bundle" 不等价 "真实 pdf happy path"。CLI 签名 `bundle <packet_path>` 非 `bundle --intake`。`/api/workbench/bundle` 取 `packet_payload` 非 intake。| v2 S1 改用 **真实 `/api/p15/run-pipeline` endpoint**（`demo_server.py:491` handler · `ai_doc_analyzer.run_pipeline_from_intake()` 入口）· 构造 intake_packet dict（minimal compliant · 含真实 pdf source_document 引用 + SHA）· POST 该 endpoint 或直接 Python 调函数 · assert return shape。**注：当前无 "pdf binary → document_text" 真路径**（S4 会证实 `readAsText` 对 binary broken）· S1 的 "真实" 边界 = "真实 intake packet dict · 真实函数入口 · 真实 SHA 绑定" · 非 "从 pdf 原始字节起跑"（honest statement · v7 §2b 已承认） |
| **Fix #2 · blocked 响应契约** | v1 写 "pipeline 返 error dict" · 实代码 `ai_doc_analyzer.py:838-843` 返 `{status: 'blocked', blockers: [...], message: ...}` · Step E contract-lock 示例亦错 | v2 Exit Criteria S2 改用真实 contract：`return["status"] == "blocked"` + `return["blockers"]` 非空列表 + `return["message"]` 字符串。Step E yaml contract-lock 示例亦校正 |
| **Fix #3 · JS consumer 同步（Counter F 完整 closure）** | v1 只修后端 · 但 `ai-doc-analyzer.js:527-528` 读 `result.status === "blocked"` + `result.blockers` · 是现有前端契约。如果后端改 emit key 必须同步前端；若保外部契约不变则前端无需改 | v2 决策：**保外部 response 契约不变** · 后端仅改 READ side（`assessment.get("blocking_reasons")`）· emit side 保留 `"blockers"` key（前端已读 · 不破兼容）· 前端 zero change · Counter F closure = backend internal fix only · frontend 用 test 守为 "stale reader detection"（S3b inventory：grep frontend 所有 `result.blockers` reader · 确认契约点） |
| **Fix #4 · fixtures 白名单显式** | v1 用 "tests/fixtures 子目录合理" 自授权 `tests/fixtures/p43_spike/*` | v2 在 TL;DR 显式记：v7 §3d Test Whitelist 已包 `tests/fixtures/p43_validator_cases.json` 单例显式；`tests/fixtures/p43_spike/*` 作 **generic fixtures extension**（与 v7 §3d Test Whitelist note "其他 tests/ 文件新建需 Executor 判是否合理" 一致）· 本 plan 作 Executor 显式判：合理 · 用 spike 前缀隔离。Codex r1 flag 信号弱但保留 · 记为 Executor 判决 |

**v2 其他必补（Codex r1 未被 4-fix 直接覆盖但必须处理）：**

- **Step D 方法论**：Node subprocess 不够 · `FileReader.readAsText(file)` 是浏览器 API · Node `fs.readFile` 不等价。v2 Step D 改用 **Playwright**（仓库已有先例 `tests/test_system_switcher_smoke.py:49`）· headless chromium load 真实 pdf/docx binary file · 真调 `FileReader.readAsText` · dump 结果
- **Step B test 1/2 假测修正**：v1 把 `blocking_reasons` 当 intake 输入字段 · `intake_packet_from_dict()` 不读此字段。v2 test 1 改用：构造 packet（缺 `source_documents`）→ `assess_intake_packet(packet)` → assertion `blocking_reasons` 非空 → `run_pipeline_from_intake({..packet dict..})` → assert return.status == "blocked" + return.blockers 非空（真实 contract 闭环）
- **Q1 假问题修正**：v1 Q1 A/B 均非真路径。v2 Q1 改 3 真选项：A=直接 Python 调 `run_pipeline_from_intake(intake_dict)` · B=HTTP `POST /api/p15/run-pipeline` + fixture intake dict · C=Playwright 模拟 ai-doc-analyzer UI 从 pdf upload 到 result（最真实 · 最重 · 依赖 S4 readAsText 可能 broken）
- **Q5 超范规则删**：v1 Q5 "critical > 3 触发 P43-00 v8" 是 Executor 私设新治理规则 · 超父 plan。v2 删 · 恢复为 "登记后 · Kogami R4 决策"
- **C4/C5/C6 新 counter**：v1 缺 "假 E2E 替代真 pdf 路径" / "前端 blocked-reader stale" / "fixtures 白名单歧义" 三个 blind spot · v2 §5 加

**v2 scope 承诺**：只修 4 点 + 6 条 correction + 3 counter 新增。不扩 S1-S5 + R6-R8 scope · 不改 §2 deliverables · 不改 Step A/B/C/D/E/F/G 整体序列 · 不改 Exit Criteria 数量（9 条）。

---

## 1. Scope（v7 §3a 8 item · 精简优先级）

### 1a · Must-land（5 必跑）

| # | Item | 输出 |
|---|------|------|
| S1 | **真实 `/api/p15/run-pipeline` happy path** asserted_pass | intake_packet dict（含真实 pdf source_document SHA 引用）→ HTTP POST 或 Python 直调 → assert return dict 含 `assessment` + `bundle` + `system_snapshot` + `bundle` 非 null + `system_snapshot.ready_for_spec_build=true` |
| S2 | **真实 `/api/p15/run-pipeline` failure path** · blocked contract asserted_pass | intake_packet dict 人造缺 `source_documents` → POST 或直调 → assert `return.status == "blocked"` + `return.blockers` 非空 + `return.message` 字符串 + `ai-doc-analyzer.js:527-528` consumer 契约对齐 |
| S3 | **修 `blockers`/`blocking_reasons` 真 bug** + **frontend consumer audit** | S3a `ai_doc_analyzer.py:838` READ side 改 `assessment.get("blocking_reasons")` · `:841` READ side 改 `assessment["blocking_reasons"]` · EMIT key 保留 `"blockers"`（前端契约不变）· S3b grep frontend 全文 `result.blockers` / `result.status === "blocked"` reader · 确认前端契约点 · 登记报告 |
| S4 | **PDF/docx 浏览器 `readAsText` 实测**（Playwright） | headless chromium load pdf/docx file · 真调 `FileReader.readAsText` · dump 返字符串 · 结论（broken / partial / functional）入报告 |
| S5 | **API 契约 lock** · `/api/workbench/*` + `/api/p15/*` 全枚举 | dump 每 endpoint request/response shape（含 blocked 分支 / error 分支）入 `docs/P43-api-contract-lock.yaml` |

### 1b · Report-only（3 登记 · 不修）

| # | Item | 输出 |
|---|------|------|
| R6 | **analyzer ID ↔ intake clarification ID 契约核 · 含 `clarify-{i}` 真 bug**（Codex r2 Fix C · Counter F 第二层）| Must prove 3 facts：① `_inject_clarification_answers()` (`ai_doc_analyzer.py:799`) 写 `question_id: f"clarify-{i}"`（indexed sequence）· 实证 grep · ② `_unanswered_clarifications()` (`document_intake.py:~820-840`) 对 stable question_id（`source_documents` / `component_state_domains`）做 lookup · 实证 grep · ③ 两者不匹配 · session_clarification_history 答案被静默丢弃 · `ready_for_spec_build` 可能永假 · 构造 fixture dump 证据。**登记 P43-03 必修**（Q12=B+a 服务端 pypdf 框架已决 · 加 ID 契约即可）· 非 P43-01 scope 内修 |
| R7 | `generate_adapter.py:255,448` hardcode 暴露 | `max_n1k_deploy_limit=60.0` + `thr_lock` terminal hardcode · 对非 thrust-reverser spec 影响 · 报告登记 known limitation |
| R8 | `workbench.js` + `workbench_bundle.py` schema inventory | localStorage key / manifest shape / handoff-note shape dump 入报告 |

### 1c · Scope discipline（v1 Codex r3 "P43-01 scope 膨胀" + r1 "假 E2E 替代" 响应）

- Must-land 5 条 · 全部 asserted_pass · 不可降级
- Report-only 3 条 · 只 inventory · 不实装
- 发现新断裂 → 登记报告 "Additional findings" · 不扩 P43-01 scope
- S1-S5 任一 block 超 2 小时 · stop · 升 Kogami 判
- **S1 honesty 边界**（v1→v2 引入 · v3 Codex r2 Fix B 再矫正）：`"真实 pdf happy path"` **正式改称** `"pipeline function contract proof"` · 边界定义 = **真实 intake_packet dict（符合 dataclass 契约 · 含 pdf metadata `location` 引用）+ 真实 `/api/p15/run-pipeline` 或直接函数入口 + 真实 assert return shape**。**不等价以下任一**：① "从 pdf 原始字节起跑到 bundle"（P7 contract 当前无此路径 · S4 会证实 `readAsText` 路径 broken）② "pdf 真被 pipeline 消费"（`run_pipeline_from_intake` 不 read file · 不 validate SHA · **Codex r2 Fix B 明示**）③ "full user flow pipeline proof"（绕过前端 `p15RunPipeline()` wrapper / `FileReader.readAsText()` / `session_id → _inject_clarification_answers` · **Codex r2 Fix E 明示**）· S1 测 function layer contract · 不夸大

## 2. Deliverables（v7 §3d whitelist 内 · v2 修正白名单解释）

### 2a · Source Code 改动

| 文件 | 类型 | 规模 | §3d ladder |
|------|------|------|-----------|
| `src/well_harness/ai_doc_analyzer.py` | L2 bug fix · lines 838+841 READ side 改 `blocking_reasons` · EMIT key 保留 `"blockers"` · 不相关重构禁 | ~5 LOC | L2 · whitelist row 10 |

### 2b · Doc Deliverables（v7 Doc Deliverables Whitelist）

| 新建文件 | 规模 | 内容 |
|---------|------|------|
| `docs/P43-contract-proof-report.md` | ~500 行 | §2a-i: S1-S5 asserted_pass 证据 + R6-R8 inventory + S3b frontend consumer audit + S4 Playwright dump |
| `docs/P43-api-contract-lock.yaml` | ~250 行 | `/api/workbench/*` + `/api/p15/*` 全 endpoint request/response shape（含 blocked + error 分支）|

### 2c · Test Whitelist 新建（v2 显式白名单解释）

| 新建测试 | 授权源 | 规模 | 内容 |
|---------|--------|------|------|
| `tests/test_p43_doc_analyzer_blocker_fix.py` | v7 §3d Test Whitelist 显式列 | ~100 LOC · 4 test | ① positive blocked: 缺 source_documents 的 packet · `run_pipeline_from_intake()` 返 `{status: "blocked", blockers: [...], message}` ② happy: ready packet · return 含 `assessment + bundle + system_snapshot` ③ regression: stale `assessment.get("blockers")` 读（AST grep 守）· assert 无 ④ frontend consumer: grep `ai-doc-analyzer.js` confirms `result.blockers` reader 存在 · 后端 emit key 契约 alignment |
| `tests/fixtures/p43_spike/real_pdf_happy_path/*` | **Executor 显式判**（v7 Test Whitelist note · 合理）· 3 files | fixture | `intake_minimal_ready.json`（含真实 pdf source_document + SHA）· `expected_pipeline_response.json`（ready branch）· `README.md`（fixture 说明）|
| `tests/fixtures/p43_spike/synthetic_blocker/*` | 同上 · 2 files | fixture | `intake_missing_source_docs.json`（缺 source_documents 引发 blocker）· `expected_blocked_response.json`（status=blocked · blockers 非空） |
| `tests/test_p43_readAsText_browser_behavior.py` | **Executor 显式判**（Playwright lane · opt-in e2e · 非默认 lane） | ~80 LOC · 1 test | Playwright load pdf/docx · trigger `FileReader.readAsText` · dump 结果 · assert 结果 shape（预期 broken · v2 Codex r1 Step D 方法论修正） |

注：所有新 fixture / test 属 `tests/` 目录下 · 父 plan v7 §3d Test Whitelist note "其他 tests/ 文件新建需 Executor 判是否合理" · 本 plan 作 Executor 显式 approval。Codex r1 flag "信号弱" 记录 · 不扩父 plan。

## 3. Execution plan（按序 · 每步独立 commit · v2 修正）

### Step A · 真实 `/api/p15/run-pipeline` happy path（S1）

1. Read `demo_server.py:1748` P15 handler + `ai_doc_analyzer.py:809` `run_pipeline_from_intake` 完整签名 + return shape
2. 构造 intake_packet dict（minimal compliant · Codex r2 Fix A 字段矫正 · 以 `document_intake.py:47,186,293` 实际 dataclass 契约 为准）：
   - **packet 顶层**：`system_id` / `title` / `objective` / **`source_of_truth`**（Codex r2 漏）
   - **`source_documents`**: list of `{id, kind, title, location, role}`（**Codex r2 字段矫正 · v2 `{kind, path, sha256}` 写错**）· 示例：
     ```python
     [{
       "id": "c919-etras-spec-pdf",
       "kind": "pdf",
       "title": "C919反推控制逻辑需求文档",
       "location": "uploads/20260417-C919反推控制逻辑需求文档.pdf",
       "role": "authoritative_requirements"
     }]
     ```
   - SHA 字段（Codex r2 Fix B）：`SourceDocumentRef` dataclass **不包 sha256** · `run_pipeline_from_intake()` 不 read file · 不 validate SHA · 因此 **S1 证的是 "pipeline function contract"** 非 "pdf 被消费"（§1c honesty boundary）· SHA 若 P40 provenance 需要可在 P40 registry lookup 外部获取 · 但不作 P43-01 S1 证据
   - `components` / `logic_nodes` / `acceptance_scenarios` / `fault_modes` / `knowledge_capture`（全部 minimal 但合 schema · 产 ready packet）
   - `clarification_answers`: 全答（确保 `ready_for_spec_build=true`）· **Codex r2 Fix C warning**: 用 stable question_id（`source_documents` / `component_state_domains` · 非 `clarify-{i}`）· 若用 stable ID 则 ready 路径可跑通 · `clarify-{i}` 漂移是 R6 separate finding
3. 保存为 `tests/fixtures/p43_spike/real_pdf_happy_path/intake_minimal_ready.json`
4. Python 直调 `run_pipeline_from_intake(intake_dict)`：
   - assert return dict 含 `assessment` + `bundle` + `system_snapshot`
   - assert `bundle` 非 null
   - assert `system_snapshot.ready_for_spec_build is True`
   - assert 无 `status` key（ready path 不返 status）· 或 assert status != "blocked"
5. 保存 return dict 为 `expected_pipeline_response.json` · asserted_pass 证据
6. 同样通过 HTTP `POST /api/p15/run-pipeline` 验证（服务端路径） · 对比直调 return · assert shape 一致

**Step A commit**: `feat(P43-01): real /api/p15/run-pipeline happy path harness + fixtures (S1 · v2)` · 仅 fixture + 报告 §2a 骨架

### Step B · 修 `blockers`/`blocking_reasons` bug（S3a + S3b · v2 前端 audit）

1. Edit `ai_doc_analyzer.py:838`: `assessment.get("blockers")` → `assessment.get("blocking_reasons")` (READ side 修)
2. Edit `ai_doc_analyzer.py:841`: `"blockers": assessment["blockers"]` → `"blockers": assessment["blocking_reasons"]` (READ side 修 · EMIT key `"blockers"` 保留 · frontend 契约不变)
3. 写 `tests/test_p43_doc_analyzer_blocker_fix.py` 4 test（详见 §2c）：
   - test 1 positive blocked: 构造 packet 缺 source_documents → `run_pipeline_from_intake(intake_dict)` → assert `status == "blocked"` + `blockers` 非空 + `message` 字符串
   - test 2 happy: ready packet → assert return 有 `assessment + bundle + system_snapshot` + no "blocked" status
   - test 3 regression: grep `src/well_harness/ai_doc_analyzer.py` · assert 无 `assessment.get("blockers")` 或 `assessment["blockers"]` 剩余（防 stale reader 复发）
   - test 4 frontend consumer audit: grep `src/well_harness/static/ai-doc-analyzer.js` · assert 含 `result.blockers` + `result.status === "blocked"` substring · 证前端契约点 · 后端 emit key `"blockers"` 与前端 reader alignment
4. 跑 default lane · assert 4 test pass
5. 跑全量 default lane regression · vs P42 baseline (`a6521ca` · 796 passed) · assert 800 passed（+4 spike tests · no regression）

**Step B commit**: `fix(P43-01): ai_doc_analyzer blocking_reasons READ fix + 4 regression tests (S3 · v2)` · 含 ai_doc_analyzer.py + test file

**Codex review 必调（v5.3 adapter-boundary）**: Step B commit 后即刻 `/codex-gpt54` · v1 Codex r1 6-point Gap 3 未覆盖全 · v2 Step B 需确认 4-test 闭环 + 无其他 stale reader · verified-by trailer

### Step C · 真实 failure path（S2）

1. 构造 packet 缺 `source_documents`（触发 `assess_intake_packet` 首 blocker "at least one source document is required"）→ `tests/fixtures/p43_spike/synthetic_blocker/intake_missing_source_docs.json`
2. 直调 `run_pipeline_from_intake(intake_dict)` + HTTP POST 两路径均测
3. Assert:
   - `return["status"] == "blocked"`
   - `return["blockers"]` 非空 list · 含 "at least one source document is required."
   - `return["message"]` == "存在阻塞问题，无法构建诊断 bundle。请先解决以下问题。"
   - Frontend consumer 能拿到：`result.status === "blocked"` → UI render blocker list
4. 保存 return dict 为 `expected_blocked_response.json`

**Step C commit**: `test(P43-01): real failure path blocked contract proof (S2 · v2)` · fixture + 报告 §2c

### Step D · PDF/docx `readAsText` 实测（S4 · v2 Playwright）

**Codex r1 修正: Node subprocess 不等价浏览器 FileReader · 用 Playwright 实测。**

1. 写 `tests/test_p43_readAsText_browser_behavior.py`（opt-in e2e lane · 非默认）：
   - Playwright launch chromium headless
   - load ai-doc-analyzer.html
   - programmatically inject file input with `uploads/20260417-...-.pdf` binary
   - trigger `FileReader.readAsText(file)`
   - await result · dump string · assert shape（预期：PDF header `%PDF-1.x` + 大量 mojibake chars · docx 返 `PK\x03\x04` header + binary noise）
2. 测试 analyzer UI 对 "乱码 document_text" 的行为 · 是否 silently 产 garbage spec · 还是 raise · dump
3. 结论入报告 §2d · "PDF/docx 浏览器 readAsText 路径 broken" · 登记 P43-03 必修（v7 Q12=B+a 服务端 pypdf+python-docx 已决定）

**Step D commit**: `test(P43-01): Playwright readAsText browser behavior proof (S4 · v2)` · 仅 test file + 报告 §2d

### Step E · API 契约 lock（S5）

1. 枚举 `demo_server.py` `/api/workbench/*` + `/api/p15/*` 全 endpoint（grep `*_PATH` 常量 + `do_POST`/`do_GET` 分派）
2. 每 endpoint dump：
   - method
   - request body shape（从 `request_payload.get(...)` 反推 · AST scan）
   - response body shape（从 `_send_json(code, {...})` 反推 · 分 success/blocked/error 分支）
   - required vs optional field
3. Dump 到 `docs/P43-api-contract-lock.yaml` · **v2 示例用真实 contract**：
   ```yaml
   endpoints:
     /api/p15/run-pipeline:
       method: POST
       request_body:
         required: [intake_packet]
         properties:
           intake_packet: {type: object, $ref: system_intake_packet_v1}
           session_clarification_history: {type: array, optional: true}
       response_success:
         required: [assessment, bundle, system_snapshot]
       response_blocked:
         required: [status, blockers, message]
         status: {const: "blocked"}
       response_error:
         required: [error, message]
         error: {const: "intake_validation_failed"}
   ```
4. Dump `workbench.js` + `ai-doc-analyzer.js` 所有 localStorage key · `workbench_bundle.py` manifest shape · 入报告 §2h (R8)

**Step E commit**: `docs(P43-01): full API contract lock + frontend schema inventory (S5 + R8)` · 仅 docs

### Step F · Report-only（R6 + R7）

1. R6 analyzer ↔ intake clarification ID 映射核（前后端 ID 前缀 vs 稳定 question_id · 做表 · 断则登记 P43-03）
2. R7 generator hardcode（`:255`, `:448` · 登记 known limitation）

**Step F commit**: `docs(P43-01): R6/R7 report-only findings` · 仅报告

### Step G · 报告 finalize + Codex review + Gate

1. `docs/P43-contract-proof-report.md` finalize · 含 Executive summary + §2a-§2i 每项证据 + §3 Additional findings + §4 P43-02+ scope recommendations
2. Executor 自查 non-goal #16 asserted_pass
3. Codex adversarial review（v5.3 · Q7=A）· 审报告真 asserted_pass
4. 提交 Kogami 审 GATE-P43-01-CLOSURE

**Step G commit**: `docs(P43-01): contract proof report finalize + Codex r2 review trailer`

## 4. Exit Criteria（9 条 · 必 asserted_pass · P43-00 non-goal #16 硬守 · v2 contract 修正）

1. **S1 asserted_pass**：真实 `/api/p15/run-pipeline` happy path · intake_packet dict (含真实 pdf SHA) → return 含 `assessment + bundle + system_snapshot` + `bundle` 非 null + `ready_for_spec_build=true` · fixture 入 `tests/fixtures/p43_spike/real_pdf_happy_path/*` · 测试默认 lane 跑通
2. **S2 asserted_pass**：真实 failure path · `return.status == "blocked"` + `return.blockers` 非空 + `return.message` 字符串 · fixture 入 `tests/fixtures/p43_spike/synthetic_blocker/*` · 测试默认 lane 跑通
3. **S3a asserted_pass**：`ai_doc_analyzer.py:838+841` READ side 改 `blocking_reasons` · EMIT key 保 `"blockers"` · `test_p43_doc_analyzer_blocker_fix.py` 4 test 默认 lane pass · Codex Step B review 返"可过"或等价
4. **S3b asserted_pass**（v2 新 · Counter F 完整 closure）：frontend consumer audit · grep `ai-doc-analyzer.js` 存在 `result.blockers` + `result.status === "blocked"` reader · 报告登记前端契约点 · alignment 证实（后端 emit key 未变）
5. **S4 报告**：Playwright `readAsText` 行为实测 · 结论（broken / functional / partial）明确 · 登记 P43-03 必修
6. **S5 asserted_pass**：`docs/P43-api-contract-lock.yaml` 覆盖 `/api/workbench/*` + `/api/p15/*` 全 endpoint · 每 endpoint 含 success/blocked/error 分支 shape
7. **R6/R7/R8 报告**：3 条 inventory 结论 · 后续 phase 修复 checklist
8. **三轨回归**（Codex r2 Fix D · v2 scope creep 诚实化）：vs P42 main baseline (`a6521ca`)：default pytest **800 passed** (P42 baseline 796 + 4 spike default tests) / 1 skipped · e2e **50 passed** (P42 baseline 49 + 1 Playwright readAsText test · opt-in) · adversarial wrapper 1 passed · 无既有 test 回归。**scope 诚实声明**：v1→v2 确实扩了（v1 3 test · v2 4 default + 1 e2e · 共 +5）· v2→v3 承诺不再扩（v3 scope 硬承诺 §0 已明）
9. **`docs/P43-contract-proof-report.md` 完整** + Codex adversarial review 两轮证据（Step B + Step G）入 09C 外部审查简报

## 5. Tier 1 self-audit（v2 · C1-C6 · v1 C1-C3 + v2 C4-C6 Codex r1 响应）

**C1 · Spike 可能揭示 v7 plan 不可执行**
- 风险：S1 真路径 block
- 缓解：Step A asserted_pass Gate · fail 停工升 Kogami · non-goal #16

**C2 · Bug 修引入新回归**
- 风险：READ side 改若不小心破坏 ready path
- 缓解：Step B 4-test + 全量 default lane regression · Codex review

**C3 · Contract lock 遗漏 dynamic endpoint**
- 风险：`demo_server.py` 动态路径漏扫
- 缓解：常量 grep + AST scan method body + 登记

**C4（v2 新 · Codex r1 + r2）· 假 E2E 路径替代真 pdf 路径**
- 风险：v1 用 "手构 intake + CLI bundle" 冒充真 happy path · v2 虽换真入口但仍用 "pdf SHA 引用" 夸成 pdf 被消费（Codex r2 Fix B）· 同类错可能在 S2/S3 重演
- 缓解：v3 §1c honesty boundary 三层明示不等价（pdf binary consumption / full pipeline user flow / function contract proof）· Step A Python 直调 + HTTP POST 双路径对比 · Q1=D 诚实选项承认 full pipeline 超 scope · Codex r2 specifically 记录为 v3 Fix B+E

**C5（v2 新 · Codex r1）· 前端 blocked-reader stale**
- 风险：Step B 后端改 · 前端 `ai-doc-analyzer.js:527-528` 未 audit · Counter F closure 不完整
- 缓解：S3b 增 · Step B test 4 审计前端 consumer · 后端 emit key 保留 `"blockers"` 保外部契约 · 前端 zero change · alignment 证实

**C6（v2 新 · Codex r1）· fixtures / tests 白名单歧义**
- 风险：`tests/fixtures/p43_spike/*` 非 v7 §3d 显式列 · 可能被解释为 violation
- 缓解：v2 §2c 表格明记 "Executor 显式判"（v7 Test Whitelist note "其他 tests/ 文件新建需 Executor 判是否合理"） · 签名 + 证据入 commit metadata · Codex Step G review 可复审

## 6. Open Questions · Kogami 签 GATE-P43-01-PLAN (v2) 时仲裁

### Q1（v2→v3 再修正 · Codex r2 Fix E · full-pipeline gap 诚实承认）· S1 driving method
- A · Python 直调 `ai_doc_analyzer.run_pipeline_from_intake(intake_dict)`（覆盖函数层 contract · 快）
- B · HTTP `POST /api/p15/run-pipeline` + requests 库（覆盖 server + handler 路径）
- C · Playwright 模拟 ai-doc-analyzer UI 全链路（最真 · 最重 · 依赖 S4 readAsText 可能 broken · 归 opt-in e2e）
- **D · 诚实承认（Codex r2 Fix E · v3 新）**：A+B combined 只覆盖 function + HTTP handler layer · **不覆盖** frontend `p15RunPipeline()` wrapper / `FileReader.readAsText()` / `session_id → _inject_clarification_answers()` 分支 · **full user flow pipeline proof 超 P43-01 scope** · 归 P43-03+（P43-03 doc pipeline 含 PDF/docx 服务端抽取 + clarification 契约修 · Q12=B+a）。P43-01 仅证 **function layer contract** · 不假装 full pipeline
- **Executor 建议：A + B + D**（A+B 并跑互证 function/HTTP layer · D 明示边界 · C 不选因 readAsText broken 且 e2e lane 不稳）

### Q2 · S3 Codex review 时机
- A · Step B commit 后即刻（推荐 · v1 同）
- B · Step G finalize 一次性
- **Executor 建议：A**

### Q3 · Spike 报告格式
- A · 纯 markdown
- **B · markdown + yaml contract-lock（推荐 · v1 同 · 下游机器可 consume）**
- **Executor 建议：B**

### Q4（v2 修正 · Codex r1 methodology 错）· S4 dump 方法
- A · Playwright headless chromium（**推荐 · 仓库已有先例 · 真 browser FileReader API**）
- B · Node subprocess 模拟（**Codex r1 指正非等价 · 拒绝**）
- C · 仅 grep 源码推断（弱证据 · 拒绝）
- **Executor 建议：A**

### Q5（v2 修正 · Codex r1 超范规则删）· Additional findings 处理
- A · 全部登记报告 + 全部推给 P43-02+ plan
- **B · 登记报告 · 分类 critical/warning/nit · critical 上升 Kogami 判（推荐 · 但不预设 "> 3 触发 v8" 治理规则 · 由 Kogami R4 权威仲裁）**
- **Executor 建议：B**（"> 3 触发" 规则 v1 私设 · v2 删）

## 7. 停点

**~~停点 1~~** (已 passed · v2 Codex r2 review 完成)
**~~停点 2~~** (已 passed · v2 Codex r2 返 需修正·信号强 · v3 执行 final surgical per §7 v2 版本规则)
**停点 1 (v3)**: v3 commit + branch push 后 · Executor 调 Codex round-3 re-review
**停点 2 (v3 · final)**: Codex r3 返 "可过 Gate" → 提交 Kogami 审 GATE-P43-01-PLAN (v3) · **否则硬停 path ①** · 升 Kogami R4 决策（候选：B freeze v3 + accept residual · C R4 撤回 P43-01 重分解 · 不再自动 iterate v4）
**停点 2a**：Step A (S1) asserted_pass fail → stop · 升 Kogami · non-goal #16 自动冻结 P43-02+
**停点 2b**：Step B (S3) Codex review 返"需修正" → path ① 到 P43-01 v3
**停点 3**：Step G 报告 finalize + Codex r3 review（报告本体）· 提交 GATE-P43-01-CLOSURE
**停点 4**：Kogami 基于报告 decide P43-02+ scope

## 8. Codex review plan

**Pre-Gate（v2 本 plan · 第二轮）**：审 4 fix 是否闭（S1 真路径 / blocked contract 真 shape / JS consumer alignment / fixtures 白名单解释）+ C4-C6 counter 是否真缓解 + scope 真无扩

**Step B 后第一轮实装**（必调 · v5.3 adapter-boundary）：审 `ai_doc_analyzer.py:838+841` 修 + 4 test 闭环 + grep 全仓无 stale reader

**Step G 后第二轮实装**（必调 · Q7=A milestone-wide）：审 `docs/P43-contract-proof-report.md` 真 asserted_pass · Exit Criteria 9 条机械验证 · P43-02+ scope 建议 evidence-based

---

**Signed:** Claude App Opus 4.7 (Solo Executor) · v5.2 solo-signed + v5.3 addendum · 2026-04-20
**Revision:** v3 (post-Codex-round-2 · path ① final surgical · 5-fix · §7 停点 2 final round before Kogami R4 escalation)
**Awaiting:** Codex round-3 re-review (pre-Gate) + `GATE-P43-01-PLAN (v3): Approved` (Kogami) + Q1-Q5 仲裁
**Parent:** P43-00-PLAN.md v7 (GATE-P43-PLAN (v7) Approved · main `b2f8692`)
**Verified-by:** codex-gpt54-xhigh (r1 需阻止 · r2 需修正·信号强 · v3 5 surgical fixes · if r3 still not-pass · hard stop per §7)
