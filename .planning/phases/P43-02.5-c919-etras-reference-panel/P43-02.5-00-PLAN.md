---
phase: P43-02.5
plan: P43-02.5-00
plan_revision: v4 (post Codex r3 · 需修正·信号强 closed · R2 timer direction + Exit #25 prereqs + E2E whitelist + operate routing fix)
title: P43-02.5 — C919 E-TRAS Reference Panel (hand-crafted frozen-spec rendering)
status: DRAFT · awaiting Codex r4 adversarial review + Kogami P43-00 v9 amend (test + E2E + optional docs whitelist · **3 delta batched**)
date: 2026-04-21
owner: Claude App Opus 4.7 (Solo Executor · v5.2 solo-signed + v5.3 addendum)
scope_tier: Tier 1 · frontend adapter-boundary write (chat.html / chat.js / chat.css) + backend already landed at HEAD (44967f1)
verified-by: pending Codex r2 post-plan adversarial review (Q7=A · per-touchpoint)
classification: **Decimal-inserted phase** between P43-02 Batch (execution authorized · not started) and P43-05 Progressive panel preview (Batch-3 · not planned). Does NOT amend P43-00 v8 plan · does NOT block Batch-2 · does NOT touch §3e R1-R6 authority contract surface.
governance_framing: Hand-crafted C919 reference panel is a **rendering of the frozen adapter+YAML truth** (not a second truth). Whether P43-05 later adopts it as a validation target is a **P43-05-side decision**; this plan only commits to producing a carry-forward artifact recommendation, not to pre-locking any P43-05 Exit Criteria.
preconditions:
  - `GATE-P43-PLAN (v8): Approved` (Kogami 2026-04-20 → 2026-04-21 amended · §3d +8 entry)
  - **Kogami P43-00 v9 amend (pending · triggered by this plan · v4 expanded to 3 delta)**: Test Whitelist +2 (`tests/test_p43_02_5_c919_panel_schema_alignment.py` · schema static · **Delta 1** + `tests/test_p43_02_5_c919_panel_e2e_deploy_flow.py` · `@pytest.mark.e2e` runtime flow · **Delta 3 · v4 Codex r3 R3 新增**) · optional Docs Whitelist +2 (phase-local `reference-panel.svg` + `reference-panel-topology.json` · **Delta 2 optional**) — **3 delta batched in one v9 amend request** (见 §Q Q1 revised + §Q Q4 revised · amend doc at `.planning/phases/P43-02.5-c919-etras-reference-panel/P43-00-v9-amend-request.md`)
  - `GATE-P43-01-CLOSURE: Approved` (Kogami 2026-04-21 · main `61b12b3`)
  - `GATE-P43-02-BATCH-PLAN-QUALITY: Approved` (Kogami 2026-04-21 · Option A)
  - P43-02 Batch execution NOT started (runway clear · no execution-plan collision)
  - **Step A backend wiring already landed at HEAD `44967f1`** (SYSTEM_REGISTRY["c919-etras"] + lru_cache(5) + `_default_snapshot_for_system` c919 branch with 34-field default). §3 below only audits, does not re-implement.
  - C919 E-TRAS adapter 1446 LOC complete (`adapters.c919_etras_adapter.C919_ETRAS_CONTROLLER_METADATA` · truth_level=certified · status=In use)
  - C919 hardware YAML 329 lines (`config/hardware/c919_etras_hardware_v1.yaml` · SHA256-locked · PDF §1.1.x traceable)
  - User directive 2026-04-21 14:05 (approximate): "需要你完成C919 E-TRAS的控制逻辑面板的完整开发，深度规划开发计划，然后开始执行"
non-goals:
  - 不改 P43-00 v8 plan · 不改 P43-01 v5 plan · 不改 P43-02 v3.1 plan
  - 不影响 P43-02 Batch 执行（Step 3a/A 仍可随时开工 · 本 phase 与 Batch-2 可并行 · 同一 branch）
  - 不改 c919-etras adapter 源码（1446 LOC 已 frozen-spec-aligned · panel 是渲染层 · 非 truth 层）
  - 不新建 plant 仿真代码（adapter evaluate_snapshot 已是 behavioral truth）
  - 不改 thrust-reverser / landing-gear / bleed-air / efds 任一既有 panel（regression isolated · 三 demonstrative 仍 UI-freeze）
  - 不 bump JSON schema $id（P42 已锁 · schema 不动）
  - 不新建 `workbench/orchestrator.py` / `draft_truth.py` 等 §3d blacklist 禁项
  - 不把本 phase 当作 P43-05 的等价替代 · P43-05 仍需独立 plan + Kogami gate · 本 phase 仅留 carry-forward artifact 建议
  - 不预锁 P43-05 Exit Criteria（v1 的"Nyquist validation target + diff-match Exit"措辞 retracted · P43-05 自主起草）
  - 不删 placeholder 代码路径 · 直接替换同一 div id `chain-topology-c919-etras` 内部 DOM
  - 不引入 SPA 框架（vanilla JS 继承 · Q2=A 硬规则）
  - 不改 §3e R1-R6 mechanical verification 规则（validator 唯一在 workbench.js 原则保留）
  - 不复刻 adapter 的 `_select_mlg_wow` / `_tls_unlocked_confirmed` 等 Python 判据到 JS（所有判据仍由 server-side evaluate_snapshot 产出 · 前端仅读 truth_evaluation）
---

# P43-02.5 · C919 E-TRAS Reference Panel — v4 (post Codex r3)

## §0. TL;DR (v4 delta from v3 · narrow tactical fixes)

Codex r3 verdict 仍 `需修正·信号强`，但 finding 已缩窄到 **4 required + 3 polish**（其中 3 项 previously-required 已 confirmed closed: R3 Q1=A prefix · §2a real IDs · Q5 grep 足够）· v4 surgical fix:

| v3 bug | v4 修正 |
|--------|---------|
| **R1 · Reset timer 方向反了** (§2c 19 · 原方案 `tr_stowed_locked_confirm_s=threshold` 会 reset eicu_cmd3 · adapter:1285-1289) | §2c row 19 flip: **"Advance deploy latches"** button · atomic set `lock_unlock_confirm_s=0.4` + `tr_position_deployed_confirm_s=0.5` + `comm2_timer_s=nominal` + **`tr_stowed_locked_confirm_s=0.0`**（keep CMD3 latch）· 新增 row 19-alt "Stow latches" 双向测试 |
| **R2 · Exit #25 脚本缺前段条件** (default snapshot tra=0 + atltla/apwtla=false · 跑不到 fadec_deploy_command) | Exit #25 完整 rewrite: 先设 TRA=-14 + atltla=true + apwtla=true (+ 核心 12 controls 默认值确认) · 再设 unlock/deploy 7 新控件 · 最后点 v4 修正的 "Advance deploy latches" button · 断言全 4 ln_* active + fadec_deploy_command=true + completion_reached=true |
| **R3 · schema-alignment test 不覆盖 flow** (只断言 data-node ⊂ spec ids · 静态) | **v9 amend +Delta 3**: `tests/test_p43_02_5_c919_panel_e2e_deploy_flow.py` (`@pytest.mark.e2e`) · 脚本化 Exit #25 · 补 DOM/event/POST flow 覆盖 · 沿用既有 E2E lane (no new CI · no Playwright stack) |
| **R4 · operate intent 路由 false-positive** (Exit #20 "帮我..." 命中 isGeneralQuestion 的 `帮我` pattern · 跑 handleGeneralQuestion 而非 handleOperateIntent) | Exit #20 phrasing 改 **"把 TRA 调到 -14°"** (无 "帮我" prefix) · 新增 Exit #20a 用 "帮我..." verify general path snapshot 一致 · 覆盖两种路由 |
| P1 · §1a Must-land #5 stale count (12 可见/22 只读) | Fix to "19 可见 + 15 只读" (v3 Q6=B pivot 一致) |
| P2 · §6 Gate strategy stale (27 Exit + 5 rounds) | Fix to "34 Exit + 6 rounds" + 加 v9 amend approval 依赖 |
| P3 · `spec.component_nodes` phantom language | 清为 `spec.components + spec.logic_nodes`（backend 实际 authoritative at demo_server.py:3345）|

**Scope unchanged from v3**: 8 chat.js touchpoint + backend 2 endpoint + 19 visible / 15 readonly controls + SVG · **Codex budget unchanged: 6 rounds** · wall-time unchanged: 12h。v4 仅仅 plan 文字修正 + v9 amend 从 2 delta 扩到 3 delta · 不扩 code scope。

## §0b. TL;DR (v3 delta from v2 · kept for audit)

用户 2026-04-21 直接指令：完成 C919 E-TRAS 控制逻辑面板的完整开发，**不等 P43-05 AI 自动生成**。v2 plan 在 Codex r2 获 `需修正·信号强`（v1 同结论），v3 做 4 项 required surgical fix + 6 项 polish。**关键扩张**：v2 只收 4 个 chat.js touchpoint，但 operate/reason 还有第 5 组硬耦合（5 subtouchpoint）必须并入 scope；v2 Q6=A 把 22 字段全只读 会锁死 unlock/deploy 演示链，必须 pivot 到 Q6=B (~19 可见 + 15 只读)。

| v2 错位 | v3 修正 |
|--------|---------|
| **R1** · §1a Must-land #3 只收 4 touchpoint · 漏 operate/reason 支路整条链（handleOperateIntent / lastTruthPayloadBySystem / _applySuggestedOverrides / backend ALLOWED_SYSTEM_IDS / operate system_prompt） | §1a Must-land #3 扩到 **8 touchpoint**（T1-T4 保留 + 新增 T5-T8）· backend `ALLOWED_SYSTEM_IDS` (demo_server.py:1028) c919-enable · operate system_prompt **选项 D4=C**（allowlist + stub neutral message "operate for c919-etras 当前为 panel-only 交互模式 · 请直接在面板调节"）· 真 operate prompt 推 P43-05 |
| **R2** · Q6=A (22 只读) 锁死 unlock/deploy 演示 · tls_ls_* / pls_ls_* / pylon_ls_* / tr_position_percent / trcu_power_on 是 deploy gating 门控不是 annotation (c919_etras_adapter.py:1305-1325) | **Q6 pivot A→B**: 可见控件从 12 扩到 **~19**（+ 2 TLS_unlock + 3 PLS/pylon unlock + tr_position_percent slider + trcu_power_on）· 剩 ~15 只读（其中 4 timer 字段 server-computed · 非 UI input）· panel 能真实演示 unlock→deploy 完整链 |
| **R3** · Q1=B 治理说法错 · "adapter whitelist 已含 tests/test_c919_etras_adapter.py" 系误述（父计划 P43-00:297-315 未授权此条） | **Q1 pivot B→A**: 新测试文件 `tests/test_p43_02_5_c919_panel_schema_alignment.py`（严格 `test_p43_*` 父计划 prefix 约定）· 触发 **Kogami P43-00 v9 amend**（test + 可选 docs whitelist 一并 amend · D5=A 批量） |
| **R4** · §2a / Exit #19 残留 phantom 词表 · TR_COMMAND3 / FADEC_DEPLOY_CMD1 / ETRAS_540V / TLS_115V / PLS / Pylon | §2a Col 3/5 改为真 spec id (`eicu_cmd2 / eicu_cmd3 / tr_command3_enable / fadec_deploy_command / fadec_stow_command`) · Exit #19 用 real id 验证 |
| P1 · §2b 主渲染路径引用不准 | §2b 改引 `applySystemSnapshotToCanvas() → renderChainMap(... truth_evaluation.asserted_component_values)` (chat.js:1649-1664) · `extractNodeStates` 降级为辅助 |
| P2 · Step A 少 POST smoke | Exit #4a 新增 POST /api/system-snapshot smoke |
| P3 · C2 docs whitelist 措辞过头 | 删 "docs whitelist per-phase 已涵盖" · 改为依赖 Kogami amend 显式列举 |
| P4 · C11 只 scope marker 不够（仓库有 causal-arrow global def）| 所有 c919 panel 内 `<defs>` 子元素 id (marker/filter/clipPath/symbol/linearGradient) 一律 `c919-` 前缀 |
| P5 · Q5 自撞 (T4 "仍必须标 true" vs Q5-B "return false") | **Q5 unify A**: `canvasAdjustmentsSupported()` c919 return false · `#canvas-global-controls` hidden · panel 内控件是唯一交互入口 |
| P6 · 150ms debounce 无 repo 先例 | §2c 加免责 "新 tuning · Step D 实测可调到 Q2-B 300ms if POST 风暴" |

**Scope**: 8 chat.js touchpoint（T1-T8 全 in-scope）+ backend ALLOWED_SYSTEM_IDS c919-enable + operate stub · 完整 SVG signal-flow (22 truth-tracked + 8-10 annotation) · ~19 可见控件 + ~15 只读 annotation/tooltip + state rendering（asserted_component_values 主驱动）+ hardware tooltip + freeze banner · **6 steps** · ~12h wall-time · **6 Codex 轮** (Step A audit 不计 · Step D split 为 D1/D2)。

## §1. Scope (in / out)

### §1a · Must-land deliverables

1. **Backend SYSTEM_REGISTRY wiring** — **audit-only**（HEAD `44967f1` 已落地）:
   - `SYSTEM_REGISTRY["c919-etras"] = build_c919_etras_controller_adapter` ✓
   - `@lru_cache(maxsize=5)` ✓（4→5 bump 已生效 · 覆盖 4 adapter + c919）
   - `_default_snapshot_for_system("c919-etras")` 34-field nominal pre-deploy dict ✓
   - Step A 任务降级为：三路 smoke（snapshot GET / schema GET / 4 既有 adapter 回归）+ 记录 audit 证据 · 无写操作
2. **完整 SVG signal-flow 面板** (chat.html L1 additive · 替换当前 placeholder 内容 · 保留 div id `chain-topology-c919-etras`)
   - **6-column layout**: sensors · LGCU 余度 · Command chain · Timing/Safety guards · Actuators · Feedback/state
   - **~22 truth-tracked nodes** (1:1 匹配 adapter `spec.components` (17) + `spec.logic_nodes` (5) 合法 id 集合 · 由 §2d mapping 表规定 · backend authoritative: `demo_server.py:3345` · v4 Codex r3 P3 修正 phantom `component_nodes`) + **~8-10 annotation nodes** (LGCU 双路可视化 · timing countdown · safety 限幅 · 不参与 state driving · 仅装饰)
   - **节点 class 复用**: sensor-node-svg / logic-gate-svg / actuator-node-svg / deploy-indicator
   - **新增 2 class**（§3d chat.css L1 additive）:
     - `.lgcu-selector-svg` — 双框 concentric 表示余度（annotation only）
     - `.timing-guard-svg` — 六边形 with inline countdown text（annotation only）
   - **SVG marker scoping**（见 §5 C11）: c919 panel 内部 marker id 一律加前缀 `c919-` (`c919-arrow-green` 等) · 不复用现有 `arrow-green/cyan/gray` 全局 id
3. **chat.js + backend 系统耦合消除** (L1 additive · **8 touchpoint, 全部 in-scope**) — **本 phase scope 的真正缺口** (Codex r1 R3 + r2 R1):
   - **T1** (chat.js:52) · `SYSTEM_LABELS` 加 `'c919-etras': 'C919 反推系统（E-TRAS）'`
   - **T2** (chat.js:291 · `buildDefaultLeverPayload`) · non-thrust 系统不应强行拼 thrust-reverser 字段 · 若 `systemId === 'c919-etras'`，return `{prompt, system_id}` + "以当前 panel 交互状态为准" 的 hint，由 T3 补齐
   - **T3** (chat.js:2871 · `handleGeneralQuestion` non-thrust 分支) · c919 分支改为：**优先读当前 panel interaction state**（新建 `collectC919PanelSnapshot()` 读 DOM input 当前值 → POST /api/system-snapshot body）· 这样聊天/解释链路与 panel 看到的是同一份 snapshot
   - **T4** (chat.js:2356 · `canvasAdjustmentsSupported`) · **Q5=A 统一方案**: c919 下 **return false** · `#canvas-global-controls` `display:none` via CSS or JS · panel 内控件是 c919 唯一交互入口（消除 v2 内撞）
   - **T5** (chat.js:396-421 · `_applySuggestedOverrides`) · 当前 thrust-only 路径 · c919 分支: no-op + console.debug("c919-etras: apply via panel controls instead") · 不 mutate thrust DOM（Exit #30 断言无 console.error）
   - **T6** (chat.js:2962-2979 · `handleOperateIntent`) · 当前读 `lastTruthPayloadBySystem[systemId] || null` · c919 分支改为读 `collectC919PanelSnapshot()` · 并在成功 POST 后把 snapshot 写入 `lastTruthPayloadBySystem['c919-etras']`（让后续 operate/explain 调用有 cache）
   - **T7** (demo_server.py:1028 · `_validate_chat_payload` ALLOWED_SYSTEM_IDS) · 加入 `"c919-etras"` · 同时守 `/api/chat/reason` 和 `/api/chat/operate` 两条 endpoint 的 400 invalid_system_id 不再误伤 c919
   - **T8** (demo_server.py:1117-1253 · operate `system_prompt` 构造) · **D4=C 选项**: 在 system_prompt 构造函数入口加 c919 branch · 返 stub neutral prompt：
     ```
     你是控制逻辑操作助手。用户问 C919 E-TRAS 反推系统。
     本 phase 的 operate 模式是 panel-only 交互。
     请建议用户直接在面板调节对应控件。
     返回 action_type=manual_steps · reasoning 描述需要的 panel 操作 · parameter_overrides 空 dict · auto_apply=false
     ```
     — 真 operate prompt (含 5 logic node + 17 component vocab) 推到 P43-05 plan 自主起草 · 本 phase 不扩 LLM prompt quality 审查
4. **State rendering** (chat.js L1 additive · c919-specific dispatcher)
   - **主驱动**: `truth_evaluation.asserted_component_values`（与现有 `extractNodeStates` / `applySystemSnapshotToCanvas` 保持一致，不新建并行路径）· truth-tracked 节点按 dict value boolean/numeric 上色
   - **辅助**: `truth_evaluation.active_logic_node_ids` 在 logic-gate 节点（OR/AND）上高亮
   - **annotation 节点** 不由 state driving · timing 六边形 countdown text 读 snapshot 原始 timer 字段 · 与 truth-tracked 通道解耦
   - 连线动态: 绿线 (active) · 红线 (blocked by 下游 asserted=false) · 灰线 (inactive)
5. **交互控件** (chat.html + chat.js L1 additive) — 见 §2c 34-field 完整表 · **可见子集 19 个**（12 核心 + 7 unlock/deploy gating 扩展 · v3 Q6=B pivot）· 其余 **~15 个**只读或隐藏（4 timer server-computed + 冗余 LS_valid + vdt/prev_eicu_cmd3）
6. **Hardware tooltip overlay** (chat.html + chat.css L1) — 鼠标悬停 truth-tracked 节点 · 显示 YAML `min/max/unit/note` + PDF 段引用
7. **Freeze banner integration** (chat.html L1) — 顶部 badge `certified · In use · P43-02.5 reference panel` (no P43-05 pre-lock 措辞)
8. **Regression**: 不破 thrust-reverser / landing-gear / bleed-air / efds 任一 panel · 不破 chat.js 既有 currentSystem switching logic · 不破 8 endpoints API contract lock · 不破 P43-01 contract-proof spike
9. **Carry-forward artifacts to phase folder** (§3d doc whitelist · per-phase):
   - `.planning/phases/P43-02.5-c919-etras-reference-panel/reference-panel.svg` — 最终 SVG 独立保存
   - `.planning/phases/P43-02.5-c919-etras-reference-panel/reference-panel-topology.json` — normalized: `{nodes: [{id, kind, col, x, y}], edges: [{from, to}]}`（不含样式 · 不含 truth state · 只拓扑）
   - **作用**: 为 P43-05 plan 起草时**可选参考**（P43-05 自主决定是否将其引入 Exit Criteria · 本 phase 不预锁）

### §1b · Non-goals（scope discipline · 与 P43-02 Batch / P43-05 的 boundary）

- **不改 c919-etras adapter 源码**：adapter 是 behavioral truth · panel 是渲染层 · 严禁 panel JS "re-implement" adapter logic
- **不建新 Python module**：backend 已 audit-only · 不新建 `c919_etras_ui_helpers.py` / `panel_generator.py`
- **不把 truth-tracked 和 annotation 节点混淆**：
  - truth-tracked = id ∈ adapter `spec.components` (17) ∪ `spec.logic_nodes` (5) id 列表（backend authoritative: demo_server.py:3345 · v4 Codex r3 P3 修正 phantom `component_nodes` 词）· 由 `truth_evaluation.asserted_component_values` / `active_logic_node_ids` 驱动 · 参与 schema-alignment 断言
  - annotation = LGCU 双框可视化 / timing 六边形 countdown / safety 菱形 · 不在 spec id 列表 · 不参与 state driving · 不参与 schema-alignment 断言
- **不当 P43-05 替代品 · 不预锁 P43-05 Exit Criteria**：carry-forward artifact 只是建议 · P43-05 plan 起草时自主决定是否采纳
- **不扩 _SUPPORTED_FOR_ANALYSIS**：诊断 / Monte Carlo / sensitivity 对 c919 仍不支持（不在 scope）
- **不手画 PDF 表2 的 LGCU 余度选择规则**：panel 只**显示** `_select_mlg_wow` 输出 · 不在 SVG 里复述选择算法
- **不 bump chat.js 为 TypeScript / ES modules**: vanilla JS 继承

## §2. Architecture decisions

### §2a · SVG 布局策略

6-column grid · viewBox **1100 × 640**（比 thrust-reverser 的 900×480 大约 60% · 因节点数翻倍）· 节点复用 chat.css 现有 class + 2 新 annotation class

```
Col 1 (x=30-160)   : Sensor components (truth-tracked · 12 ids): tra_deg / atltla / apwtla / engine_running / n1k_percent / tr_inhibited / mlg_wow / tr_wow / trcu_power_on / e_tras_over_temp_fault / tr_position_percent / lock_state
Col 2 (x=200-320)  : LGCU 余度 annotation (lgcu1/lgcu2 value+valid 4 字段 · 双框 concentric · mlg_wow 作为 col 1 已有 truth-tracked sensor · 箭头汇流到 col 1 mlg_wow)
Col 3 (x=360-500)  : Logic node chain (truth-tracked · 5 ln_* ids): ln_eicu_cmd2 → ln_eicu_cmd3 (S-R latch) → ln_tr_command3_enable → ln_fadec_deploy_command (+ ln_fadec_stow_command 并联)
Col 4 (x=540-680)  : Timing / safety annotation (non-truth-tracked · ~6 hex w/ countdown): comm2_timer_s / lock_unlock_confirm_s / tr_position_deployed_confirm_s / tr_stowed_locked_confirm_s + 2-3 safety diamond (over-temp / vdt / trcu power)
Col 5 (x=720-840)  : Command-result components (truth-tracked · 5 command ids from spec): eicu_cmd2 / eicu_cmd3 / tr_command3_enable / fadec_deploy_command / fadec_stow_command
Col 6 (x=880-1070) : Aggregate state + feedback (读 asserted_component_values): lock_state badge · tr_position_percent bar · completion_reached badge
```

### §2b · 状态模型（frontend-only · server evaluates）

**单一 truth 源**: server `evaluate_snapshot` 返回 `truth_evaluation`。前端驱动表（**v3 P1 修正 · 精确引用主路径**）:

| SVG element | 驱动字段 | 备注 |
|------------|---------|------|
| truth-tracked component node color | `truth_evaluation.asserted_component_values[component_id]` (bool / str / float) | **主路径** · `applySystemSnapshotToCanvas()` → `renderChainMap(referenceTopologyNodes, truth_evaluation.asserted_component_values)` (chat.js:1649-1664) |
| truth-tracked logic node highlight | `truth_evaluation.active_logic_node_ids` (tuple of 5 ln_* ids) | **辅助高亮** · logic-gate 节点（ln_eicu_cmd2 / ln_eicu_cmd3 / ln_tr_command3_enable / ln_fadec_deploy_command / ln_fadec_stow_command）· `extractNodeStates()` (chat.js:1594-1647) 是 helper 不是主颜色驱动 |
| connection line color | 由下游节点 asserted state 推断（既有 `deriveConnectionState` 模式） | 复用 · 不新建 |
| annotation timing countdown text | snapshot 原始字段 `*_confirm_s` / `comm2_timer_s` 直接 display | 装饰用 · 不参与 truth · **server-computed · 非 UI input** |
| annotation LGCU selector 框 | `lgcu1_*` / `lgcu2_*` 4 字段（在 §2c 已列为可见 toggle）+ `_select_mlg_wow` 输出（display only） | LGCU 输入是可见控件 · 框本身 annotation · 不复刻选择逻辑 |

**node-id mapping 表** (§3 Step C 实作时创建): `C919_SVG_NODE_MAP = {svg_element_id → spec_component_or_logic_id}` · ~22 entry · 1:1 匹配 adapter spec。annotation 节点**不在** mapping 表（保证两类分离）。

**关键变化 vs v1**: v1 写 "至少 3 个绿色节点 at default snapshot" 被删。默认 snapshot (tra_deg=0 · engine_running=true · on-ground · not inhibited) 下 adapter 的实际 `asserted_component_values` 分布由 Step A smoke 实测决定 · Exit criteria 改为 "≥1 truth-tracked node 有 non-null asserted value" + "≥1 boolean asserted_component_value 与 default snapshot 语义一致 (e.g., tr_position_percent=0 → lock_state=LOCKED 可断言)"。

### §2c · 34-field 交互 → 可见控件 mapping（v3 R2 pivot · Q6=B · ~19 可见 + ~15 只读）

C919 adapter 默认 snapshot **34 字段**（来自 `demo_server.py:3290-3340` c919 branch · v3 P2 更正真实行号范围）全部由 panel 代表。**v3 Q6=B pivot**: 可见控件从 v2 的 12 扩到 **19**，覆盖 unlock/deploy gating（否则 panel 无法演示完整部署链 · Codex r2 R2）:

| # | 可见 Control | UI 形式 | Range / Values | Snapshot field(s) | 必要性 |
|---|-------------|--------|----------------|-------------------|-------|
| 1 | TRA | slider | -32 .. 0 step 0.1 | `tra_deg` | 核心 (gating) |
| 2 | ATLTLA | toggle | bool | `atltla` | 核心 |
| 3 | APWTLA | toggle | bool | `apwtla` | 核心 |
| 4 | N1K | slider | 0 .. 120 step 1.0 | `n1k_percent` | 核心 (deploy envelope) |
| 5 | engine_running | toggle | bool | `engine_running` | 核心 |
| 6 | tr_inhibited | toggle | bool | `tr_inhibited` | 核心 (blocking) |
| 7 | tr_wow | toggle | bool | `tr_wow` | 核心 (FADEC gate) |
| 8 | LGCU1 value | toggle | bool | `lgcu1_mlg_wow_value` | 核心 |
| 9 | LGCU1 valid | toggle | bool | `lgcu1_mlg_wow_valid` | 核心 |
| 10 | LGCU2 value | toggle | bool | `lgcu2_mlg_wow_value` | 核心 |
| 11 | LGCU2 valid | toggle | bool | `lgcu2_mlg_wow_valid` | 核心 |
| 12 | e_tras_over_temp_fault | toggle | bool | `e_tras_over_temp_fault` | 核心 (safety) |
| **13 (v3 新)** | TLS A unlock | toggle | bool (valid AND unlocked 组合) | `tls_ls_a_valid=true` + `tls_ls_a_unlocked` 联动 | **unlock 演示** (adapter:1305-1313) |
| **14 (v3 新)** | TLS B unlock | toggle | bool | `tls_ls_b_valid=true` + `tls_ls_b_unlocked` | **unlock 演示** |
| **15 (v3 新)** | Pylon unlock (L+R aggregate) | toggle | bool (sets left+right pylon valid+unlocked 对称) | `left_pylon_ls_a/b_*` + `right_pylon_ls_a/b_*` (8 字段 composite 单一 toggle) | **unlock 演示** |
| **16 (v3 新)** | PLS locked | toggle | bool (sets both pls_ls_a/b_locked) | `pls_ls_a_locked` + `pls_ls_b_locked` | **unlock→deploy 切换** |
| **17 (v3 新)** | TR position | slider | 0 .. 100 step 1 | `tr_position_percent` | **deploy 演示** (adapter:1306-1309) |
| **18 (v3 新)** | TRCU power | toggle | bool | `trcu_power_on` | **deploy gating** |
| **19 (v4 修正 · Codex r3 R1)** | **Advance deploy latches** (保 CMD3 latched) | button | click → atomic set 以下 3 个 timer 到 confirm 阈值 ≥，并**清零 tr_stowed_locked_confirm_s**（否则 adapter:1285-1289 `>= TR_STOWED_LOCKED_CONFIRM_S` 会 reset `eicu_cmd3` · 整条链断） | `lock_unlock_confirm_s := LOCK_CONFIRMATION_400MS_S` (0.4) · `tr_position_deployed_confirm_s := CONFIRMATION_0_5_S` (0.5) · `comm2_timer_s := <nominal advance>` · **`tr_stowed_locked_confirm_s := 0.0`** | 让 `ln_tr_command3_enable` / `ln_fadec_deploy_command` 能在 adapter:1297-1325 gating 下被激活 · **按钮命名故意避免 "Reset" 避免暗示全清零**（v3 R2 misdirection 修复）|
| **19-alt (v4 新增 · 双向测试用)** | **Stow latches** (触发 CMD3 reset) | button | click → `tr_stowed_locked_confirm_s := TR_STOWED_LOCKED_CONFIRM_S` (1.0) | 演示 stowed 态 → `eicu_cmd3` reset 到 False (adapter:1284-1289)· 保留 reverse 路径测试覆盖 |

**~15 只读字段**（SVG annotation / tooltip 只显示 · panel 不提供直接 UI 控件 · 保 scope）:
- `tls_ls_a_valid` / `tls_ls_b_valid` (bool · 通常恒 true · annotation)
- `left_pylon_ls_a_valid` / `left_pylon_ls_b_valid` / `right_pylon_ls_a_valid` / `right_pylon_ls_b_valid` (bool · annotation)
- `vdt_sensor_valid` (bool · annotation · 非 deploy gating critical path)
- `prev_eicu_cmd3` (bool · S-R latch state · server-driven · 前端只读)
- `comm2_timer_s` / `tr_position_deployed_confirm_s` / `tr_stowed_locked_confirm_s` / `lock_unlock_confirm_s` (float · server-computed countdown · annotation hex countdown text)

**P6 修正 · debounce tuning disclaimer**: 无 repo 内 debounce 先例（thrust 路径 global controls 是 `change` 直接提交 · chat.js:2427-2436 · 无 debounce）。v3 选 **Q2=A 150ms** 是新 tuning · Step D2 实测若 POST 风暴明显 → 调到 Q2=B 300ms · 实测方法：DevTools Network tab 观察拖 slider 时 POST 请求数 / 秒 · 若 ≥ 10 req/s 调粗。

Event model: `oninput` / `onchange` → debounce **150ms** → `collectC919PanelSnapshot()` → POST `/api/system-snapshot` body `{system_id, snapshot: {...34 fields...}}` → dispatcher re-render。

### §2d · Freeze 关系 + drift guard

C919 adapter + hardware YAML 是 frozen source (P34 + P38 · SHA-locked)。SVG 是它们的 view。

**Drift vectors & mitigation**:
| Drift vector | 检测 | Mitigation |
|-------------|-----|-----------|
| YAML 新字段 / 改单位 | Step E regression test (见 §Q Q1 重写) | assert `truth-tracked SVG data-node values ⊂ spec component_id union logic_node_id` |
| spec 新 logic node | Step E assertion 同上 | Alert → plan delta |
| SVG `c919-arrow-*` marker id 全局污染 | Step B Exit #6 browser check | marker 一律加 `c919-` 前缀 · c919 panel 自包含 |
| browser cache 看旧 SVG | Step E Exit #20 | chat.html 引用 `chat.css?v=...` / `chat.js?v=...` 增加 version query（若现有实现缺这个则 Step E 加） |

## §3. Execution plan (6 steps · 6 Codex rounds · v3 D6=b split D→D1/D2)

| Step | Scope | Codex required | Estimated effort |
|------|-------|----------------|------------------|
| **A** | **Audit-only** (HEAD `44967f1` 已落 backend wiring) · 四路 smoke (GET snapshot / GET schema / **POST snapshot** (v3 P2 新增) / 4 adapter 回归) · 记证据 | No (only reads) | 0.5h |
| **B** | SVG 骨架 + 22 truth-tracked + 8-10 annotation nodes · 6-column grid (v3 R4 修正 · 真 spec id 填充 col 3/5) · 连线 · 替换 `#chain-topology-c919-etras` 内部 · 2 new CSS class · **所有 `<defs>` 子元素 `c919-` 前缀** (v3 P4) · node stubs 全 inactive | **Yes · r1** (chat.html primary write · adapter-boundary) | 3h |
| **C** | chat.js c919-specific state dispatcher · `C919_SVG_NODE_MAP` 表（~22 entry · 1:1 匹配 17 component id + 5 logic id）· **主驱动 `applySystemSnapshotToCanvas() → renderChainMap(... asserted_component_values)`** (v3 P1 精确引用) · logic-node 高亮辅助 via `active_logic_node_ids` · annotation 不参与 · `deriveConnectionState` 复用 | **Yes · r2** (chat.js adapter-boundary · state 主路径) | 2h |
| **D1 (v3 拆分)** | **chat.js 8 touchpoint 全消除**: T1 SYSTEM_LABELS · T2 buildDefaultLeverPayload · T3 handleGeneralQuestion · T4 canvasAdjustmentsSupported (Q5=A return false + hide) · **T5 _applySuggestedOverrides c919 no-op** · **T6 handleOperateIntent c919 读 panel state + 写 lastTruthPayloadBySystem** · **T7 backend ALLOWED_SYSTEM_IDS +c919** · **T8 backend operate system_prompt c919 stub** | **Yes · r3** (boundary-critical · chat.js + demo_server · 多 endpoint) | 3h |
| **D2 (v3 拆分)** | §2c **19 可见控件 panel** (12 核心 + 7 unlock/deploy gating 扩展 · Q6=B pivot) + `collectC919PanelSnapshot()` 读取函数 + POST debounce 150ms + reset-timer-latches button | **Yes · r4** (UI interaction + state sync) | 2h |
| **E** | Hardware tooltip overlay (YAML note + PDF ref) · freeze banner `certified · In use · P43-02.5 reference panel` · cache-busting `?v=<git-short-sha>` query · carry-forward artifacts (`reference-panel.svg` + `reference-panel-topology.json` 入 phase 文件夹) · regression test `tests/test_p43_02_5_c919_panel_schema_alignment.py` (v3 Q1=A · **需 Kogami P43-00 v9 amend**) · exit criteria verification | **Yes · r5** (polish + test + artifact) | 2h |
| **Closure** | P43-02.5 closure review · combined sanity | **Yes · r6** (closure · verification lens) | — |

**Total Codex budget**: **6 rounds** (Step A audit-only 不计 · Step D split 为 D1+D2 · v3 D6=b)。每一 round 都是 adapter-boundary / boundary-critical touchpoint · Q7=A 满足。

## §4. Exit Criteria (per step · asserted_pass)

### Step A (Backend audit-only · 0.5h)
1. `grep SYSTEM_REGISTRY.*c919-etras src/well_harness/demo_server.py` 返命中 + `@lru_cache(maxsize=5)` grep 命中
2. `curl /api/system-snapshot?system_id=c919-etras` → 200 · `truth_evaluation` present + `asserted_component_values` 非空 dict (17 keys)
3. `curl /api/hardware/schema?system_id=c919-etras` → 200 · schema 完整
4. 既有 4 adapter (thrust-reverser / landing-gear / bleed-air / efds) 三路 smoke 全 200（efds schema 400 是 pre-existing · 无 hardware YAML）
5. **(v3 P2 新增)** `curl -X POST /api/system-snapshot -H 'Content-Type: application/json' -d '{"system_id":"c919-etras","snapshot":{"tra_deg":-11.74,...}}'` → 200 · `truth_evaluation.active_logic_node_ids` 返非空 list（tra=-11.74 < FWD_IDLE_THRESHOLD · 在具备 mlg_wow + atltla 场景下应激活 eicu_cmd3）· 证明 backend POST 路径可用（demo_server.py:261-344,3447-3462）

### Step B (SVG skeleton · 3h)
6. `#chain-topology-c919-etras` 内部含 `<svg>` root · viewBox 1100×640
7. ≥ 22 truth-tracked `<rect>` / `<polygon>` nodes · 每个 `data-node` 属性值 ∈ spec component_id (17) ∪ logic_node_id (5) · **具体 id 列表**: `mlg_wow / tr_inhibited / tra_deg / atltla / apwtla / tr_position_percent / lock_state / engine_running / n1k_percent / tr_wow / trcu_power_on / e_tras_over_temp_fault / eicu_cmd2 / eicu_cmd3 / tr_command3_enable / fadec_deploy_command / fadec_stow_command / ln_eicu_cmd2 / ln_eicu_cmd3 / ln_tr_command3_enable / ln_fadec_deploy_command / ln_fadec_stow_command`
8. ≥ 8 annotation nodes · 每个 `data-annotation="true"` 属性 · 无 `data-node`
9. ≥ 40 `<line class="conn-line">` · `data-conn-from` + `data-conn-to` 合法
10. **(v3 P4 扩)** c919 panel 内所有 `<defs>` 子元素 id (marker/filter/clipPath/symbol/linearGradient) 一律 `c919-` 前缀 · `grep -E 'id="(arrow|causal|filter|clip|symbol)[^"]*"' #chain-topology-c919-etras` 命中全部 `c919-*` · 无全局名泄漏（比如 `causal-arrow` · chat.js:1314-1328）
11. Browser visual inspection (human-eye): 无 overlapping · arrows 指向正确 · 6 columns 视觉清晰

### Step C (State wiring · 2h)
12. 切到 c919-etras · GET 默认 snapshot · `asserted_component_values` 驱动 ≥ 1 truth-tracked 节点显示 non-inactive（默认 snapshot 的 asserted 有 17 key · 例如 `engine_running=true` → engine_running 节点 active · `tr_inhibited=false` → tr_inhibited 节点 inactive）
13. 语义一致 assertion: 默认 snapshot 的 `lock_state="LOCKED"` (adapter:1261-1262) → col 6 lock_state badge 显示 LOCKED · 若改 `pls_ls_a_locked=false` POST → badge 变 UNKNOWN
14. annotation 节点（timing hex / LGCU 双框 / safety diamond）不依赖 `asserted_component_values` · 独立渲染（验证方法: DevTools console `applySystemSnapshotToCanvas({truth_evaluation:{asserted_component_values:{}}})` · annotation 仍显示）
15. 无 DevTools console errors

### Step D1 (chat.js 8 touchpoint · 3h)
16. **T1**: `SYSTEM_LABELS['c919-etras']` grep 命中 · UI 右上角 system badge 显示 "C919 反推系统（E-TRAS）"
17. **T2 + T3**: 改 TRA slider 到 -11.74° → `collectC919PanelSnapshot()` 产的 snapshot **与** 聊天"为什么现在不能反推？"走的 snapshot 完全一致（DevTools Network panel diff 证据 · POST /api/system-snapshot body 两处 byte-identical）· 聊天链路不再走 GET 默认
18. **T4 (Q5=A)**: c919 下 `#canvas-global-controls` `display:none` · `canvasAdjustmentsSupported()` 返 false · 无 DevTools error · 切回 thrust-reverser 时 global-controls 正常显示
19. **T5**: c919 下触发 `_applySuggestedOverrides({tra_deg:-14})` (DevTools console) · 不 mutate thrust DOM · console.debug "c919-etras: apply via panel controls" 命中 · 无 console.error
20. **T6**: c919 下问 operate 意图问题（**"把 TRA 调到 -14°"** · 避免 "帮我" 前缀 · v4 Codex r3 R4 修正 · 否则命中 chat.js `isGeneralQuestion()` 的 `帮我` pattern 会优先走 handleGeneralQuestion 而非 handleOperateIntent）· network 显示 POST /api/chat/operate body 的 `current_snapshot` 等于当前 panel snapshot（不是 null）· 随后 `lastTruthPayloadBySystem['c919-etras']` 被 set（DevTools console 可验证）· **且可选验证 #20a**: 用 "帮我把 TRA 调到 -14°" 走 handleGeneralQuestion path 同样走 T3 分支 · snapshot 来源一致 · Network tab diff 无分叉
21. **T7**: `curl -X POST /api/chat/reason -d '{"system_id":"c919-etras","question":"test","current_snapshot":{}}'` → 200（非 400 invalid_system_id）· 同 `/api/chat/operate`
22. **T8**: c919 下问 "把 TRA 拉到 -14°" · operate endpoint 返 `action_type=manual_steps` + reasoning 含 "panel" 关键字 · `parameter_overrides={}` · `auto_apply=false`

### Step D2 (19 visible controls · 2h)
23. 19 可见控件全部 functional · 改任意控件 → debounce 150ms POST 一次 · panel 300ms 内 re-render（若实测 POST ≥10 req/s 则 bump 到 300ms per §2c P6 disclaimer）
24. `tr_inhibited=true` → `eicu_cmd2` 节点变 red · `tr_command3_enable` blocked/gray · `fadec_deploy_command` unreachable（基于 adapter:1271-1289）
25. **Unlock→deploy 完整链演示** (v4 Codex r3 R1/R2 修正 · 完整 preconditions 列出): 默认 snapshot 开始 · **先设前段条件**（核心 12 controls）: TRA slider → **-14°** (≤ TRA_REVERSE_IDLE_THRESHOLD_DEG) · ATLTLA toggle → **true** · APWTLA toggle → **true** · engine_running=true (default) · tr_wow=true (default) · tr_inhibited=false (default) · e_tras_over_temp_fault=false (default) · N1K=35% (< MAX_N1K_DEPLOY_LIMIT) · mlg_wow=true (from default LGCU)。**再设后段 unlock/deploy** (7 新控件): tls_a_unlock=true · tls_b_unlock=true · pylon_unlock (composite) = true · pls_locked → **false** · tr_position slider → **90%** (≥ TR_DEPLOYED_POSITION_PERCENT 80%) · trcu_power=true。**最后点 "Advance deploy latches" button** (控件 #19 · v4 修正 · 正确方向): `lock_unlock_confirm_s=0.4` + `tr_position_deployed_confirm_s=0.5` + `comm2_timer_s=<nominal>` + **`tr_stowed_locked_confirm_s=0.0`** (保持 CMD3 latch)。**验证** (POST /api/system-snapshot response): `truth_evaluation.active_logic_node_ids` 包含 `ln_eicu_cmd2` + `ln_eicu_cmd3` + `ln_tr_command3_enable` + `ln_fadec_deploy_command`（4 个全部）· `asserted_component_values.fadec_deploy_command=true` · `blocked_reasons` 为空（或仅含非-deploy-critical 原因）· `completion_reached=true` (若 tr_position=90 且所有 gating 通过)。这是 v3 Q6=B pivot 核心验收 + v4 R1/R2 修正闭环。
26. 只读字段在 tooltip / annotation 中可查 · 无 UI 控件 leak（即 panel 内无 `<input>` 对应只读字段）

### Step E (Polish + tests + artifacts · 2h)
27. Hover tooltip 显示 ≥ 10 个 truth-tracked 节点的 YAML `note` + PDF 段引用
28. Freeze banner 可见 · 文案 `certified · In use · P43-02.5 reference panel`（无 P43-05 pre-lock 措辞）
29. chat.html / chat.js / chat.css 引用加 `?v=<git-short-sha>` cache-bust（v3 C12）
30. **Kogami P43-00 v9 amend**（本 phase 依赖 · 不是 Step E 产出）已 Approved · `tests/test_p43_02_5_c919_panel_schema_alignment.py` 符合父计划 Test Whitelist `test_p43_*` 前缀约定（v3 Q1=A）
31. `tests/test_p43_02_5_c919_panel_schema_alignment.py` PASS · 断言: `#chain-topology-c919-etras` 的 `data-node` attribute 值集合 ⊂ (spec `components` id set ∪ spec `logic_nodes` id set) · annotation 节点不参与
32. Carry-forward artifacts 生成: `.planning/phases/P43-02.5-c919-etras-reference-panel/reference-panel.svg` + `reference-panel-topology.json` (normalized `{nodes:[{id,kind,col,x,y}], edges:[{from,to}]}`)
33. Three-lane regression vs `61b12b3` baseline: 默认 pytest ≥ 800 baseline + N new (N=1 · 即 test_p43_02_5_c919_panel_schema_alignment.py) · E2E pytest ≥ 50 passed · 零 pre-existing regression
34. **All 6 Codex rounds (r1-r6 Steps B/C/D1/D2/E/Closure) returned `可过-Gate`**

## §5. Tier 1 self-audit (C1-C12)

| Counter | Hypothesis | Self-verification |
|---------|-----------|-------------------|
| C1 · phantom source file | References exist? | `c919_etras_adapter.py` ✓ · `c919_etras_hardware_v1.yaml` ✓ · `demo_server.py` ✓ · `chat.{html,js,css}` ✓ · no phantom |
| C2 · §3d whitelist violation | Any unauthorized new file? | **(v3 P3 修正)** schema-alignment test 走 §Q Q1=A → `tests/test_p43_02_5_c919_panel_schema_alignment.py`（严格 `test_p43_*` 前缀约定 · 父计划 P43-00:297-315）· **必须 Kogami P43-00 v9 amend**（Test Whitelist +1）· carry-forward SVG + topology JSON 进 phase 文件夹 · 父计划 `docs/` whitelist 实际不覆盖 `.planning/phases/` per-phase artifacts（P43-02 `reports/` 仅是 precedent 不是 blanket）· **v9 amend 可选同批处理** Docs Whitelist +2（D5=A）· 若 Kogami 拒 docs 扩张则 artifact 改放 `reports/` precedent-backed 路径 |
| C3 · second truth authoring | Panel 是否复述 c919 logic? | NO · §1b 明示 · panel 仅 render truth_evaluation · adapter 仍 authority · annotation 独立但不参与 driving |
| C4 · P43-05 collision | v1 的 pre-lock 已 retract · 只剩 artifact 建议 | Mitigation: P43-05 plan 起草时 Kogami 路径判断是否采纳 · 本 phase 无越权 |
| C5 · Codex fatigue | 5 rounds sustainable? | 与 P43-01 Step G (4 rounds) + P43-02 v1-v3.1 (4 rounds) 相比 · Step A 已降级 · 实际工作 4 round · 合理 |
| C6 · Non-goals leakage | 有没有隐藏扩张? | §1b 显式清单 · 22 只读字段不做 UI 控件 · 不扩 _SUPPORTED_FOR_ANALYSIS · 不改 adapter |
| C7 · Backend regression | 改 SYSTEM_REGISTRY 破既有? | 已落 HEAD · Step A smoke 验证 · 4 既有 adapter 全 pass |
| C8 · hardware YAML 格式不对齐 | YAML 字段与 SVG `data-node` 对齐? | Step E schema-alignment test · 只断言 truth-tracked · annotation 不卷入 |
| C9 · c919 adapter bug 浮现 | UI 建造时发现 adapter 漏 edge case? | Counter F-style 报告 · 不自动扩 scope · 升 Kogami R4 |
| C10 · placeholder 删除风险 | 已 commit `9daa7f6` placeholder 是否被意外覆盖? | 直接替换 `#chain-topology-c919-etras` 内部 DOM · div id / css class 保留 · 旧 placeholder CSS 可一并清理或保留（倾向保留 · diff 更小） |
| **C11 · SVG `<defs>` id 污染 (v3 P4 扩 · 原 v2 C11)** | c919 arrow / filter / clipPath 等 `<defs>` id 复用全局名会冲突? | **(v3 P4)** 所有 c919 panel 内 `<defs>` 子元素 id (marker / filter / clipPath / symbol / linearGradient) 一律 `c919-` 前缀（不只是 marker）· 参考冲突先例: 仓库已有全局 `causal-arrow` def (chat.js:1314-1328)· Step B Exit #10 grep 断言覆盖所有 `<defs>` |
| **C12 · browser cache / stale asset** | 用户刷新仍看到旧 SVG? | Step E Exit #29 加 `?v=<git-short-sha>` cache-bust query · 部署 / 切分支时自动变 |
| **C13 · operate 支路闭环风险 (v3 R1 新)** | chat.js 只消 T1-T4 不够 · operate/reason backend allowlist + 前端 suggestedOverrides 仍然把 c919 当 thrust? | Step D1 必须覆盖 T5-T8 · backend `ALLOWED_SYSTEM_IDS` +c919 + stub operate prompt · 否则用户在 c919 下问 operate 意图会拿到 400 invalid_system_id 或 thrust vocab 错答 · Exit #19/#20/#21/#22 守 |
| **C14 · Q6 pivot 扩 19 可见控件的 regression (v3 R2 新)** | 新加的 tls/pylon/PLS/trcu_power/tr_position 控件会不会意外破 thrust-reverser panel? | 19 控件全部 scoped 在 c919 panel div 内 · CSS class 加 `c919-` 前缀 · 切到 thrust-reverser 时不显示 · Exit #25 "切回 thrust-reverser 时 global-controls 正常显示" 守 regression |

## §6. Gate strategy

**Single gate**: `GATE-P43-02.5-CLOSURE` covering all **6 steps** (Step D split 为 D1+D2 · v3 D6=b).

Submission criteria: **34 Exit Criteria** met + **6 Codex rounds** all `可过-Gate` + three-lane regression green + carry-forward artifacts 上传 phase 文件夹 + **Kogami P43-00 v9 amend Approved**（Delta 1 unit test 必须 + Delta 3 E2E test 必须 · v4 Codex r3 R3 修正 · Delta 2 docs optional）。

Per `/gsd-insert-phase` decimal pattern (not part of Q1=D 4-gate Batch sequence · independent closure gate like P43-01).

## §7. Stop points

1. Step A audit 发现 HEAD 状态不如预期（smoke 失败或 POST endpoint 异常）→ stop · 先修 44967f1 上的 bug 再继续
2. Step B SVG 骨架 layout overlap / arrow-from-nonexistent-node / `<defs>` id 冲突（含 marker + filter + clipPath + symbol）→ stop · path ① fix before C（v3 P4 合并 v2 #2/#3）
3. Step C 发现 `asserted_component_values` 对默认 snapshot 为空（adapter 未 populate） → stop · 记 Counter F · 升 Kogami · 不前端自造 state
4. Step D1 T3/T6 panel snapshot 与聊天/operate snapshot 分叉 → stop · Network tab diff 定位 · 不 merge until byte-identical
5. Step D1 T7 backend `ALLOWED_SYSTEM_IDS` 扩 c919 后其他 system 400 错误 regression → stop · 检查 `_validate_chat_payload` 改动是否意外改 signature（v3 C13）
6. Step D1 T8 operate stub 返非 `manual_steps` action_type → stop · prompt 可能被意外覆盖真 thrust 逻辑
7. Step D2 新 7 控件（tls/pylon/PLS/tr_position/trcu_power）破 thrust-reverser panel → stop · CSS scope 验证（v3 C14）
8. 任一 step > 1.5x estimated effort → stop · escalate Kogami
9. Codex Q7=A `需阻止` verdict on any round → stop · path ① rewrite
10. Three-lane regression 显示 ANY pre-existing panel regression → stop · root-cause（常见: CSS class 名冲撞 / `<defs>` id 冲撞）
11. c919-etras adapter logic gap 发现（Counter F） → 报告 · 不自动扩 scope
12. **Kogami P43-00 v9 amend 被拒**（Test Whitelist / Docs Whitelist 有争议）→ stop · 降 Q1=B 或 Q4=A' · 或 Kogami R4 仲裁（v3 R3 风险）
13. P43-02 Batch 执行（若启动）与本 phase 冲突 → stop · coordinate on same branch

## §8. Governance (Codex per Q7=A · v3 D6=b 扩到 6 rounds)

| # | Step | Scope | Rationale |
|---|------|-------|-----------|
| 1 | 3B | chat.html SVG skeleton 写入 · 22 truth + 8-10 annotation · 所有 `<defs>` id `c919-` scoping | New frontend DOM surface · adapter-boundary |
| 2 | 3C | chat.js c919 state dispatcher · `asserted_component_values` 主路径 + `active_logic_node_ids` 高亮辅助 · annotation 分离 | 状态模型主路径 · multi-file |
| 3 | 3D1 | **chat.js 8 touchpoint + backend ALLOWED_SYSTEM_IDS + operate stub** (v3 D6=b split) | **Boundary-critical**: chat.js + demo_server 双侧 · 聊天/解释/operate 三链路合并 · 高耦合 |
| 4 | 3D2 | **19 可见控件 panel + debounce + reset-timer button** (v3 Q6=B pivot · R2 核心) | UI interaction surface · unlock/deploy 演示闭环 · 跨 c919 HTML/JS/CSS |
| 5 | 3E | polish · test · artifacts · cache-bust · **Kogami amend closure 验证** | Test file delta · whitelist decision · closure readiness |
| 6 | Closure | Combined P43-02.5 sanity | Single-phase closure review |

Total: **6 Codex rounds** · 全部 adapter-boundary / boundary-critical 跨文件耦合点。

## §Q. Open questions (for Codex r2 + Kogami)

### Q1 · schema-alignment test 收纳位置（v3 R3 pivot · B→A · 依赖 Kogami P43-00 v9 amend）

**断言定义**（v2 已修 truth-tracked only · v3 保留）:
- 输入: `#chain-topology-c919-etras` 内 `<svg>` 的所有 `data-node` 属性值（不含 `data-annotation="true"` 的节点）
- 断言: 每个 `data-node` 值 ∈ (adapter spec `components` id set · 17 个) ∪ (`logic_nodes` id set · 5 个)
- **不** 断言 YAML sensor / logic_thresholds / timing keys（那是原始 snapshot keys · 非 node ids）

**v4 Codex r3 R3 扩**: v3 Q1=A 只加 Delta 1 schema-alignment unit test · Codex r3 指出此 test 仅断言静态 `data-node ⊂ spec ids`，不覆盖运行时 DOM/event/POST flow · 必须另加 Delta 3 E2E test 跑 Exit #25 脚本。v4 v9 amend 扩到 **3 delta** (D5=A' 批量):

- **A (v4 选定 · 扩 v3)** · **Kogami amend P43-00 v9 §3d Test Whitelist** · 新增 **2 行**: 
  1. `tests/test_p43_02_5_c919_panel_schema_alignment.py` (Delta 1 · unit · 静态 schema · 见 §9 v9-amend-request.md)
  2. `tests/test_p43_02_5_c919_panel_e2e_deploy_flow.py` (Delta 3 · E2E `@pytest.mark.e2e` · runtime flow · 脚本化 Exit #25 · v4 新增)
  - 两 test 均严格 `test_p43_*` prefix 约定 · 加 Docs Whitelist +2 (Delta 2 optional) 一并 batch · **3 delta · 一次 Kogami roundtrip**
- **B (retracted · v2 路径)** · v2 选 B 的理由（"adapter whitelist 已含"）误述 · 不推荐
- **C (unchanged · 不推荐)** · 放弃自动化 · drift 会悄悄回归

**Executor 决策**: **A** (扩版 · 3 delta)。

### Q2 · Panel 激活时触发的 POST 频率

- **A** · 150ms debounce · **Executor 建议**
- **B** · 300ms debounce (更保守 · 感知延迟高)
- **C** · requestAnimationFrame throttle (~16ms) · 过高负载

### Q3 · Freeze banner 措辞

- **A** · `CERTIFIED · In Use — C919 TRCU (甲方) · P34+P38 真实 PDF · SHA256 locked · P43-02.5 reference panel` · **Executor 建议**（无 P43-05 pre-lock）
- **B** · 短版 `CERTIFIED · P43-02.5 reference panel`
- **C** · 无 banner · 仅顶部 eyebrow label

### Q4 · P43-05 carry-forward artifacts（v3 D5=A · 与 Q1 一并 Kogami v9 amend）

v1 错位已 retract（不再写 "P43-05 Exit Criteria 将追加"）· v3 补充 docs whitelist 治理路径:

- **A (v3 选定)** · 落 2 artifact 到 phase 文件夹（`reference-panel.svg` + `reference-panel-topology.json`）· P43-02.5 closure note 写 "available for P43-05 plan to optionally adopt" · P43-05 自主决定 · **Kogami P43-00 v9 amend 包含 Docs Whitelist +2 显式授权**（D5=A · 与 Q1 同批 · 一次 roundtrip）· 若 Kogami 拒 docs 扩张 → 降级到路径 A' 把 artifact 放到 `reports/p43-02-5-c919-reference-panel/` (precedent-backed by P43-02 `reports/`)
- **B (not chosen)** · 不产生 artifact · P43-05 起草时重做 · 浪费
- **C (not chosen)** · 只产生 SVG 不产生 JSON · diff-match 只能 raw DOM · 脆弱

**Executor 决策**: **A**（首选 · 若 Kogami 拒则降 A'）

### Q5 · canvasAdjustmentsSupported + c919 global controls 深度（v3 D2=A 统一 · P5 内撞修复）

T4 touchpoint v2 存在内撞：§1a T4 措辞 "仍必须标 c919 为有 panel interaction" vs Q5-B 措辞 "return false for c919" — v3 统一为:

- **A (v3 选定)** · **hide + return false**: c919 下 `#canvas-global-controls` `display:none`（CSS or JS）· `canvasAdjustmentsSupported()` 返 **false** for c919 · panel 内控件是 c919 唯一交互入口 · T4 scope 极简（1 行 JS + 1 CSS rule）· **前置依赖**: 检查 `canvasAdjustmentsSupported()` 下游所有调用点（chat.js `collectCanvasGlobalOverrides` etc.）在 c919 返 false 时不会触发 thrust-specific behavior · Step D1 实作前 5min grep 验证
- **B (retracted · v2 路径)** · keep-true-and-hide · 自撞 + 留治理间隙（Codex r2 P5）· 不选
- **C (not chosen)** · 延迟 P43-05 · 风险: explain/operate 分叉

**Executor 决策**: **A**。

### Q6 · 34-field 可见/只读 UI 分割（v3 D1=B pivot · A→B · R2 核心）

v2 Q6=A（22 全只读）会锁死 unlock/deploy 演示链（Codex r2 R2）· v3 pivot 到 **12 + 7 = 19 可见 + ~15 只读**:

- **A (v2 路径 · retracted)** · 22 全只读 · tls/pls/pylon/tr_position/trcu_power/timer 无 UI · panel 最多证明前段 gating · **不符合用户"完整开发"要求**
- **B (v3 选定)** · **19 可见（12 核心 + 7 unlock/deploy gating 扩展）**: +2 TLS unlock toggle / +3 PLS+pylon unlock toggle(composite) / +1 tr_position slider / +1 trcu_power toggle / +1 reset-timer-latches button（atomic 设 4 timer 到 confirm 阈值 · 因为 server-computed countdown 默认 `tr_stowed_locked_confirm_s=2.0` 会永 reset CMD3 · adapter:1285-1289）· 剩 ~15 只读（timer 4 server-computed + 一些冗余 LS_valid 恒 true 字段 + vdt_sensor_valid + prev_eicu_cmd3）· **Executor 决策**
- **C (not chosen)** · 全 34 controls + "scenario preset" dropdown · scope 扩张最大 · 留给 P43-05 考虑

**Executor 决策**: **B** · 见 §2c 新表。

## §9. Signature

**Signed**: Claude App Opus 4.7 (Solo Executor · v5.2 solo-signed + v5.3 addendum) · 2026-04-21

**Revision**: v4 (post Codex r3 · 需修正·信号强 closed · 4 required + 3 polish fixed · R3/§2a/Q5 confirmed closed)

**Classification**: Decimal-inserted phase · parallel to P43-02 Batch · carry-forward artifact provider to P43-05 (not pre-lock) · **blocks on Kogami P43-00 v9 amend** (Test Whitelist +2 mandatory + Docs Whitelist +2 optional · 3 delta batched)

**Estimated wall-time**: **12h** (0.5+3+2+3+2+1.5 spread over evening + tomorrow)

**Delta from v3**: 4 required + 3 polish surgical fixes · **scope unchanged** (8 touchpoint + 19 controls unchanged · 仅 plan 文字修正 + E2E test whitelist entry 扩)。核心: R1 timer button 方向翻转（Advance deploy latches · keep CMD3 latched）· R2 Exit #25 完整 preconditions（TRA=-14 + atltla/apwtla=true + 7 unlock controls + button + 断言全 4 ln_* active）· R3 v9 amend 扩 Delta 3 E2E test `test_p43_02_5_c919_panel_e2e_deploy_flow.py`（`@pytest.mark.e2e`）· R4 Exit #20 去 "帮我" 前缀避免 isGeneralQuestion false-positive · 3 polish (stale counts + phantom component_nodes)

**Next action**: (1) 提交 **Kogami P43-00 v9 amend** request（Test Whitelist +2 + 可选 Docs Whitelist +2 · 3 delta batched · 文档 at `.planning/phases/P43-02.5-c919-etras-reference-panel/P43-00-v9-amend-request.md`）· (2) Submit v4 for Codex r4 adversarial review (Q7=A) · 若 r4 `可过-Gate` → 进 Step B（Step A audit 已完 · 可与 Kogami v9 amend 审批并行 · v9 amend 只 block Step E）· 若 r4 `需修正·信号弱` → path ① 微调 then Step B · 若 `需修正·信号强` → path ① rewrite v5 before B · 若多轮仍不闭 → 升 Kogami R4 仲裁
