# P6-01 Plan - Sync Control-Tower Truth And Freeze Packet Baseline

## Objective

Refresh the control-tower's stale status story and create a freeze/demo packet baseline that reflects the latest P5 GitHub-backed evidence without adding any new demo functionality.

## Context

- `P5` has already shifted the demo from presenter-ready into edge-case-hardened, with GitHub-backed smoke coverage across prompts, presets, and key blocker toggles.
- The Notion `01 当前状态` page still says `129 tests OK` and recommends manual browser drag/toggle QA, which no longer matches the actual state of the project.
- The project constitution explicitly favors verifiability, stability, and preserving the controller-truth boundary over new feature expansion.
- After `P5` approval, the next highest-value work is to close the gap between current truth and current documentation/control-plane messaging.

## Tasks

1. Update the control-tower status surfaces and local summary docs so they reflect the actual P5 evidence baseline, including latest GitHub-backed test and smoke coverage.
2. Create a concise freeze/demo packet that summarizes the current demo route, GitHub evidence links, truth-boundary reminders, and the single remaining human signoff step.
3. Retire or relabel stale manual-browser-QA wording so it is clearly archived presenter guidance, not an active approval requirement.

## Success Criteria

- The main status surfaces no longer contradict the latest validated P5 state.
- A reviewer or presenter can open one concise packet and understand the current demo evidence, boundary conditions, and final signoff posture without reading local terminal files.
- No API, controller, or demo-surface contracts change while closing the documentation/control-plane gap.
