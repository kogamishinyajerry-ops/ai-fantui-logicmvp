# P6-05 Plan - Sync Repo-Side Coordination Docs From The Live Snapshot

## Objective

Bring the active repo-side coordination docs onto the same GitHub-backed live snapshot used by the Notion dashboard, status page, and freeze packet, so users stop landing on stale `129 tests OK` / manual-browser-QA wording in repo handoff materials.

## Context

- `P6-02` through `P6-04` aligned the active Notion-facing surfaces, but several repo-side coordination docs still present old Round 92 guidance as if it were current truth.
- Those repo docs are still practical handoff surfaces, so leaving them stale recreates the same control-plane drift on the filesystem side.
- The fix should preserve historical notes while making the current baseline obvious and automation-owned.

## Tasks

1. Extend repo-side sync with managed markdown sections for the active coordination docs and freeze packet.
2. Regenerate the active repo docs from the same live review snapshot used by the current Notion sync flow.
3. Verify the generated docs show the current P6 baseline while preserving older round notes below as historical context.

## Success Criteria

- `docs/coordination/plan.md`, `docs/coordination/dev_handoff.md`, `docs/coordination/qa_report.md`, and the repo freeze packet gain auto-synced current-baseline sections.
- The active repo docs no longer present stale `129 tests OK` / manual-browser-QA language as the current project state.
- Historical round records remain available below the managed snapshot instead of being silently deleted.
