# P6-17 Plan - Lift The Stronger QA Baseline Back To The Homepage

## Objective

Restore the richer shared validation baseline to the live dashboard/freeze/handoff text, even after newer control-plane maintenance runs advance the latest plan with narrower targeted checks.

## Context

- `P6-15` let narrow maintenance runs stop overwriting the latest verified plan, and `P6-16` made slow review refreshes bounded.
- But the current homepage/freeze text still shows `PASS. 1/1 shared validation checks pass.` because the repo snapshot being carried forward was already weakened before preservation logic had a stronger source to reuse.
- The stronger baseline still exists in repo history (`175 tests OK`, `10 demo smoke scenarios pass`, `8/8 shared validation checks pass`). We need to actively lift that history back into the live snapshot selection path.

## Tasks

1. Extend compact-summary parsing so repo history files can yield a normalized QA summary from human-readable archive text.
2. Add repo-history summary selection that chooses the strongest shared validation baseline from current + archived freeze/QA artifacts.
3. Teach snapshot preference and repo-doc recovery to use that stronger baseline when the latest maintenance run is narrower than the historical full validation chain.

## Success Criteria

- Repo/notion homepage text shows the stronger shared QA baseline again instead of a narrow `1/1` maintenance summary.
- The latest verified plan and latest success run can still point at the newest P6 maintenance slice.
- Unit tests cover archive parsing, stronger-summary selection, and snapshot preference behavior.
