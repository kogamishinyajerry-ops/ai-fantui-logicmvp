# P6-19 Plan - Neutralize Historical Bootstrap Examples In The Notion Guide

## Objective

Remove the last concrete stale project-state examples from the repo-side Notion bootstrap guide so it can only be read as a historical/reference template, not as a live status source.

## Context

- `P6-18` fixed the active control-plane links and page-rotation drift, and the bootstrap guide now has a top-level warning that it is historical/reference-only.
- But the guide body still contains concrete example values such as `Round 92`, `129 tests OK`, and old validation-record titles inside “recommended initialization content” sections.
- Even with the top warning, those examples still look like current truth when someone jumps into the middle of the document or copies a subsection in isolation.

## Tasks

1. Replace the remaining concrete old-state examples in `docs/notion/ai_fantui_logicmvp_notion_hub.md` with neutral placeholders or generic template labels.
2. Reword the initialization sections so they explicitly tell the reader to fill in the current real state at setup time instead of copying historical sample values.
3. Record the slice in GSD state and refresh the control plane against a real validation command so the newest P6 truth reflects the cleanup.

## Success Criteria

- The bootstrap guide no longer contains body text that reads like the current project is still on `Round 92` or `129 tests OK`.
- Initialization examples are phrased as templates, not current-state recommendations.
- The control plane advances to a new P6 slice without weakening the stronger shared QA baseline.
