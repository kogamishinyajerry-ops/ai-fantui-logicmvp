# P6-04 Plan - Route Active Status Links To An Auto-Synced Replacement Page

## Objective

Bypass the old archived-ancestor `01 当前状态` page with a new repo-managed status page that can be fully rewritten by automation, so the active control-plane links stop landing on a stale surface.

## Context

- `P6-02` fixed the homepage snapshot and `P6-03` fixed the freeze packet snapshot.
- The last visible stale surface is the old `01 当前状态` page, which neither the local integration nor MCP can reliably update because its blocks live under an archived ancestor.
- Users should not have to understand Notion ancestry problems just to read the live project state.

## Tasks

1. Create a new active status page under the control-tower root that is owned by the current integrations.
2. Extend repo-side sync so it rewrites that replacement status page from the same live review snapshot used by 09C, the dashboard, and the freeze packet.
3. Point control-plane links at the new status page and verify the user-visible surfaces no longer depend on the stale archived page.

## Success Criteria

- The control-plane config points `pages.status` at an auto-synced status page that can be rewritten by repo-side sync.
- Dashboard, 09C, and freeze packet all link to the active replacement status page rather than the stale archived one.
- The remaining P6 drift is narrowed to roadmap/database rows that are still blocked by archived ancestors, not to the user-facing status surface.
