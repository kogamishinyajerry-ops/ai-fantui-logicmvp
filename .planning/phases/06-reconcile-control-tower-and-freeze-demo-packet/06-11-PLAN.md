# P6-11 Plan - Let Repo Docs Follow The Fresher Dashboard Snapshot

## Objective

Keep repo-side handoff and freeze docs aligned with the freshest live control-plane snapshot, even when local database reads lag behind the dashboard that GitHub-backed writeback already refreshed.

## Context

- `P6-10` made dashboard-only degraded mode explicit and pushed a fresh GitHub-backed dashboard snapshot onto Notion.
- Repo-side `plan.md`, `dev_handoff.md`, and the freeze packet can still fall back to an older database snapshot, which leaves them stuck on `P6-07` even after the dashboard is already on `P6-10`.
- During P6 closeout, the dashboard is the most trustworthy current-state surface when database freshness and page freshness disagree.

## Tasks

1. Compare the database-derived snapshot and the dashboard/page-derived snapshot during repo-doc sync.
2. Prefer the fresher page snapshot when its GitHub success run or default-plan alignment is newer than the database snapshot.
3. Re-run repo-doc sync so active repo docs move from the stale `P6-07` baseline onto the current `P6-10` baseline.

## Success Criteria

- Repo-side coordination and freeze docs stop lagging behind the dashboard when page-backed evidence is fresher than database-backed evidence.
- The sync logic still keeps database-backed snapshots when they are at least as fresh as the dashboard.
- Active repo docs update to the current P6 baseline without requiring Opus intervention.
