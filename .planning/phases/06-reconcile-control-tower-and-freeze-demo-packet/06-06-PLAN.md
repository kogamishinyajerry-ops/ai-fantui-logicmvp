# P6-06 Plan - Move Historical Repo Handoff Prose Out Of Active Docs

## Objective

Keep the active repo-side coordination packet focused on the current GitHub-backed baseline by moving old Round prose into explicit archive files, so readers stop wading through stale history before they reach the live truth.

## Context

- `P6-05` introduced managed current-baseline sections at the top of the active repo docs, but those files still carried long historical bodies inline below the snapshot.
- Leaving those old sections in the active docs keeps stale `129 tests OK` / manual-browser-QA-era language only a few scrolls away from the current truth.
- The historical content is still useful for audit and replay, so it should be preserved, but it should live in clearly labeled archive files instead of the active handoff surfaces.

## Tasks

1. Split the older long-form repo coordination and freeze-packet prose into dedicated archive markdown files.
2. Reduce the active repo docs to the managed live snapshot plus a short explanation of current use and archive locations.
3. Re-run repo-doc sync and validation to confirm the active docs still render the current P6 baseline after the archive split.

## Success Criteria

- `docs/coordination/plan.md`, `docs/coordination/dev_handoff.md`, `docs/coordination/qa_report.md`, and `docs/freeze/2026-04-10-freeze-demo-packet.md` become slim active surfaces that prioritize the live snapshot.
- Historical repo-side prose remains preserved in explicit archive files instead of being deleted.
- The active docs no longer mix current truth and old Round guidance in the same scrolling surface.
