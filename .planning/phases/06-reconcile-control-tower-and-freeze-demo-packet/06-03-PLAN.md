# P6-03 Plan - Auto-Sync The Freeze Demo Packet Snapshot

## Objective

Pull the freeze/demo packet into the same repo-managed snapshot loop as 09C and the homepage dashboard, so the handoff surface always reflects the latest approved phase, evidence run, and QA baseline instead of drifting behind the live control-plane truth.

## Context

- `P6-01` created the freeze/demo packet and `P6-02` fixed the first-screen dashboard drift.
- The freeze packet is now one of the main handoff artifacts, so stale evidence there would recreate the same control-plane mismatch under a different page title.
- The local integration can read/write the freeze packet page directly even though `01 当前状态` still lacks page-block access.

## Tasks

1. Extend `tools/gsd_notion_sync.py` with a freeze-packet rendering path that uses the same live review snapshot as 09C and the dashboard.
2. Reuse the managed-section pattern so dashboard and freeze packet snapshots share the same repo-side insertion/removal behavior.
3. Refresh the freeze packet through that path and verify the top summary now reflects the current P6 baseline.
4. Ensure missing Notion database/page sharing cannot turn an otherwise green validation run into a failing CI result.

## Success Criteria

- The freeze/demo packet exposes a repo-managed top snapshot with the current active phase, latest verified plan, latest success evidence, QA summary, and gate posture.
- Dashboard and freeze packet managed sections share the same synchronization pattern instead of duplicating bespoke block-mutation logic.
- Remaining drift is narrowed to explicitly known integration-scope gaps, not silent stale summaries.
- GitHub Actions stays green when code validation passes but Notion writeback is temporarily blocked by integration scope.
