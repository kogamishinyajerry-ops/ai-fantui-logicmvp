# P6-08 Plan - Clean Duplicate Active-Page Bodies And Bloated Run Summaries

## Objective

Finish the remaining P6 control-plane readability cleanup so the active Notion dashboard, status page, and freeze packet show one current snapshot each and summarize successful validation evidence concisely instead of dumping raw JSON-heavy output.

## Context

- `P6-07` made the active pages self-healing under partial Notion database access, but the user-facing surfaces can still look messy after repeated syncs.
- The dashboard and freeze packet still carry older manual snapshot bodies below the current top section, which makes the control tower feel duplicated and harder to trust at a glance.
- Successful run and QA summaries still tend to mirror raw validation output, which is useful for machines but too noisy for the control-plane surfaces humans actually read.

## Tasks

1. Replace append-style active-page snapshot updates with full-body rewrites for the dashboard and freeze packet so stale duplicate prose cannot remain below the current snapshot.
2. Generate compact success summaries from validation results and use them for run / QA / fallback snapshot notes instead of raw output digests when the run succeeds.
3. Refresh the active Notion pages and repo-side coordination docs, then verify that the control plane now shows one clean snapshot per surface with concise evidence lines.

## Success Criteria

- The dashboard and freeze packet no longer show older duplicated snapshot bodies after a sync.
- Successful run, QA, status, and freeze summaries use short human-readable evidence lines such as test count, smoke coverage, and shared-check pass count.
- The shared validation suite still passes, and the refreshed Notion/repo surfaces stay aligned to the latest GitHub-backed plan.
