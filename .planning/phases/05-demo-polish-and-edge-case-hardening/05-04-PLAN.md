# P5-04 Plan - Fast Condition Toggle Smoke Sweep

## Objective

Verify the visible blocker toggles through GitHub-verifiable smoke coverage so rapid condition changes in the demo cockpit are backed by automated evidence instead of manual browser-only confidence.

## Context

- `P5-03` widened the smoke suite across the visible presenter presets.
- Phase P5 still explicitly calls out `fast condition toggles` as a target for regression protection.
- The demo cockpit exposes visible toggles for `engine_running`, `aircraft_on_ground`, `reverser_inhibited`, and `eec_enable`.
- Those toggles already map to meaningful blocker states in the live demo, so the next hardening slice should assert those blocker semantics through the existing HTTP evidence plane.

## Tasks

1. Extend `tools/demo_path_smoke.py` with a toggle-sweep scenario that exercises the visible blocker toggles and checks the expected blocker field plus chain state.
2. Update regression coverage and smoke output expectations to reflect the new toggle-sweep evidence.
3. Retarget local control-plane state to `P5-04` and document the widened smoke coverage.

## Success Criteria

- The demo smoke suite proves the visible blocker toggles through `POST /api/lever-snapshot` without browser automation.
- The toggle sweep stays stable inside the shared validation loop and GitHub Actions.
- The smoke report makes it clear which visible blocker toggles are now protected by automated evidence.
