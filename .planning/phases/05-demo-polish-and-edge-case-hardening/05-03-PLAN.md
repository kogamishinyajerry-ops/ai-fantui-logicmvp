# P5-03 Plan - Visible Lever Preset Smoke Sweep

## Objective

Turn the first-screen visible lever presets into GitHub-verifiable demo evidence so the most common presenter jump points are validated without relying on manual browser-only confidence checks.

## Context

- `P5-01` established an HTTP smoke suite for core prompt, clamp, mode-switch, and invalid-input paths.
- `P5-02` hardened the browser shell so stale responses cannot overwrite a newer presenter action.
- The UI still exposes four visible presenter presets: `L3 等待 VDT90`, `RA blocker`, `N1K blocker`, and `VDT90 ready`.
- Those presets are central to the live demo route, so P5 should verify their backend-aligned semantics directly through the existing `POST /api/lever-snapshot` evidence plane.

## Tasks

1. Extend `tools/demo_path_smoke.py` with preset-focused scenarios that assert the expected blocker or ready-state semantics for the visible lever presets.
2. Update regression tests and validation expectations so the shared suite protects the wider preset sweep automatically.
3. Point local control-plane state at `P5-03` and document the expanded smoke evidence surface.

## Success Criteria

- The demo smoke suite verifies the visible presenter presets through `POST /api/lever-snapshot` without browser automation.
- The widened preset sweep remains stable inside the shared validation loop and GitHub Actions.
- The smoke report and tests make it clear which visible presenter presets are covered and what semantic state each one proves.
