# P8-05 Plan - Connect Second-System Smoke To The Adapter-Backed Runtime Proof

## Objective

Promote the P8 landing-gear adapter chain into the reusable second-system smoke entrypoint, so the default smoke proof now exercises adapter-backed runtime generalization instead of stopping at the older intake-packet bundle path.

## Context

- `P8-01` through `P8-04` proved that a minimal landing-gear adapter can publish metadata/spec and drive playback, diagnosis, and knowledge contracts.
- The shared `second-system-smoke` entrypoint still defaults to the older packet/bundle flow from P7, so the main reusable smoke surface has not yet absorbed the runtime proof chain.
- The safest slice is to repoint the default smoke proof to the adapter-backed landing-gear path while keeping an explicit legacy intake-packet mode available for regression coverage.

## Tasks

1. Update the second-system smoke contract/CLI so the default proof mode is adapter-backed runtime proof, with an explicit legacy intake-packet mode still available.
2. Add focused tests and schema validation proving both the new default runtime path and the retained legacy packet path still validate cleanly.
3. Refresh the shared control-plane rule text so Notion surfaces inherit the new controller/adapters/runner/FlyByWire guardrails before writeback.

## Success Criteria

- `well_harness.cli second-system-smoke --format json` defaults to a passing adapter-backed landing-gear runtime proof.
- `tools/validate_second_system_smoke_schema.py --format json` validates both the default adapter-backed path and the explicit legacy packet mode.
- The auto-synced control-plane rule section now reflects the updated project guide, including controller truth, runner orchestration, adapter-only truth admission, and FlyByWire reference rules.
